@RefCounted
struct TcpStream {
  handle: i64,
}

@RefCounted
struct TcpListener {
  handle: i64,
}

@RefCounted
struct UdpSocket {
  handle: i64,
}

struct AsyncUdpSocket {
  socket: UdpSocket,
}

struct AsyncTcpStream {
  stream: TcpStream,
}

struct AsyncTcpListener {
  listener: TcpListener,
}

extern function __otter_net_resolve_async(host: str): Future<str>;
extern function __otter_net_tcp_connect_async(addr: str): Future<str>;
extern function __otter_net_tcp_connect_timeout_async(addr: str, nanos: i64): Future<str>;
extern function lang_net_tcp_stream_release(handle: i64);
extern function __otter_net_tcp_stream_close_async(handle: i64): Future<str>;
extern function __otter_net_tcp_stream_peer_addr_async(handle: i64): Future<str>;
extern function __otter_net_tcp_stream_local_addr_async(handle: i64): Future<str>;
extern function __otter_net_tcp_stream_take_error_async(handle: i64): Future<str>;
extern function __otter_net_tcp_stream_nodelay_async(handle: i64): Future<str>;
extern function __otter_net_tcp_stream_set_nodelay_async(handle: i64, on: i64): Future<str>;
extern function __otter_net_tcp_stream_set_nonblocking_async(handle: i64, on: i64): Future<str>;
extern function __otter_net_tcp_stream_read_timeout_async(handle: i64): Future<str>;
extern function __otter_net_tcp_stream_set_read_timeout_async(
  handle: i64,
  nanos: i64,
  present: i64
): Future<str>;
extern function __otter_net_tcp_stream_write_timeout_async(handle: i64): Future<str>;
extern function __otter_net_tcp_stream_set_write_timeout_async(
  handle: i64,
  nanos: i64,
  present: i64
): Future<str>;
extern function __otter_net_tcp_stream_ttl_async(handle: i64): Future<str>;
extern function __otter_net_tcp_stream_set_ttl_async(handle: i64, ttl: i64): Future<str>;
extern function __otter_net_tcp_stream_read_async(handle: i64, count: i64): Future<str>;
extern function __otter_net_tcp_stream_read_to_end_async(handle: i64): Future<str>;
extern function __otter_net_tcp_stream_write_async(handle: i64, contents_hex: str): Future<str>;
extern function __otter_net_tcp_stream_write_all_async(handle: i64, contents_hex: str): Future<str>;
extern function __otter_net_tcp_stream_flush_async(handle: i64): Future<str>;
extern function __otter_net_tcp_stream_peek_async(handle: i64, count: i64): Future<str>;
extern function lang_net_tcp_listener_release(handle: i64);
extern function __otter_net_tcp_listener_bind_async(addr: str): Future<str>;
extern function __otter_net_tcp_listener_close_async(handle: i64): Future<str>;
extern function __otter_net_tcp_listener_accept_async(handle: i64): Future<str>;
extern function __otter_net_tcp_listener_local_addr_async(handle: i64): Future<str>;
extern function __otter_net_tcp_listener_take_error_async(handle: i64): Future<str>;
extern function __otter_net_tcp_listener_set_nonblocking_async(handle: i64, on: i64): Future<str>;
extern function __otter_net_tcp_listener_ttl_async(handle: i64): Future<str>;
extern function __otter_net_tcp_listener_set_ttl_async(handle: i64, ttl: i64): Future<str>;
extern function lang_net_udp_release(handle: i64);
extern function __otter_net_udp_bind_async(addr: str): Future<str>;
extern function __otter_net_udp_close_async(handle: i64): Future<str>;
extern function __otter_net_udp_connect_async(handle: i64, addr: str): Future<str>;
extern function __otter_net_udp_local_addr_async(handle: i64): Future<str>;
extern function __otter_net_udp_peer_addr_async(handle: i64): Future<str>;
extern function __otter_net_udp_take_error_async(handle: i64): Future<str>;
extern function __otter_net_udp_set_nonblocking_async(handle: i64, on: i64): Future<str>;
extern function __otter_net_udp_read_timeout_async(handle: i64): Future<str>;
extern function __otter_net_udp_set_read_timeout_async(
  handle: i64,
  nanos: i64,
  present: i64
): Future<str>;
extern function __otter_net_udp_write_timeout_async(handle: i64): Future<str>;
extern function __otter_net_udp_set_write_timeout_async(
  handle: i64,
  nanos: i64,
  present: i64
): Future<str>;
extern function __otter_net_udp_ttl_async(handle: i64): Future<str>;
extern function __otter_net_udp_set_ttl_async(handle: i64, ttl: i64): Future<str>;
extern function __otter_net_udp_broadcast_async(handle: i64): Future<str>;
extern function __otter_net_udp_set_broadcast_async(handle: i64, on: i64): Future<str>;
extern function __otter_net_udp_multicast_loop_v4_async(handle: i64): Future<str>;
extern function __otter_net_udp_set_multicast_loop_v4_async(handle: i64, on: i64): Future<str>;
extern function __otter_net_udp_multicast_loop_v6_async(handle: i64): Future<str>;
extern function __otter_net_udp_set_multicast_loop_v6_async(handle: i64, on: i64): Future<str>;
extern function __otter_net_udp_multicast_ttl_v4_async(handle: i64): Future<str>;
extern function __otter_net_udp_set_multicast_ttl_v4_async(handle: i64, ttl: i64): Future<str>;
extern function __otter_net_udp_join_multicast_v4_async(handle: i64, group: str, iface: str): Future<str>;
extern function __otter_net_udp_leave_multicast_v4_async(handle: i64, group: str, iface: str): Future<str>;
extern function __otter_net_udp_join_multicast_v6_async(handle: i64, group: str, iface: i64): Future<str>;
extern function __otter_net_udp_leave_multicast_v6_async(handle: i64, group: str, iface: i64): Future<str>;
extern function __otter_net_udp_send_async(handle: i64, contents_hex: str): Future<str>;
extern function __otter_net_udp_recv_async(handle: i64, count: i64): Future<str>;
extern function __otter_net_udp_peek_async(handle: i64, count: i64): Future<str>;
extern function __otter_net_udp_send_to_async(
  handle: i64,
  contents_hex: str,
  addr: str
): Future<str>;
extern function __otter_net_udp_recv_from_async(handle: i64, count: i64): Future<str>;
extern function __otter_net_udp_peek_from_async(handle: i64, count: i64): Future<str>;

struct __StdNetField {
  text: str,
  next: i64,
}

function __std_net_io_error(message: str): IoError {
  IoError { kind: OtherIo {}, message: message }
}

function __std_net_payload(result: str): str {
  if result.size() <= 1 { "" } else { result.substring(1, result.size()) }
}

function __std_net_is_error(result: str): bool {
  result.size() == 0 || result.substring(0, 1) == "1"
}

function __std_net_decode_text(result: str): str | IoError {
  if __std_net_is_error(result) {
    __std_net_io_error(__std_net_payload(result))
  } else {
    __std_net_payload(result)
  }
}

function __std_net_decode_unit(result: str): null | IoError {
  if __std_net_is_error(result) {
    __std_net_io_error(__std_net_payload(result))
  } else {
    null
  }
}

function __std_net_decode_optional_error(result: str): IoError | null {
  if __std_net_is_error(result) {
    __std_net_io_error(__std_net_payload(result))
  } else {
    var payload = __std_net_payload(result);
    if payload == "0" {
      null
    } else if payload.size() > 0 && payload.substring(0, 1) == "1" {
      __std_net_io_error(__std_net_payload(payload))
    } else {
      __std_net_io_error("malformed std:net provider payload")
    }
  }
}

function __std_net_digit_at(text: str, index: i64): i64 | IoError {
  var ch = ' ';
  for c in text.substring(index, index + 1).chars() {
    ch = c;
  }
  var cp = ch as i64;
  if cp >= 48 && cp <= 57 {
    cp - 48
  } else {
    __std_net_io_error("malformed std:net provider payload")
  }
}

function __std_net_parse_i64(text: str): i64 | IoError {
  if text == "" {
    __std_net_io_error("empty decimal value in std:net provider payload")
  } else {
    var value = 0;
    var i = 0;
    while i < text.size() {
      var digit = __std_net_digit_at(text, i);
      if digit is IoError {
        return digit as IoError;
      }
      if value > 922337203685477580 || (value == 922337203685477580 && (digit as i64) > 7) {
        return __std_net_io_error("decimal value in std:net provider payload overflows i64");
      }
      value = value * 10 + (digit as i64);
      i = i + 1;
    }
    value
  }
}

function __std_net_decode_i64(result: str): i64 | IoError {
  var text = __std_net_decode_text(result);
  if text is IoError {
    text as IoError
  } else {
    __std_net_parse_i64(text as str)
  }
}

function __std_net_decode_bool(result: str): bool | IoError {
  var value = __std_net_decode_i64(result);
  if value is IoError {
    value as IoError
  } else {
    (value as i64) != 0
  }
}

function __std_net_decode_u32(result: str): u32 | IoError {
  var value = __std_net_decode_i64(result);
  if value is IoError {
    value as IoError
  } else if (value as i64) < 0 || (value as i64) > 4294967295 {
    __std_net_io_error("u32 value in std:net provider payload is out of range")
  } else {
    (value as i64) as u32
  }
}

function __std_net_decode_optional_duration(result: str): Duration | null | IoError {
  var text = __std_net_decode_text(result);
  if text is IoError {
    text as IoError
  } else if (text as str) == "n" {
    null
  } else {
    var nanos = __std_net_parse_i64(text as str);
    if nanos is IoError {
      nanos as IoError
    } else {
      Duration.from_nanos(nanos as i64)
    }
  }
}

function __std_net_duration_nanos_or_zero(timeout: Duration | null): i64 {
  if timeout is Duration { (timeout as Duration).as_nanos() } else { 0 }
}

function __std_net_duration_present(timeout: Duration | null): i64 {
  if timeout is Duration { 1 } else { 0 }
}

function __std_net_hex_digit(value: u8): str {
  var n = value as i64;
  var cp = if n < 10 { 48 + n } else { 87 + n };
  (cp as char) as str
}

function __std_net_bytes_to_hex(data: Bytes): str {
  var out = "";
  for b in data.iter() {
    out = out + __std_net_hex_digit((b / 16u8) as u8) + __std_net_hex_digit((b % 16u8) as u8);
  }
  out
}

function __std_net_hex_value(ch: char): u8 | IoError {
  var cp = ch as i64;
  if cp >= 48 && cp <= 57 {
    (cp - 48) as u8
  } else if cp >= 97 && cp <= 102 {
    (cp - 87) as u8
  } else if cp >= 65 && cp <= 70 {
    (cp - 55) as u8
  } else {
    __std_net_io_error("invalid hexadecimal digit in std:net byte payload")
  }
}

function __std_net_hex_char_at(text: str, index: i64): u8 | IoError {
  var result:
    u8 |
    IoError = __std_net_io_error("missing hexadecimal digit in std:net byte payload");
  for ch in text.substring(index, index + 1).chars() {
    result = __std_net_hex_value(ch);
  }
  result
}

function __std_net_hex_to_bytes(text: str): Bytes | IoError {
  if text.size() % 2 != 0 {
    __std_net_io_error("odd-length std:net byte payload")
  } else {
    var out = Bytes.new();
    var i = 0;
    while i < text.size() {
      var hi = __std_net_hex_char_at(text, i);
      if hi is IoError {
        return hi as IoError;
      }
      var lo = __std_net_hex_char_at(text, i + 1);
      if lo is IoError {
        return lo as IoError;
      }
      out.push(((hi as u8) * 16u8 + (lo as u8)) as u8);
      i = i + 2;
    }
    out
  }
}

function __std_net_decode_bytes(result: str): Bytes | IoError {
  var text = __std_net_decode_text(result);
  if text is IoError {
    text as IoError
  } else {
    __std_net_hex_to_bytes(text as str)
  }
}

function __std_net_read_field(payload: str, start: i64): __StdNetField | IoError {
  var i = start;
  var len = 0;
  var digits = 0;
  while i<payload.size() && payload.substring(i, i + 1) != ":" {
    var digit = __std_net_digit_at(payload, i);
    if digit is IoError {
      return digit as IoError;
    }
    len = len * 10 + (digit as i64);
    digits = digits + 1;
    i = i + 1;
  }

  if digits == 0 || i >= payload.size() {
    __std_net_io_error("malformed std:net provider payload")
  } else {
    var text_start = i + 1;
    var text_end = text_start + len;
    if text_end > payload.size() {
      __std_net_io_error("truncated std:net provider payload")
    } else {
      __StdNetField { text: payload.substring(text_start, text_end), next: text_end }
    }
  }
}

function __std_net_parse_socket_addr(text: str): SocketAddr | IoError {
  var parsed = parse_socket_addr(text);
  if parsed is ParseError {
    __std_net_io_error("provider returned invalid socket address: " + (parsed as ParseError).to_str())
  } else {
    parsed as SocketAddr
  }
}

function __std_net_decode_datagram(result: str, buf: Bytes): (i64, SocketAddr) | IoError {
  var decoded = __std_net_decode_text(result);
  if decoded is IoError {
    decoded as IoError
  } else {
    var payload = decoded as str;
    var bytes_field = __std_net_read_field(payload, 0);
    if bytes_field is IoError {
      return bytes_field as IoError;
    }
    var addr_field = __std_net_read_field(payload, (bytes_field as __StdNetField).next);
    if addr_field is IoError {
      return addr_field as IoError;
    }
    if (addr_field as __StdNetField).next != payload.size() {
      return __std_net_io_error("trailing bytes in std:net provider payload");
    }
    var data = __std_net_hex_to_bytes((bytes_field as __StdNetField).text);
    if data is IoError {
      return data as IoError;
    }
    var addr = __std_net_parse_socket_addr((addr_field as __StdNetField).text);
    if addr is IoError {
      return addr as IoError;
    }
    var chunk = data as Bytes;
    for b in chunk.iter() {
      buf.push(b);
    }
    var received_pair = (chunk.size(), addr as SocketAddr);
    received_pair
  }
}

function __std_net_parse_ip(text: str): IpAddr | IoError {
  var parsed = if text.contains(":") {
    parse_ip_v6(text)
  } else {
    parse_ip_v4(text)
  };
  if parsed is ParseError {
    __std_net_io_error("provider returned invalid IP address: " + (parsed as ParseError).to_str())
  } else {
    parsed as IpAddr
  }
}

function resolve(host: str): Future<List<IpAddr> | IoError> async {
  var result = await __otter_net_resolve_async(host);
  if __std_net_is_error(result) {
    __std_net_io_error(__std_net_payload(result))
  } else {
    var out = List<IpAddr>();
    var payload = __std_net_payload(result);
    var pos = 0;
    while pos < payload.size() {
      var field = __std_net_read_field(payload, pos);
      if field is IoError {
        return field as IoError;
      }
      var ip = __std_net_parse_ip((field as __StdNetField).text);
      if ip is IoError {
        return ip as IoError;
      }
      out.push(ip as IpAddr);
      pos = (field as __StdNetField).next;
    }
    out
  }
}

extend TcpStream {
  function connect(addr: SocketAddr): Future<TcpStream | IoError> async {
    var raw = await __otter_net_tcp_connect_async(addr.to_str());
    var handle = __std_net_decode_i64(raw);
    if handle is IoError {
      handle as IoError
    } else {
      TcpStream { handle: handle as i64 }
    }
  }

  function connect_timeout(addr: SocketAddr, timeout: Duration): Future<TcpStream | IoError> async {
    var raw = await __otter_net_tcp_connect_timeout_async(
        addr.to_str(),
        timeout.as_nanos()
      );
    var handle = __std_net_decode_i64(raw);
    if handle is IoError {
      handle as IoError
    } else {
      TcpStream { handle: handle as i64 }
    }
  }

  function peer_addr(self): Future<SocketAddr | IoError> async {
    var raw = await __otter_net_tcp_stream_peer_addr_async(self.handle);
    var text = __std_net_decode_text(raw);
    if text is IoError {
      text as IoError
    } else {
      __std_net_parse_socket_addr(text as str)
    }
  }

  function local_addr(self): Future<SocketAddr | IoError> async {
    var raw = await __otter_net_tcp_stream_local_addr_async(self.handle);
    var text = __std_net_decode_text(raw);
    if text is IoError {
      text as IoError
    } else {
      __std_net_parse_socket_addr(text as str)
    }
  }

  function nodelay(self): Future<bool | IoError> async {
    var raw = await __otter_net_tcp_stream_nodelay_async(self.handle);
    __std_net_decode_bool(raw)
  }

  function take_error(self): Future<IoError | null> async {
    var raw = await __otter_net_tcp_stream_take_error_async(self.handle);
    __std_net_decode_optional_error(raw)
  }

  function set_nodelay(self, on: bool): Future<null | IoError> async {
    var raw = await __otter_net_tcp_stream_set_nodelay_async(self.handle, if on { 1 } else { 0 });
    __std_net_decode_unit(raw)
  }

  function set_nonblocking(self, on: bool): Future<null | IoError> async {
    var raw = await __otter_net_tcp_stream_set_nonblocking_async(self.handle, if on { 1 } else { 0 });
    __std_net_decode_unit(raw)
  }

  function read_timeout(self): Future<Duration | null | IoError> async {
    var raw = await __otter_net_tcp_stream_read_timeout_async(self.handle);
    __std_net_decode_optional_duration(raw)
  }

  function set_read_timeout(self, timeout: Duration | null): Future<null | IoError> async {
    var raw = await __otter_net_tcp_stream_set_read_timeout_async(
        self.handle,
        __std_net_duration_nanos_or_zero(timeout),
        __std_net_duration_present(timeout)
      );
    __std_net_decode_unit(raw)
  }

  function write_timeout(self): Future<Duration | null | IoError> async {
    var raw = await __otter_net_tcp_stream_write_timeout_async(self.handle);
    __std_net_decode_optional_duration(raw)
  }

  function set_write_timeout(self, timeout: Duration | null): Future<null | IoError> async {
    var raw = await __otter_net_tcp_stream_set_write_timeout_async(
        self.handle,
        __std_net_duration_nanos_or_zero(timeout),
        __std_net_duration_present(timeout)
      );
    __std_net_decode_unit(raw)
  }

  function ttl(self): Future<u32 | IoError> async {
    var raw = await __otter_net_tcp_stream_ttl_async(self.handle);
    __std_net_decode_u32(raw)
  }

  function set_ttl(self, ttl: u32): Future<null | IoError> async {
    var raw = await __otter_net_tcp_stream_set_ttl_async(self.handle, ttl as i64);
    __std_net_decode_unit(raw)
  }

  function close(self): Future<null | IoError> async {
    var raw = await __otter_net_tcp_stream_close_async(self.handle);
    __std_net_decode_unit(raw)
  }

  function peek(self, buf: Bytes): Future<i64 | IoError> async {
    var raw = await __otter_net_tcp_stream_peek_async(self.handle, buf.size());
    var data = __std_net_decode_bytes(raw);
    if data is IoError {
      data as IoError
    } else {
      var chunk = data as Bytes;
      for b in chunk.iter() {
        buf.push(b);
      }
      chunk.size()
    }
  }

  function read(self, buf: Bytes): Future<i64 | IoError> async {
    var raw = await __otter_net_tcp_stream_read_async(self.handle, buf.size());
    var data = __std_net_decode_bytes(raw);
    if data is IoError {
      data as IoError
    } else {
      var chunk = data as Bytes;
      for b in chunk.iter() {
        buf.push(b);
      }
      chunk.size()
    }
  }

  function read_to_end(self, buf: Bytes): Future<i64 | IoError> async {
    var raw = await __otter_net_tcp_stream_read_to_end_async(self.handle);
    var data = __std_net_decode_bytes(raw);
    if data is IoError {
      data as IoError
    } else {
      var chunk = data as Bytes;
      for b in chunk.iter() {
        buf.push(b);
      }
      chunk.size()
    }
  }

  function write(self, buf: Bytes): Future<i64 | IoError> async {
    var raw = await __otter_net_tcp_stream_write_async(
        self.handle,
        __std_net_bytes_to_hex(buf)
      );
    __std_net_decode_i64(raw)
  }

  function write_all(self, buf: Bytes): Future<null | IoError> async {
    var raw = await __otter_net_tcp_stream_write_all_async(
        self.handle,
        __std_net_bytes_to_hex(buf)
      );
    __std_net_decode_unit(raw)
  }

  function flush(self): Future<null | IoError> async {
    var raw = await __otter_net_tcp_stream_flush_async(self.handle);
    __std_net_decode_unit(raw)
  }
}

extend TcpStream: Clone {
  function clone(self): TcpStream { self }
}

extend TcpStream: Drop {
  function drop(self) {
    lang_net_tcp_stream_release(self.handle);
  }
}

extend TcpStream: ToStr {
  function to_str(self): str {
    "TcpStream { handle: " + (self.handle as str) + " }"
  }
}

extend TcpStream: Debug {
  function debug(self): str { self.to_str() }
}

extend AsyncTcpStream: AsyncReader + AsyncWriter {
  function connect(addr: SocketAddr): Future<AsyncTcpStream | IoError> async {
    var raw = await __otter_net_tcp_connect_async(addr.to_str());
    var handle = __std_net_decode_i64(raw);
    if handle is IoError {
      handle as IoError
    } else {
      AsyncTcpStream { stream: TcpStream { handle: handle as i64 } }
    }
  }

  function connect_timeout(addr: SocketAddr, timeout: Duration): Future<AsyncTcpStream | IoError> async {
    var raw = await __otter_net_tcp_connect_timeout_async(addr.to_str(), timeout.as_nanos());
    var handle = __std_net_decode_i64(raw);
    if handle is IoError {
      handle as IoError
    } else {
      AsyncTcpStream { stream: TcpStream { handle: handle as i64 } }
    }
  }

  function from_stream(stream: TcpStream): AsyncTcpStream {
    AsyncTcpStream { stream: stream.clone() }
  }

  function into_stream(self): TcpStream {
    self.stream.clone()
  }

  function peer_addr(self): Future<SocketAddr | IoError> async {
    await self.stream.peer_addr()
  }

  function local_addr(self): Future<SocketAddr | IoError> async {
    await self.stream.local_addr()
  }

  function close(self): Future<null | IoError> async {
    await self.stream.close()
  }

  function peek_async(self, buf: Bytes): Future<i64 | IoError> async {
    var raw = await __otter_net_tcp_stream_peek_async(
        self.stream.handle,
        buf.size()
      );
    var data = __std_net_decode_bytes(raw);
    if data is IoError {
      data as IoError
    } else {
      var chunk = data as Bytes;
      for b in chunk.iter() {
        buf.push(b);
      }
      chunk.size()
    }
  }

  function read_async(self, buf: Bytes): Future<i64 | IoError> async {
    var raw = await __otter_net_tcp_stream_read_async(
        self.stream.handle,
        buf.size()
      );
    var data = __std_net_decode_bytes(raw);
    if data is IoError {
      data as IoError
    } else {
      var chunk = data as Bytes;
      for b in chunk.iter() {
        buf.push(b);
      }
      chunk.size()
    }
  }

  function write_async(self, buf: Bytes): Future<i64 | IoError> async {
    var raw = await __otter_net_tcp_stream_write_async(
        self.stream.handle,
        __std_net_bytes_to_hex(buf)
      );
    __std_net_decode_i64(raw)
  }
}

extend AsyncTcpStream: Clone {
  function clone(self): AsyncTcpStream {
    AsyncTcpStream { stream: self.stream.clone() }
  }
}

extend AsyncTcpStream: ToStr {
  function to_str(self): str {
    "Async" + self.stream.to_str()
  }
}

extend AsyncTcpStream: Debug {
  function debug(self): str { self.to_str() }
}

function __std_net_decode_accept(result: str): (TcpStream, SocketAddr) | IoError {
  var decoded = __std_net_decode_text(result);
  if decoded is IoError {
    decoded as IoError
  } else {
    var payload = decoded as str;
    var handle_field = __std_net_read_field(payload, 0);
    if handle_field is IoError {
      return handle_field as IoError;
    }
    var addr_field = __std_net_read_field(payload, (handle_field as __StdNetField).next);
    if addr_field is IoError {
      return addr_field as IoError;
    }
    if (addr_field as __StdNetField).next != payload.size() {
      return __std_net_io_error("trailing bytes in std:net provider payload");
    }
    var handle = __std_net_parse_i64((handle_field as __StdNetField).text);
    if handle is IoError {
      return handle as IoError;
    }
    var addr = __std_net_parse_socket_addr((addr_field as __StdNetField).text);
    if addr is IoError {
      return addr as IoError;
    }
    var accepted_pair = (TcpStream { handle: handle as i64 }, addr as SocketAddr);
    accepted_pair
  }
}

extend TcpListener {
  function bind(addr: SocketAddr): Future<TcpListener | IoError> async {
    var raw = await __otter_net_tcp_listener_bind_async(addr.to_str());
    var handle = __std_net_decode_i64(raw);
    if handle is IoError {
      handle as IoError
    } else {
      TcpListener { handle: handle as i64 }
    }
  }

  function accept(self): Future<(TcpStream, SocketAddr) | IoError> async {
    var raw = await __otter_net_tcp_listener_accept_async(self.handle);
    __std_net_decode_accept(raw)
  }

  function local_addr(self): Future<SocketAddr | IoError> async {
    var raw = await __otter_net_tcp_listener_local_addr_async(self.handle);
    var text = __std_net_decode_text(raw);
    if text is IoError {
      text as IoError
    } else {
      __std_net_parse_socket_addr(text as str)
    }
  }

  function take_error(self): Future<IoError | null> async {
    var raw = await __otter_net_tcp_listener_take_error_async(self.handle);
    __std_net_decode_optional_error(raw)
  }

  function set_nonblocking(self, on: bool): Future<null | IoError> async {
    var raw = await __otter_net_tcp_listener_set_nonblocking_async(
        self.handle,
        if on { 1 } else { 0 }
      );
    __std_net_decode_unit(raw)
  }

  function ttl(self): Future<u32 | IoError> async {
    var raw = await __otter_net_tcp_listener_ttl_async(self.handle);
    __std_net_decode_u32(raw)
  }

  function set_ttl(self, ttl: u32): Future<null | IoError> async {
    var raw = await __otter_net_tcp_listener_set_ttl_async(self.handle, ttl as i64);
    __std_net_decode_unit(raw)
  }

  function close(self): Future<null | IoError> async {
    var raw = await __otter_net_tcp_listener_close_async(self.handle);
    __std_net_decode_unit(raw)
  }
}

extend TcpListener: Clone {
  function clone(self): TcpListener { self }
}

extend TcpListener: Drop {
  function drop(self) {
    lang_net_tcp_listener_release(self.handle);
  }
}

extend TcpListener: ToStr {
  function to_str(self): str {
    "TcpListener { handle: " + (self.handle as str) + " }"
  }
}

extend TcpListener: Debug {
  function debug(self): str { self.to_str() }
}

extend AsyncTcpListener {
  function bind(addr: SocketAddr): Future<AsyncTcpListener | IoError> async {
    var listener = await TcpListener.bind(addr);
    if listener is IoError {
      listener as IoError
    } else {
      AsyncTcpListener { listener: listener as TcpListener }
    }
  }

  function from_listener(listener: TcpListener): AsyncTcpListener {
    AsyncTcpListener { listener: listener.clone() }
  }

  function into_listener(self): TcpListener {
    self.listener.clone()
  }

  function local_addr(self): Future<SocketAddr | IoError> async {
    await self.listener.local_addr()
  }

  function take_error(self): Future<IoError | null> async {
    await self.listener.take_error()
  }

  function ttl(self): Future<u32 | IoError> async {
    await self.listener.ttl()
  }

  function set_ttl(self, ttl: u32): Future<null | IoError> async {
    await self.listener.set_ttl(ttl)
  }

  function close(self): Future<null | IoError> async {
    await self.listener.close()
  }

  function accept_async(self): Future<(AsyncTcpStream, SocketAddr) | IoError> async {
    var accepted = await self.listener.accept();
    if accepted is IoError {
      accepted as IoError
    } else {
      var pair = accepted as (TcpStream, SocketAddr);
      var async_pair = (AsyncTcpStream.from_stream(pair.0), pair.1);
      async_pair
    }
  }
}

extend AsyncTcpListener: Clone {
  function clone(self): AsyncTcpListener {
    AsyncTcpListener { listener: self.listener.clone() }
  }
}

extend AsyncTcpListener: ToStr {
  function to_str(self): str {
    "Async" + self.listener.to_str()
  }
}

extend AsyncTcpListener: Debug {
  function debug(self): str { self.to_str() }
}

extend UdpSocket {
  function bind(addr: SocketAddr): Future<UdpSocket | IoError> async {
    var raw = await __otter_net_udp_bind_async(addr.to_str());
    var handle = __std_net_decode_i64(raw);
    if handle is IoError {
      handle as IoError
    } else {
      UdpSocket { handle: handle as i64 }
    }
  }

  function local_addr(self): Future<SocketAddr | IoError> async {
    var raw = await __otter_net_udp_local_addr_async(self.handle);
    var text = __std_net_decode_text(raw);
    if text is IoError {
      text as IoError
    } else {
      __std_net_parse_socket_addr(text as str)
    }
  }

  function connect(self, addr: SocketAddr): Future<null | IoError> async {
    var raw = await __otter_net_udp_connect_async(self.handle, addr.to_str());
    __std_net_decode_unit(raw)
  }

  function peer_addr(self): Future<SocketAddr | IoError> async {
    var raw = await __otter_net_udp_peer_addr_async(self.handle);
    var text = __std_net_decode_text(raw);
    if text is IoError {
      text as IoError
    } else {
      __std_net_parse_socket_addr(text as str)
    }
  }

  function ttl(self): Future<u32 | IoError> async {
    var raw = await __otter_net_udp_ttl_async(self.handle);
    __std_net_decode_u32(raw)
  }

  function take_error(self): Future<IoError | null> async {
    var raw = await __otter_net_udp_take_error_async(self.handle);
    __std_net_decode_optional_error(raw)
  }

  function set_nonblocking(self, on: bool): Future<null | IoError> async {
    var raw = await __otter_net_udp_set_nonblocking_async(self.handle, if on { 1 } else { 0 });
    __std_net_decode_unit(raw)
  }

  function read_timeout(self): Future<Duration | null | IoError> async {
    var raw = await __otter_net_udp_read_timeout_async(self.handle);
    __std_net_decode_optional_duration(raw)
  }

  function set_read_timeout(self, timeout: Duration | null): Future<null | IoError> async {
    var raw = await __otter_net_udp_set_read_timeout_async(
        self.handle,
        __std_net_duration_nanos_or_zero(timeout),
        __std_net_duration_present(timeout)
      );
    __std_net_decode_unit(raw)
  }

  function write_timeout(self): Future<Duration | null | IoError> async {
    var raw = await __otter_net_udp_write_timeout_async(self.handle);
    __std_net_decode_optional_duration(raw)
  }

  function set_write_timeout(self, timeout: Duration | null): Future<null | IoError> async {
    var raw = await __otter_net_udp_set_write_timeout_async(
        self.handle,
        __std_net_duration_nanos_or_zero(timeout),
        __std_net_duration_present(timeout)
      );
    __std_net_decode_unit(raw)
  }

  function set_ttl(self, ttl: u32): Future<null | IoError> async {
    var raw = await __otter_net_udp_set_ttl_async(self.handle, ttl as i64);
    __std_net_decode_unit(raw)
  }

  function broadcast(self): Future<bool | IoError> async {
    var raw = await __otter_net_udp_broadcast_async(self.handle);
    __std_net_decode_bool(raw)
  }

  function set_broadcast(self, on: bool): Future<null | IoError> async {
    var raw = await __otter_net_udp_set_broadcast_async(self.handle, if on { 1 } else { 0 });
    __std_net_decode_unit(raw)
  }

  function multicast_loop_v4(self): Future<bool | IoError> async {
    var raw = await __otter_net_udp_multicast_loop_v4_async(self.handle);
    __std_net_decode_bool(raw)
  }

  function set_multicast_loop_v4(self, on: bool): Future<null | IoError> async {
    var raw = await __otter_net_udp_set_multicast_loop_v4_async(self.handle, if on { 1 } else { 0 });
    __std_net_decode_unit(raw)
  }

  function multicast_loop_v6(self): Future<bool | IoError> async {
    var raw = await __otter_net_udp_multicast_loop_v6_async(self.handle);
    __std_net_decode_bool(raw)
  }

  function set_multicast_loop_v6(self, on: bool): Future<null | IoError> async {
    var raw = await __otter_net_udp_set_multicast_loop_v6_async(self.handle, if on { 1 } else { 0 });
    __std_net_decode_unit(raw)
  }

  function multicast_ttl_v4(self): Future<u32 | IoError> async {
    var raw = await __otter_net_udp_multicast_ttl_v4_async(self.handle);
    __std_net_decode_u32(raw)
  }

  function set_multicast_ttl_v4(self, ttl: u32): Future<null | IoError> async {
    var raw = await __otter_net_udp_set_multicast_ttl_v4_async(self.handle, ttl as i64);
    __std_net_decode_unit(raw)
  }

  function join_multicast_v4(self, group: IpAddr, iface: IpAddr): Future<null | IoError> async {
    var raw = await __otter_net_udp_join_multicast_v4_async(
        self.handle,
        group.to_str(),
        iface.to_str()
      );
    __std_net_decode_unit(raw)
  }

  function leave_multicast_v4(self, group: IpAddr, iface: IpAddr): Future<null | IoError> async {
    var raw = await __otter_net_udp_leave_multicast_v4_async(
        self.handle,
        group.to_str(),
        iface.to_str()
      );
    __std_net_decode_unit(raw)
  }

  function join_multicast_v6(self, group: IpAddr, iface: u32): Future<null | IoError> async {
    var raw = await __otter_net_udp_join_multicast_v6_async(self.handle, group.to_str(), iface as i64);
    __std_net_decode_unit(raw)
  }

  function leave_multicast_v6(self, group: IpAddr, iface: u32): Future<null | IoError> async {
    var raw = await __otter_net_udp_leave_multicast_v6_async(
        self.handle,
        group.to_str(),
        iface as i64
      );
    __std_net_decode_unit(raw)
  }

  function send_to(self, buf: Bytes, addr: SocketAddr): Future<i64 | IoError> async {
    var raw = await __otter_net_udp_send_to_async(
        self.handle,
        __std_net_bytes_to_hex(buf),
        addr.to_str()
      );
    __std_net_decode_i64(raw)
  }

  function send(self, buf: Bytes): Future<i64 | IoError> async {
    var raw = await __otter_net_udp_send_async(
        self.handle,
        __std_net_bytes_to_hex(buf)
      );
    __std_net_decode_i64(raw)
  }

  function recv(self, buf: Bytes): Future<i64 | IoError> async {
    var raw = await __otter_net_udp_recv_async(self.handle, buf.size());
    var data = __std_net_decode_bytes(raw);
    if data is IoError {
      data as IoError
    } else {
      var chunk = data as Bytes;
      for b in chunk.iter() {
        buf.push(b);
      }
      chunk.size()
    }
  }

  function peek(self, buf: Bytes): Future<i64 | IoError> async {
    var raw = await __otter_net_udp_peek_async(self.handle, buf.size());
    var data = __std_net_decode_bytes(raw);
    if data is IoError {
      data as IoError
    } else {
      var chunk = data as Bytes;
      for b in chunk.iter() {
        buf.push(b);
      }
      chunk.size()
    }
  }

  function recv_from(self, buf: Bytes): Future<(i64, SocketAddr) | IoError> async {
    var raw = await __otter_net_udp_recv_from_async(self.handle, buf.size());
    __std_net_decode_datagram(raw, buf)
  }

  function peek_from(self, buf: Bytes): Future<(i64, SocketAddr) | IoError> async {
    var raw = await __otter_net_udp_peek_from_async(self.handle, buf.size());
    __std_net_decode_datagram(raw, buf)
  }

  function close(self): Future<null | IoError> async {
    var raw = await __otter_net_udp_close_async(self.handle);
    __std_net_decode_unit(raw)
  }
}

extend AsyncUdpSocket {
  function bind(addr: SocketAddr): Future<AsyncUdpSocket | IoError> async {
    var socket = await UdpSocket.bind(addr);
    if socket is IoError {
      socket as IoError
    } else {
      AsyncUdpSocket { socket: socket as UdpSocket }
    }
  }

  function from_socket(socket: UdpSocket): AsyncUdpSocket {
    AsyncUdpSocket { socket: socket.clone() }
  }

  function into_socket(self): UdpSocket {
    self.socket.clone()
  }

  function local_addr(self): Future<SocketAddr | IoError> async {
    await self.socket.local_addr()
  }

  function connect(self, addr: SocketAddr): Future<null | IoError> async {
    await self.socket.connect(addr)
  }

  function peer_addr(self): Future<SocketAddr | IoError> async {
    await self.socket.peer_addr()
  }

  function ttl(self): Future<u32 | IoError> async {
    await self.socket.ttl()
  }

  function take_error(self): Future<IoError | null> async {
    await self.socket.take_error()
  }

  function set_ttl(self, ttl: u32): Future<null | IoError> async {
    await self.socket.set_ttl(ttl)
  }

  function broadcast(self): Future<bool | IoError> async {
    await self.socket.broadcast()
  }

  function set_broadcast(self, on: bool): Future<null | IoError> async {
    await self.socket.set_broadcast(on)
  }

  function multicast_loop_v4(self): Future<bool | IoError> async {
    await self.socket.multicast_loop_v4()
  }

  function set_multicast_loop_v4(self, on: bool): Future<null | IoError> async {
    await self.socket.set_multicast_loop_v4(on)
  }

  function multicast_loop_v6(self): Future<bool | IoError> async {
    await self.socket.multicast_loop_v6()
  }

  function set_multicast_loop_v6(self, on: bool): Future<null | IoError> async {
    await self.socket.set_multicast_loop_v6(on)
  }

  function multicast_ttl_v4(self): Future<u32 | IoError> async {
    await self.socket.multicast_ttl_v4()
  }

  function set_multicast_ttl_v4(self, ttl: u32): Future<null | IoError> async {
    await self.socket.set_multicast_ttl_v4(ttl)
  }

  function join_multicast_v4(self, group: IpAddr, iface: IpAddr): Future<null | IoError> async {
    await self.socket.join_multicast_v4(group, iface)
  }

  function leave_multicast_v4(self, group: IpAddr, iface: IpAddr): Future<null | IoError> async {
    await self.socket.leave_multicast_v4(group, iface)
  }

  function join_multicast_v6(self, group: IpAddr, iface: u32): Future<null | IoError> async {
    await self.socket.join_multicast_v6(group, iface)
  }

  function leave_multicast_v6(self, group: IpAddr, iface: u32): Future<null | IoError> async {
    await self.socket.leave_multicast_v6(group, iface)
  }

  function send_to_async(self, buf: Bytes, addr: SocketAddr): Future<i64 | IoError> async {
    var raw = await __otter_net_udp_send_to_async(
        self.socket.handle,
        __std_net_bytes_to_hex(buf),
        addr.to_str()
      );
    __std_net_decode_i64(raw)
  }

  function send_async(self, buf: Bytes): Future<i64 | IoError> async {
    var raw = await __otter_net_udp_send_async(
        self.socket.handle,
        __std_net_bytes_to_hex(buf)
      );
    __std_net_decode_i64(raw)
  }

  function recv_async(self, buf: Bytes): Future<i64 | IoError> async {
    var raw = await __otter_net_udp_recv_async(self.socket.handle, buf.size());
    var data = __std_net_decode_bytes(raw);
    if data is IoError {
      data as IoError
    } else {
      var chunk = data as Bytes;
      for b in chunk.iter() {
        buf.push(b);
      }
      chunk.size()
    }
  }

  function peek_async(self, buf: Bytes): Future<i64 | IoError> async {
    var raw = await __otter_net_udp_peek_async(self.socket.handle, buf.size());
    var data = __std_net_decode_bytes(raw);
    if data is IoError {
      data as IoError
    } else {
      var chunk = data as Bytes;
      for b in chunk.iter() {
        buf.push(b);
      }
      chunk.size()
    }
  }

  function recv_from_async(self, buf: Bytes): Future<(i64, SocketAddr) | IoError> async {
    var raw = await __otter_net_udp_recv_from_async(self.socket.handle, buf.size());
    __std_net_decode_datagram(raw, buf)
  }

  function peek_from_async(self, buf: Bytes): Future<(i64, SocketAddr) | IoError> async {
    var raw = await __otter_net_udp_peek_from_async(self.socket.handle, buf.size());
    __std_net_decode_datagram(raw, buf)
  }

  function close(self): Future<null | IoError> async {
    await self.socket.close()
  }
}

extend AsyncUdpSocket: Clone {
  function clone(self): AsyncUdpSocket {
    AsyncUdpSocket { socket: self.socket.clone() }
  }
}

extend AsyncUdpSocket: ToStr {
  function to_str(self): str {
    "Async" + self.socket.to_str()
  }
}

extend AsyncUdpSocket: Debug {
  function debug(self): str { self.to_str() }
}

extend UdpSocket: Clone {
  function clone(self): UdpSocket { self }
}

extend UdpSocket: Drop {
  function drop(self) {
    lang_net_udp_release(self.handle);
  }
}

extend UdpSocket: ToStr {
  function to_str(self): str {
    "UdpSocket { handle: " + (self.handle as str) + " }"
  }
}

extend UdpSocket: Debug {
  function debug(self): str { self.to_str() }
}
