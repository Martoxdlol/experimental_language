/goal implement the programming language end to end. Follow the design. Do not take shortcuts. Read docs and respect choices, ask questions if things are not clear. We are building a programming language at the same level of rust. NOT A DEMO, A FULL WORKING PROGRAMMING LANGUAGE. Backend in cranelift with ALL proposed and designed features working 100% with good design. NO SHORTCUTS. Do tests, continue with the project. Always do good practices and make sure the design is fast and efficient.

/goal analyze project structure, read source files, rethink project structure and reorganize if needed to prevent organization & structural issues. Ensure no regressions.

/goal implement the programming language end to end. Continue working on every feature, every system, every part of the language. Follow the design. Do not take shortcuts. Read docs and respect choices, ask questions if things are not clear. We are building a programming language at the same level of rust. NOT A DEMO, A FULL WORKING PROGRAMMING LANGUAGE. Backend in cranelift with ALL proposed and designed features working 100% with good design. NO SHORTCUTS. Do tests, continue with the project. Always do good practices and make sure the design is fast and efficient. Keep code clean and organized.

/goal refactor channel implementation. Make channels async and non-blocking. Ensure they work with the async runtime. Update docs to reflect this. Update example. Update any code or anything needed.

/goal refactor threads api. Ensure it uses async and it is non-blocking. Update docs to reflect this. Update example. Update any code or anything needed.

/goal refactor async features. Ensure it is fully working, follow the way javascript or dart does it. No blocking functions, use async await syntax. Futures. And stuff. Do not change the language design without asking first. This is to adjust stuff not to remake everything. Add support for a spawn keyword that will work like tokio spawn (returning a future) and disallow at all calling async function without capturing the future. Update docs to reflect this. Update example. Update any code or anything needed.

/goal Apply language branding, this language is now officially called "Otter Fusion", file extensions is `.otter`. Keep most code as agnostic to the language name as possible but use the name in the places needed (like the extension)

/goal I don't want to see the name "lang" ever again. The idea is that the language code is as agnostic as possible. BUT, I DON'T WANT TO SEE "lang" as a NOT CLEAR placeholder anywhere were the real name which is already established should be. Replace WITHOUT USING BASH OR GREP all places where we are using "lang" for "otter-fusion" or "otter_fusion" (use _ for cli or commands).

/goal Refactor to the architecture in README: introduce a typed, resolved, desugared HIR that the type-checker produces and codegen + LSP consume, retiring the ~40 Span-keyed CheckResults tables (they become HIR node fields). Build it at full Rust/C/Java quality — no shortcuts, no skipped or deferred hard cases. Staged, test-gated: (1) define complete HIR; (2) lower AST+CheckResults→HIR losslessly; (3) repoint codegen to HIR only; (4) repoint LSP; (5) checker emits HIR directly, delete CheckResults table-by-table. TEST CONSTANTLY AND HEAVILY: write many, many new tests all the time — unit + integration + e2e — covering every HIR node, every lowering, every desugar, every pass, and every edge case; run the full suite after every change; keep ALL existing tests green at every step and NEVER advance on red. Every node preserves its source span (provenance through every lowering). Debuggability/observability are first-class: add --emit=tokens|ast|hir|clif with stable pretty-printers + DWARF source-level debug info for built programs. Monomorphization stays at the HIR→codegen worklist; MIR is a separate later project. Read docs + ROADMAP first; keep ROADMAP updated.

/goal Implement all module, import and package features end to end, exactly as specified in docs/17-modules and docs/23-cli (§7 dependency model, §8 manifest). NO SHORTCUTS — production quality at the level of rust/cargo. Implement: the explicit import scheme system (core:, std:, pkg:, self: root + self:./ self:../ relative, file:) with full resolution and the self:-cannot-escape-package rule; the file: allowlist gating via the manifest in project mode and unrestricted file: in direct mode; the near-empty prelude rule (built-in syntax desugars to core:prelude fully-qualified, every named symbol requires an import); run modes (run, exec, project context vs direct execution with the reachability walk) including the hard errors when pkg:/self:-root are used without project context. Implement the package manager: project.toml manifest parsing ([package], [dependencies] with version/path/git/registry forms, [features], [registry] default + [registries]), the lockfile (project.lock) with exact versions + sources + sha256 checksums and verification, the registry protocol (sparse HTTP index, config.json dl/api/auth-required, tarball fetch + content-addressed store keyed by sha256), semver resolution (one version per compatible range), and the dependency CLI commands (add, remove, update, tree, why, vendor, lock, search, publish, yank, login/logout, audit). Honor the reserved/deferred decisions in 17.14 (no pkg+https form; blobs and pkg-in-direct-mode deferred; url:/http:/https: reserved). Do many tests — unit + integration + e2e covering every scheme, every resolution path, allowlist enforcement, lockfile + checksum verification, registry resolution, and error cases. Keep docs, examples, LSP and ROADMAP consistent and up to date; read docs + ROADMAP first and ask if anything is unclear.

/goal Implement rigorous for each feature, each edge case. Implement passing and failing tests. Write them as different source files as end to end tests. Include the expected output. Organize it as a testing framework. Don't be shy, based on docs make organized tests cases for every single feature and every single edge case and every single error case you can find. Also include timing on those tests. Add a utility on the run command to measure execution time (without compilation time). Basically create a comprehensive test suite. This must include every single compiler feature, must check cases of failed compilation, cases it compiles but shouldn't, try to break memory, concurrency ALL!!!!

/goal Implement user procedural macros end to end, exactly as specified in docs/22 (only `@Derive(...)` exists today). Production quality at the level of rust's macro system — no shortcuts, no skipped hard cases. Follow the docs for the macro declaration syntax, hygiene, expansion phase ordering (lex → parse → macro-expand → resolve, per docs/22 §4), and error reporting with correct spans on synthesized nodes (mind the virtual-FileId unique-span rule used by derive/defaults/anf). Ask first before any design change. Do many tests — unit + integration + e2e covering definition, invocation, hygiene, nested/recursive expansion, and macro errors. Keep docs, examples, LSP (completion/hover/goto across macro boundaries as far as feasible) and ROADMAP consistent and up to date.

/goal Generalize the channel-endpoint reference-counting facility into a user-facing OPT-IN deterministic reference-counted object kind (`@RefCounted`, Swift-ARC style), so any annotated type gets DETERMINISTIC `Drop` at strong-count 0 — promoting the deterministic-finalization carve-out that channel `Sender`/`Receiver` handles already use (see ROADMAP "deterministic endpoint release" + docs/16 §8) from a hardcoded runtime special-case into a real language feature. Reuse the existing reusable refcount primitives (runtime acquire/release intrinsics + the codegen scope-exit release emitter + the count-0 callback) built for channels; do NOT special-case per type. Implement at full Rust/Swift quality, no shortcuts: a refcounted-object header/descriptor variant carrying the strong count; ARC-style retain/release insertion across codegen (bind/copy/param/return/capture + heap stores via the docs/16 §7 write barriers); count→0 runs the type's `Drop` synchronously then frees; and the tracing GC retained as the CYCLE-collector backstop (refcounting leaks cycles — the collector reclaims them, Python-style). Ask first before finalizing the surface syntax / any language-design decision (this is a genuine spec addition — add a new docs section and reconcile with docs/16 §8). Do many tests — unit + integration + e2e covering retain/release balance, deterministic drop ordering, cycle reclamation by the GC backstop, captures/heap-stored refcounted values, cross-thread refcounted handles, and GC-stress. Keep docs, examples, LSP and ROADMAP consistent and up to date.

[DONE] /goal Make `Shared<T>` locks ALWAYS ASYNC, implementing the LOCKED design in docs/20 §4 and the detailed spec in ROADMAP.md → "What's next" → "Async `Shared<T>` lock" (items 1–6 + subsumed tests) — follow both exactly, don't re-open the settled decisions. Summary: `lock`/`try_lock` become `lock<R>(self,body):Future<R> async` / `try_lock<R>(self,body):Future<R|LockBusy> async`, awaited by callers (today they thread-block in `crates/runtime/src/shared.rs`). One async-mutex primitive (no `RwLock`); acquire suspends the task via a FIFO waiter queue, never parks the OS thread; the mutex is HELD across the body's `await`s (fix the footgun where an async body releases before the awaited work runs), flattening so `R` is the final awaited value; release on cancel/panic via a per-task held-lock set; `Thread.spawn` workers can't lock (compile error → use `spawn`); a new escape/detachment taint pass rejects references that outlive the body, `.clone()` detaches, and a returned ref is cloned at the boundary while held. Subsumes the known-bug escape tests (remove `known-bug` markers so they error) and return clone-out. Rust/tokio quality, no shortcuts; ask first only before NEW design beyond docs/20 §4. Do many tests — unit+integration+e2e per the ROADMAP list (await-lock in async; mutual exclusion PROVEN across an `await`; try_lock busy/free; non-reentrancy; FIFO fairness; escape rejection + clone hatch; return clone-out; Thread.spawn-cannot-lock error; panic/cancel release; cross-task contention; GC-stress). Migrate `tests/cases/concurrency/*` and `examples/shared.otter` to async + `await state.lock(...)`. Keep docs, examples, LSP and ROADMAP consistent.

[DOME] /goal Improve GC throughput with per-thread TLABs (thread-local allocation buffers) and/or the MMTk Immix move (docs/16), behind the existing stable `Gc` abstraction. This is a BEHAVIOR-NEUTRAL change — the precise tracing collector is already correct single- and multi-threaded (gc_alloc slab allocator + WORLD-barrier STW), so the contract from docs/16 must be honored exactly and every existing test must stay green with NO behavior change. No shortcuts; measure before/after with `bench` and `run --time`. Do many tests — keep all unit + integration + e2e + GC-stress + concurrency cases green, and add allocation-throughput benchmarks + concurrent-allocation stress proving no regressions in correctness. Keep docs and ROADMAP consistent and up to date.

[DONE] /goal Implement `await`/suspension in genuinely-conditional positions: the right operand of `&&`/`||` and the condition of a `while` loop (today these correctly error "await in this position is not yet supported" rather than miscompiling). Follow docs/21 (async) exactly and DO NOT change the language design without asking first. The hard part is preserving evaluation order and conditional suspension frequency — a suspending op in `a && b` must only run when `a` is true, and a `while` condition suspends once per iteration. Build the lowering at full production quality (likely extending sema/anf.rs and the async state machine), no shortcuts. Remember async is implicit (the `spawn` keyword), so this is about the suspend points the fixpoint pass discovers, not surface syntax. Do many tests — unit + integration + e2e covering passing cases, ordering/short-circuit correctness, suspension-frequency, and the cases that should still error. Keep docs, examples, LSP and ROADMAP consistent and up to date.

[DONE] /goal Allow async closures in `Thread.spawn` (docs/20 §1, §4; docs/21 §6) — APPROVED design change: `Thread.spawn` accepts either a synchronous `() => R` or an async `() => Future<R>` closure, and always returns `JoinHandle<R>` (joining on the AWAITED `R`). Implemented: new `lang_thread_spawn_async(env, pending_tid)` runtime entry (closure-call + block_on-drive); checker detects the async closure (return type `Future<T'>`) and yields `JoinHandle<T'>`; backend `emit_thread_spawn` calls the new entry (pending_tid, no float_kind) when async; the Shared-lock rejection is narrowed to SYNCHRONOUS workers (async workers may await + lock); trailing `Thread.spawn { async => … }` parses; `JoinHandle.detach()` implemented (`lang_thread_detach`); a captured channel endpoint is owned by the future (released on completion, not when the building closure returns) so a worker can await-then-send/recv across a suspension. Tests: runtime drive (immediately-ready + suspend/resume), compiler (async→JoinHandle<T'>, async worker CAN lock, sync worker still errors, trailing form), parser (trailing async forms), LSP (hover), e2e concurrency (lock, parallel, detach, cross-thread channel, GC-stress) + native parity. Known limitation: one OS thread per async worker; worker-panic isolation remains separate.

[DONE] /goal Implement deterministic `Drop` semantics sufficient for channel-close on last-`Sender` drop, so `Receiver.recv()` surfaces `ChannelClosed` and a `Receiver: Iterator` terminates (docs/20 §2, docs/16 §8, docs/10). This is the known tension between deterministic resource release and the current GC-timed best-effort `Drop` — resolve it correctly (e.g. reference-counted endpoint handles or scope-bound drop for these handle types) per the design; ask first if it requires any language-design decision. No shortcuts. Wire channel close through the runtime channel registry and the prelude (`ChannelClosed`, `Receiver: Iterator`). Do many tests — unit + integration + e2e including cross-thread close, drain-then-close, iterator termination, and GC-stress. Keep docs, examples, LSP and ROADMAP consistent and up to date. — DONE: resolved via reference-counted runtime channel endpoints (the deterministic-release carve-out of docs/16 §8, generalized by `@RefCounted`). `crates/runtime/src/channels.rs` tracks live sender/receiver counts; the compiler emits `lang_chan_sender_acquire`/`_release` (+ receiver equivalents) at deterministic scope boundaries — `Sender.clone()` acquires, a worker that captured an endpoint releases it on every `emit_return` path (`FnGen.endpoint_releases`), and an async worker transfers endpoint ownership to the future's completion path. Last-sender release closes-for-receiving (drained `recv()` → `ChannelClosed`, blocking `Receiver: Iterator` `for x in rx` terminates via `lang_chan_recv_blocking`); last-receiver release closes-for-sending (`send` → `ChannelClosed`). `recv()`: `Future<T | ChannelClosed>`; `send`: `null | ChannelClosed`; `Sender`/`Receiver`/`ChannelClosed` in `std:sync`. No language-design decision needed (design was already locked in docs/20 §2 + docs/16 §8). Tests: 4 runtime unit tests (last-sender-release, drain-then-close, receiver-drop-closes-for-sending, cross-thread blocking-recv-wakes-on-close), 7 channel CLI tests, and 6 e2e cases — `channel_send_recv`, `channel_iterator_close`, `channel_multi_sender_close`, plus 3 added this pass: `channel_drain_then_close` (closed-before-consume buffered drain), `channel_close_gc_stress` (managed `str` elements across the queue under `OTTER_FUSION_GC=stress`), `channel_send_after_receiver_drop` (`send` → `ChannelClosed`). Full e2e suite 230/231 (1 expected XFAIL). Fixed a stale ROADMAP `[ ]` line that contradicted the DONE entry; `examples/channels.otter` + docs/20 already accurate. NOTE: multi-OS-thread workers + a blocking channel iterator under collect-on-every-alloc GC-stress can livelock — a deep GC-STW/threading interaction orthogonal to channel-close correctness (belongs to the GC/worker-panic goals); the GC-stress channel test deliberately uses a single worker, matching the existing stable pattern.

[DONE] /goal Complete the deferred generic/cross-module cases that currently emit clear "scoped out" errors (NOT miscompiles): generic `Drop` types (docs/16 §8, docs/10), generic-interface default methods, and cross-module interface default methods (docs/10). Implement each at full production quality following the docs — generic Drop needs per-instance drop-fn registration through the monomorphization worklist; generic-interface defaults need type-arg substitution in the copied default body; cross-module defaults need the default-expansion pass to see imported interface AST. No shortcuts; ask first before any design change. Do many tests — unit + integration + e2e covering each case plus the error cases that should remain errors. Keep docs, examples, LSP and ROADMAP consistent and up to date. — DONE: (1) GENERIC DROP — the object-header drop slot was keyed by `1000 + def.index()`, which collapses every monomorphization of a generic `Drop` type onto one id even though each compiles its own `drop` glue. Fixed in the backend: `FnGen::drop_type_id` (crates/backend/src/gen_struct.rs) now, for a generic `Drop` impl (`extend<T> S<T>: Drop`), resolves+instantiates the concrete `drop` method through the existing worklist (`declare_instance`) and keys the descriptor on its `FuncId` offset past `GENERIC_DROP_TID_BASE` (crates/backend/src/support.rs); `Codegen::collect_drops` (crates/backend/src/lib.rs) scans the instance table and registers every compiled generic-`drop` instance under the matching per-instance id (sorted for determinism). Non-generic `Drop` keeps `1000 + def`. The descriptor `type_id` is finalizer-only (union/dyn `is`/`as` use separately-written words), so this never affects matching. Works for GC-managed (best-effort) and `@RefCounted` (deterministic, ARC + cascade) generic types, JIT + native, under GC-stress. (2) GENERIC-INTERFACE DEFAULTS — `sema/defaults.rs::expand_default_methods` now indexes generic interfaces and substitutes the interface's type parameters with the `extend`'s interface arguments (`Bounded<T>` impl'd as `Bounded<i32>` ⇒ `T`→`i32`) throughout the copied signature AND body (a full type-substitution AST walk; method-local generics shadow interface params), before re-spanning. (3) CROSS-MODULE DEFAULTS — `analyze_multi_ctx` builds a program-wide `ForeignIfaces` index of every `pub` interface (root + externals, recursing inline submodules); each module is expanded against the foreign interfaces declared in the OTHER modules, with local declarations shadowing foreigns and structurally-conflicting cross-module names poisoned to ambiguous (left for resolution to diagnose — never a silently-wrong body). Tests: 8 unit (`sema::defaults::tests`), 2 LSP (`generic_interface_default_has_no_diagnostics`, `cross_module_interface_default_resolves`), 3 CLI cross-module (`cross_module_interface_default`, `_generic_interface_default`, `_default_is_overridable`) + 1 GC-managed generic-drop count test (`generic_drop_gc_managed_runs_under_stress`), 7 e2e cases (interfaces/generic_default_method, interfaces/generic_default_uses_param_locals, refcounted/generic_drop_deterministic, refcounted/generic_drop_distinct_glue, refcounted/generic_drop_gc_stress, errors/generic_default_wrong_override_type, errors/interface_default_missing_required_method) + the examples/generic_traits_and_drop showcase. Docs/10 (new "Default methods" subsection) + docs/16 §8 (generic-Drop callout) updated; full workspace suite green.

[DONE] /goal Implement async closures and async arrow closures (docs/21 §7) — the only async surface still uncompiled. Today async FUNCTIONS and bare `async { … }` blocks lower to state-machine `poll` functions, but `var f = function(x: i64): Future<str> async { … }` and `(x) async => { … }` hit "not yet implemented" errors in `h_closure_kind` (crates/backend/src/gen_hir.rs) and the async-block path rejects parameterized/captured closures (`h_async_block`). Implement the full path at production quality: a closure with the `async` body modifier must lift its body into a `poll` state machine (reusing `build_stateful_poll` / `async_state_layout` / `emit_await_suspend`) whose state struct ALSO holds the closure's captured environment, and produce a `Future<T>`-returning callable. Captures follow ordinary closure rules (docs/09 §7 — captured variables are cell-backed by reference); when the resulting future is `spawn`-ed, the captured set is subject to cross-thread isolation deep-cloning (docs/20 §1). Async closures CANNOT be `extern` — keep/add that rejection. No shortcuts; ask first before any design change. Do many tests — unit (state layout + capture save/restore across suspend) + compiler (async closure typed as `Future<T>`, arrow form, capture-by-reference mutation visibility, extern rejection) + integration + e2e (async closure bound and awaited, stored in a struct/list and driven, capturing locals across `await sleep`, spawned async closure with deep-cloned captures, `for await` inside one; JIT + native parity, GC-stress). Keep docs/21, examples, LSP signature/hover and ROADMAP consistent and up to date.

[DONE] /goal Finish the FFI tail in docs/19: the `@CallConv` function decorator (§13) and a managed `CString`/`Buffer` handle type with `Drop` (§6) — today `CString.from_str` returns a raw `*u8` the caller must free; implement the proper owning handle types that free on drop. Production quality, no shortcuts, follow docs/19 exactly; ask first before any design change. NOTE `@Variadic` is separately blocked by Cranelift and is NOT part of this goal. Do many tests — unit + integration + e2e against real libc (JIT + native parity + GC-stress), covering each calling convention, handle round-trips, and drop-frees-the-buffer. Keep docs, examples (examples/ffi.otter) and ROADMAP consistent and up to date.

[DONE] /goal Implement worker-panic isolation for threads and `spawn` workers (docs/20, docs/14): a panicking worker must fail only itself and surface as `Panicked{message}` to the joiner, NOT abort the whole process. — DONE: host unwinding can't cross Cranelift frames (no unwind tables), so each worker runs its body under a `setjmp`/`longjmp` PANIC BOUNDARY at its entry (`crates/runtime/src/panic_boundary.c`, built by `build.rs` via the `cc` crate; Rust glue in `panic_boundary.rs`). `lang_panic` checks `otter_pb_active()`: on a worker it captures the message and `longjmp`s back to the boundary (restoring the saved context directly across the generated frames — no frame-walk); on the MAIN thread no boundary is installed, so a top-level panic stays fatal (exit 101). The boundary (`run_under_boundary`) restores the invariants the longjmp skipped: drains held `Shared` locks (`lang_shared_release_all`, no poisoning) + the thread's transient cross-`poll` GC pins (new `gc::pin_for_unwind`/`unpin_for_unwind`/`release_unwind_pins`, used by `block_on`), then materialises the message as a pinned `str`. `finish_worker` publishes `Panicked{message}` so `JoinHandle.join()` surfaces it recoverably, while a `spawn EXPR` awaiter has the panic RE-PROPAGATED at its `await` (`spawn_poll`) per docs/21 §11. Covers `Thread.spawn` (sync + async closures) and the `spawn` keyword. JIT + native parity (C shim bundled into both the rlib and libruntime.a). No language-design decision needed (behavior was already specified in docs/14 §7, docs/16 panic row, docs/20 §4, docs/21 §11). Tests: 5 runtime unit/integration (panic_boundary + real worker-panic with sibling survival) + 4 CLI integration (isolated join + message, lock-release-no-poison, `spawn` propagation exits non-zero, GC-stress parity) + e2e `concurrency/{worker_panic_sibling_survives,worker_panic_gc_stress,spawn_panic_propagates,lock_released_on_panic}` (last graduated from XFAIL); full workspace + 260-case e2e suite green, 0 XFAIL. The executor-multiplexed task-panic case is the SEPARATE follow-on goal below (depends on the M:N executor).

[DONE] /goal Resolve `@Variadic` extern functions (docs/19 §13), which was blocked because Cranelift lacks portable varargs-ABI support (aarch64 Apple passes varargs on the stack; Cranelift uses registers). — DONE via `libffi` (option A, user-approved). Confirmed Cranelift 0.132 has zero variadic support (`ir::Signature` = `{params, returns, call_conv}`, no fixed/variadic boundary, no `%al` mechanism), so a variadic call genuinely cannot be lowered as an ordinary call on the targets we emit (aarch64-apple-darwin stack passing, x86-64 SysV `%al`, Windows x64 float duplication). Implementation: the backend (`gen_hir::h_variadic_call`) marshals each argument — C-default-promoted in variadic position (`f32`→`f64`, sub-int/`bool`/`char`→`int`, `@Transparent` seen through to its inner scalar via `transparent_inner`), sign/zero-extended into a flat 8-byte-slot value buffer with a parallel type-tag array — gets the callee address via `func_addr`, and calls the runtime shim `lang_variadic_call` (`crates/runtime/src/variadic.rs`), a minimal hand binding to the *system* `libffi` (neither `pkg-config` nor autotools is available here, but `-lffi` links cleanly; `ffi_cif` is treated as an opaque over-sized buffer, the only target constant `FFI_DEFAULT_ABI` verified against ffitarget.h) that drives `ffi_prep_cif_var`/`ffi_call`. JIT links libffi via the runtime build script (`cargo:rustc-link-lib=ffi`); native `otter_fusion build` links `-lffi` when the program declares any `@Variadic`. Checker (`sema/check`): `validate_variadic` rejects `@Variadic` off an extern import / on an export-with-body / with no fixed prefix / with decorator args; `check_variadic_call` relaxes call arity to `>= n_fixed` and rejects non-passable variadic args (aggregates, `str` — pass a `*u8`). Used as a value (not called by name) the import is an ordinary non-variadic fn pointer over its fixed prefix (safe, no soundness hole). The LSP inherits the fix (shared `analyze_multi_ctx` checker). Verified against real `printf`/`snprintf`: int/str/double/char/hex, i64/u64 full width, f32→double promotion, negative sign-extension, `@Transparent` newtype, no-vararg path — JIT + native parity + GC-stress all identical. Tests: 2 runtime unit tests (drive `lang_variadic_call` against `snprintf` directly), 9 checker unit tests, 4 e2e `run` cases (`tests/cases/ffi/variadic_*`), 7 e2e compile-error cases (`tests/cases/errors/variadic_*`), 1 native-parity test (`run.rs`). Full workspace + 272-case e2e suite green. Docs (docs/19 §7 `@Variadic` subsection) and ROADMAP updated.

[DONE] /goal Build the first real standard-library slice and catalog architecture: implement `std:error` with the documented `Error` interface, `Annotated` wrapper, and `with_context<T, E: Error>(T | E, str): T | Annotated`, exposed through the explicit `std:error` import rather than the near-empty prelude. Add `compiler::sema::stdlib`, a structured toolchain-module catalog recording each `core:*`/`std:*` path, tier, exported symbols, and implementation kind (`Otter`, `RustBacked`, `Mixed`), so the standard library can be partly Rust, partly Otter Fusion, organized for extension, and ready for future custom/target-specific `std:*` providers/sysroots. Supporting compiler fixes: interface super-method lookup now reaches super-interfaces for generic bounds and dynamic interface objects; `extend Type: SubInterface` records transitive super-interface impls for codegen vtables; generic inference can infer type parameters from union templates such as `T | E` using bounds (`E: Error`) to choose the residual variant; HIR coercion baking preserves `Unbox` before `Widen`/`WidenDyn`; backend `Adjust::Unbox` resolves monomorphized type parameters before loading. Tests: stdlib catalog invariants; compiler unit `generic_union_narrow_then_widen_preserves_unbox`; e2e `stdlib/std_error_with_context`, `stdlib/std_error_dynamic_object`, `stdlib/std_error_requires_import`, and `interfaces/super_interface_bound_method`. Verification: `cargo test -p compiler` green (259 lib + integration suites), `cargo test -p cli --test suite -- --nocapture` green (276/276). Docs updated in docs/13, docs/18, docs/24 and ROADMAP.

[DONE] /goal Add a dedicated stdlib extension contributor guide so future `core:*`/`std:*` work has one documented path instead of scattered tribal knowledge. — DONE: added `docs/29-extending-stdlib.html` covering stdlib principles, `core:*` vs `std:*`, `Otter`/`RustBacked`/`Mixed` implementation kinds, `StdModuleSpec` catalog exports, adding new core modules, adding new std modules, Rust-backed runtime hooks, custom providers/sysroots, import/prelude/export rules, required tests, and review checklist. Linked it from docs/README, docs/18, docs/24, and ROADMAP.

[DONE] /goal Move toolchain standard-library definitions out of the monolithic embedded `PRELUDE_SRC` and into maintainable std/core source modules while preserving the near-empty prelude rule. — DONE: added `crates/compiler/src/sema/stdlib_src/core/*.otter` and `crates/compiler/src/sema/stdlib_src/std/*.otter` for the Otter-authored toolchain surface (`core:prelude`, `core:collections`, `core:ffi`, `core:compiler`, `std:error`, `std:io`, `std:thread`, `std:sync`, `std:async`). The current architecture collects each `TOOLCHAIN_SOURCES` entry into a hidden module-local owner under the private `__builtins__` root, then the stdlib catalog builds curated public import views. This keeps user-visible imports and DefId bootstrap behavior stable while turning provider/sysroot evolution into a source-layout/catalog step instead of embedded-string surgery. Docs/18, docs/29, ROADMAP updated.

[DONE] /goal Implement the next stdlib slice for managed bytes and shared IO contracts. — DONE: added `std:bytes` as an explicit Otter-authored std module exporting `Bytes` and `Utf8Error`; expanded `std:io` from print intrinsics to a mixed module exporting `Reader`, `Writer`, `Seeker`, `SeekFrom` (`Start`/`Current`/`End`), `IoError`, `IoErrorKind`, the IO error-kind variants, `print`, and `println`. `std:io` contracts are ordinary library APIs in `std:*`, while `print`/`println` remain runtime-backed intrinsics. `SeekFrom` and `IoErrorKind` provide direct equality helpers plus clone/hash/string/debug methods; `IoError` implements `Error`, equality, clone, hash, and debug rendering. Added stdlib e2e cases for `Bytes`, near-empty-prelude import enforcement, user implementations of `Reader`/`Writer`/`Seeker`, and IO value semantics; updated catalog tests, HIR lowering test scoping, docs/13, docs/17, docs/18, docs/24, docs/29, and ROADMAP. Verification: `cargo test -p compiler` and `cargo test -p cli --test suite -- --nocapture` green (281/281 e2e; expanded later to cover IO value semantics).

[DONE] /goal Fill the missing pure value semantics for `std:bytes.Bytes`. — DONE: extended `crates/compiler/src/sema/stdlib_src/std/bytes.otter` so `Bytes` implements deep `Clone`, bytewise `Eq`, deterministic `Hash`, and compact diagnostic `ToStr` rendering (`bytes[...]`); `Utf8Error` now implements `ToStr`. UTF-8 encode/decode and pinned `Buffer` views remain planned runtime/library work. Expanded `std_bytes_basic.otter` to cover clone independence, equality, hashing, and stringification; updated docs/17, docs/18, docs/24, ROADMAP, and goals. Verification: `cargo test -p cli --test suite -- --nocapture` green (325/325 e2e).

[DONE] /goal Fill the pure `std:bytes.Bytes.from_str` UTF-8 encoding helper. — DONE: `Bytes.from_str(s: str)` now constructs managed bytes from the existing `str.bytes()` iterator, covering ASCII and non-ASCII UTF-8 without target/runtime hooks. Expanded `std_bytes_basic.otter`; updated docs/18, docs/24, ROADMAP, and goals so UTF-8 encoding is implemented while UTF-8 decoding, pinned `Buffer` views, and reader cursor adapters remain planned. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture` and `cargo test -p cli --test suite -- --nocapture` green (328/328 e2e).

[DONE] /goal Fill the pure `std:bytes.Bytes.decode_utf8` helper. — DONE: added `Bytes.decode_utf8(): str | Utf8Error` as a strict pure Otter Fusion UTF-8 decoder. It accepts ASCII plus 2-, 3-, and 4-byte sequences, reconstructs Unicode scalar values through checked `char` conversion, and rejects invalid leading bytes, malformed continuations, truncation, overlong encodings, surrogate code points, and scalar values above U+10FFFF. Expanded `std_bytes_basic.otter`; updated docs/18, docs/24, ROADMAP, and goals. Pinned `Buffer` views remain planned provider/runtime work. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture` and `cargo test -p cli --test suite -- --nocapture` green (331/331 e2e), plus `rustfmt --check crates/compiler/src/sema/stdlib.rs` and `git diff --check`.

[DONE] /goal Fill the in-memory `std:bytes.Bytes` sink side of the IO contracts. — DONE: `Bytes` now implements `std:io.Writer`; `write` snapshots and appends the input bytes, `write_all` appends or propagates `IoError`, and `flush` is a no-op for the memory buffer. Expanded `std_bytes_basic.otter` with direct writer calls, interface-dispatched writes, and self-write snapshot behavior; updated docs/18, docs/24, ROADMAP, and goals. Verification: `cargo test -p compiler` and `cargo test -p cli --test suite -- --nocapture` green (326/326 e2e).

[DONE] /goal Fill the in-memory `std:bytes.BytesCursor` reader/seeker side of the IO contracts. — DONE: added pure Otter Fusion `BytesCursor` as a snapshot cursor over `Bytes`, exported from `std:bytes`, with `new`, `position`, `len`, `is_empty`, `remaining`, `rewind`, `read_byte`, `read_bytes`, `Reader.read`, `Reader.read_to_end`, `Seeker.seek`, clone, stringification, and debug rendering. This completes the in-memory `Reader`/`Seeker` complement to `Bytes`' `Writer` implementation without adding target-backed handles. Expanded `std_bytes_basic.otter`; updated docs/17, docs/18, docs/24, ROADMAP, and goals. UTF-8 decoding, pinned `Buffer` views, buffered adapters, standard streams, and target-backed handles remain future provider/runtime work.

[DONE] /goal Fill `std:bytes.Utf8Error` foundational error/value semantics. — DONE: `Utf8Error` now implements `std:error.Error`, equality, clone, hash, diagnostic stringification, and `std:fmt.Debug`, giving the future UTF-8 decoder a stable ordinary stdlib error shape. Expanded `std_bytes_basic.otter`; updated docs/18, docs/24, ROADMAP, and goals. Verification: `cargo test -p compiler` and `cargo test -p cli --test suite -- --nocapture` green (326/326 e2e).

[DONE] /goal Implement `std:time::Duration` as the next portable stdlib value type. — DONE: added `crates/compiler/src/sema/stdlib_src/std/time.otter` and cataloged `std:time` as an Otter-authored module exporting `Duration`. The type stores signed nanoseconds and provides `from_days`, `from_hours`, `from_minutes`, `from_secs`, `from_millis`, `from_micros`, `from_nanos`, `as_days`, `as_hours`, `as_minutes`, `as_secs`, `as_millis`, `as_micros`, `as_nanos`, `is_zero`, `is_positive`, `is_negative`, `abs`, `add`, `sub`, plus `Eq`, `Ord`, `Hash`, `Clone`, and `ToStr` implementations so `+`, `-`, comparisons, `hash`, `clone`, and stringification work. Added e2e cases for runtime behavior and near-empty-prelude import enforcement; fixed HIR tests to select user bodies/defs robustly now that stdlib source has ordinary methods named `add`. Docs/18, docs/24, ROADMAP updated. Verification: `cargo test -p compiler` and `cargo test -p cli --test suite -- --nocapture` green (283/283 e2e; expanded later to cover day/hour/minute constructors and sign/absolute helpers).

[DONE] /goal Implement the `std:time` monotonic/system clock slice as mixed stdlib API. — DONE: `std:time` now exports `Instant` and `SystemTime` alongside `Duration`; the public methods and value semantics are authored in Otter Fusion while `now()` lowers through Rust-backed runtime hooks (`lang_time_monotonic_nanos`, `lang_time_system_nanos`). `Instant` covers monotonic `now`, fixed nanosecond construction, elapsed/duration arithmetic, equality/ordering/hash/clone/stringification/debug; `SystemTime` covers wall-clock `now`, Unix epoch helpers, duration arithmetic, equality/ordering/hash/clone/stringification/debug. Added runtime clock unit tests, HIR/codegen intrinsic wiring, e2e coverage for deterministic value behavior, target-backed clock sanity, import enforcement, and old `core:time` rejection; updated docs/17, docs/18, docs/24, ROADMAP, and goals. Verification: `cargo test -p runtime time::tests -- --nocapture`, `cargo test -p compiler -- --nocapture`, `cargo test -p backend -- --nocapture`, and `cargo test -p cli --test suite -- --nocapture` green (328/328 e2e).

[DONE] /goal Implement the pure `std:fs.Path` value surface without pretending target-backed file IO exists yet. — DONE: added `crates/compiler/src/sema/stdlib_src/std/fs.otter` and cataloged `std:fs` as an Otter-authored module exporting `Path`. The implemented slice provides `new`, `as_str`, `join`, `join_path`, `parent`, `file_name`, `file_stem`, `extension`, `with_extension`, `normalize`, `is_absolute`, `is_relative`, internal slash trimming, plus `Eq`, `Hash`, `Clone`, and `ToStr`; it intentionally leaves `File`, metadata, native separator normalization, filesystem canonicalization, and directory ops for future provider/runtime work. Added e2e cases for path behavior and near-empty-prelude import enforcement; updated docs/17, docs/18, docs/24, ROADMAP. Verification: `cargo test -p compiler` and `cargo test -p cli --test suite -- --nocapture` green (285/285 e2e; expanded later to cover `join_path`, `file_stem`, `with_extension`, `is_relative`, and lexical `normalize`).

[DONE] /goal Fill lexical normalization for `std:fs.Path`. — DONE: added pure Otter Fusion `Path.normalize()` that collapses repeated slashes and `.`, resolves `..` against previous normal segments, clamps absolute paths at `/`, preserves leading relative `..`, and maps the empty normalized relative path to `"."` without touching the filesystem. Expanded `stdlib/std_fs_path_basic.otter`; updated docs/17, docs/18, docs/24, ROADMAP, and goals. Native separator normalization, symlink-aware/filesystem canonicalization, metadata, directories, and file handles remain provider-backed work.

[DONE] /goal Fill normalized component and prefix helpers for `std:fs.Path`. — DONE: added pure Otter Fusion `Path.components()`, `starts_with(base)`, `ends_with(suffix)`, and `strip_prefix(base)` over the existing lexical normalization rules. These helpers compare slash-separated normalized components without touching the filesystem, return `"."` for an empty stripped remainder, and keep native separators, symlink-aware canonicalization, metadata, directories, and file handles in future provider-backed work. Expanded `stdlib/std_fs_path_basic.otter`; updated docs/17, docs/18, docs/24, ROADMAP, and goals.

[DONE] /goal Implement `std:fmt` as the next pure Otter-authored stdlib contract slice. — DONE: added `crates/compiler/src/sema/stdlib_src/std/fmt.otter` and cataloged `std:fmt` as an Otter-authored module exporting `Display: ToStr`, `Debug`, `FmtSink`, and `FmtError`. This keeps interpolation and `value as str` on the language-level `ToStr` path while giving libraries explicit user-facing/developer-facing rendering contracts and incremental formatting sinks. Pure stdlib values (`Bytes`, `Utf8Error`, `Duration`, `Path`, `Json`, `std:net:types` identifier values, and struct-shaped `std:http` values) now implement `Debug`; primitive and generic-collection `Debug` impls remain planned. Added e2e cases for interface dispatch, `Display` satisfying `ToStr`, `FmtSink` writes, standard stdlib `Debug` impls, and near-empty-prelude import enforcement; updated docs/15, docs/17, docs/18, docs/24, and ROADMAP. Verification: `cargo test -p compiler` and `cargo test -p cli --test suite -- --nocapture` green (287/287 e2e; expanded later to 326/326 with standard-value `Debug` coverage).

[DONE] /goal Fill `std:fmt.Debug` rendering for generic collections. — DONE: added pure Otter Fusion `Debug` impls for `List<T: Debug>`, `Set<T: Eq + Debug>`, and `Map<K: Eq + Hash + ToStr, V: Debug>`, producing deterministic diagnostic collection strings over the existing iteration order. Expanded `stdlib/std_fmt_standard_debug_impls.otter` to cover interface dispatch through list/map/set collection values. Fixed `type_implements` so interface names in visible `extend Target: Interface` clauses resolve in the module where the `extend` is written, matching `collect_impls`; this lets public stdlib impls such as `Bytes: Eq` satisfy bounds even when the user imports the type but not the interface name. Added a compiler unit regression. Updated docs/18, docs/24, ROADMAP, and goals. Primitive `Debug` remains planned because primitive-to-`std:fmt.Debug` interface objects need backend intrinsic vtable support. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo test -p compiler sema::check::tests -- --nocapture`, and `cargo test -p cli --test suite -- --nocapture` green (331/331 e2e).

[DONE] /goal Fill `std:error`/`std:fmt` foundational value semantics. — DONE: `std:error.Annotated` now implements `std:fmt.Debug` while intentionally not claiming clone/equality/hash over an unconstrained `Error` source; `std:fmt.FmtError` now implements `std:error.Error`, equality, clone, hash, and diagnostic debug rendering. Expanded `stdlib/std_error_with_context` and `stdlib/std_fmt_contracts`; updated docs/13, docs/17, docs/18, docs/24, ROADMAP, and goals. Verification: `cargo test -p compiler`, `cargo test -p cli --test suite -- --nocapture` green (326/326 e2e), `git diff --check`, and `rustfmt --check crates/compiler/src/sema/stdlib.rs crates/compiler/src/sema/symbols.rs`.

[DONE] /goal Fill std runtime marker value semantics without turning runtime handles into plain values. — DONE: `std:async.TimedOut`, `std:thread.Panicked`, `std:sync.ChannelClosed`, and `std:sync.LockBusy` now implement `Eq`, `Clone`, `Hash`, `ToStr`, and `std:fmt.Debug`; runtime handles such as `JoinHandle`, channel endpoints, and `Shared` remain handle types. Expanded stdlib e2e coverage in `std_async_runtime_basic`, `std_thread_basic`, and `std_sync_basic`; updated docs/18, docs/20, docs/21, docs/24, ROADMAP, and goals. Verification: `cargo test -p cli --test suite -- --nocapture` green (326/326).

[DONE] /goal Move async syntax protocols to `core:async` under the new core/std criteria. — DONE: added `crates/compiler/src/sema/stdlib_src/core/async.otter` and cataloged `core:async` as a mixed core module exporting `Future`, `Ready`, `Pending`, `Context`, and `AsyncIterator`; the protocol definitions now live in that source file while the `core:prelude` catalog view remains a compatibility/ergonomics re-export for the compiler-recognized shapes. Removed `AsyncIterator` from `std:async`, leaving runtime helpers (`TimedOut`, `yield_now`, `sleep`, `timeout`) there. Updated compiler/backend/CLI test preludes and async examples to import `AsyncIterator` from `core:async`; added e2e coverage for `core:async` interface-object `for await` dispatch, importing/constructing `Future`/`Ready`/`Pending`/`Context` from `core:async`, and rejecting the old `std:async.AsyncIterator` path. Docs/17, docs/18, docs/21, docs/24, docs/26, and ROADMAP now describe the split. Verification: `cargo test -p compiler` and `cargo test -p cli --test suite -- --nocapture` green (290/290 e2e).

[DONE] /goal Fix `await` and `spawn` for concrete `core:async.Future` implementations. — DONE: `check_await` and `check_spawn` now bake an explicit `Adjust::WidenDyn(Future<Out>)` when the operand is a concrete type implementing `Future<Out>`, matching the backend/executor representation used for polling and scheduling. Added the compiler unit test `await_concrete_future_bakes_dynamic_future_widening` plus e2e cases `core_async_await_concrete_future.otter` and `core_async_spawn_concrete_future.otter`. Docs/21, docs/24, and ROADMAP now document that concrete futures are widened to the `Future<T>` interface object for `await` and `spawn`. Verification: `cargo test -p compiler` and `cargo test -p cli --test suite -- --nocapture` green (292/292 e2e).

[DONE] /goal Fill the exported `core:collections.Set` surface instead of leaving a cataloged stub. — DONE: replaced the empty `Set<T>` toolchain definition with an Otter-authored unique-list implementation gated by `T: Eq`, implementing `Set<T>()` / `Set.new<T>()`, `size`, `is_empty`, `contains`, `insert`, `remove`, `clear`, `iter`, `union`, `intersect`, and `difference`. Added stdlib e2e coverage for set behavior and for the near-empty-prelude import rule. Fixed HIR interface-object vtable monomorphization so generic iterator adapters such as `ListIter<T>` are compiled with concrete type arguments when widened to `Iterator<T>`; this is covered by a backend regression test. Docs/17 and docs/24 now describe the implemented method surface and mark hash-backed storage, set literal syntax, and keyed-hasher conveniences as planned rather than implemented. Verification: `cargo test -p compiler`, `cargo test -p backend -- --nocapture`, `cargo test -p cli --test suite -- --nocapture` green (294/294 e2e), plus `rustfmt --check` and `git diff --check`.

[DONE] /goal Fill value semantics for `core:collections.Set`. — DONE: added pure Otter Fusion implementations of `Eq`, `Clone`, and deterministic order-insensitive `Hash` for `Set<T>` with the appropriate bounds (`T: Eq`, `T: Eq + Clone`, and `T: Eq + Hash`). Equality is membership-based, cloning snapshots every member into a new set, and hashing combines member hashes modulo a fixed prime so insertion order does not affect the result. Expanded `stdlib/core_collections_set_basic.otter`; updated docs/18, docs/24, and ROADMAP. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture` and `cargo test -p cli --test suite -- --nocapture` green (331/331 e2e).

[DONE] /goal Implement `std:json` as a portable stdlib value-tree slice. — DONE: added `crates/compiler/src/sema/stdlib_src/std/json.otter` and cataloged `std:json` as an Otter-authored module exporting an opaque `Json` value plus `json_null`, `json_bool`, `json_number`, `json_string`, `json_array`, `json_object`, and typed object/array extractors. The public type is intentionally a concrete wrapper rather than a public recursive union so `Json` remains stable inside `List<Json>` and `Map<str, Json>` across module boundaries. `Json` implements deep `Clone`, deep `Eq`, deterministic `Hash`, compact escaped `ToStr` rendering, pretty rendering, and diagnostic `Debug`; parsers and stricter canonicalization remain follow-up work. Added e2e coverage for construction, object lookup, array indexing, typed extraction, clone independence, equality, hashing, rendering, debug rendering, near-empty-prelude import enforcement, and rejecting `core:json`; updated docs/17, docs/18, docs/24, and ROADMAP. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture` and `cargo test -p cli --test suite -- --nocapture` green (313/313 e2e; expanded later to cover Debug rendering, Json hashing, escaping, and pretty rendering).

[DONE] /goal Fill JSON string/key escaping for `std:json` compact rendering. — DONE: `Json.render()` / `ToStr` now route string values and object keys through a pure Otter Fusion escaper covering quotes, backslashes, newline, carriage return, tab, backspace, form feed, and remaining U+0000..U+001F control characters as `\u00XX`, while preserving non-ASCII Unicode scalar values. Expanded `stdlib/std_json_basic.otter`; updated docs/18, docs/24, and ROADMAP so parsing and stricter canonicalization remain planned/package-tier work rather than incorrectly listing all escaping as future work.

[DONE] /goal Fill JSON pretty rendering for `std:json`. — DONE: added pure Otter Fusion `Json.pretty(indent: i64)` using the same string/key escaping rules as compact `render()`, multi-line formatting for non-empty arrays/objects, compact `[]`/`{}` for empty containers, and compact fallback when `indent <= 0`. Expanded `stdlib/std_json_basic.otter`; updated docs/18, docs/24, ROADMAP, and goals so only parsing and stricter canonicalization remain planned.

[DONE] /goal Implement `std:net:types` as the portable network identifier value slice. — DONE: added `crates/compiler/src/sema/stdlib_src/std/net_types.otter` and cataloged `std:net:types` as an Otter-authored module exporting `IpAddr`, `SocketAddr`, `Uri`, `Url`, `ParseError`, and constructor functions (`ip_v4`, `ip_v6`, `socket_addr`, `uri`, `url`). The implemented slice covers value construction, IPv4/IPv6 predicates, socket accessors, rendering, equality, hashing, cloning, stringification via `ToStr`, and debug formatting via `std:fmt.Debug`; it intentionally leaves sockets, DNS, IPv6 text parsing, URL canonicalization, IDNA, and `Display` formatting contracts for later `std:net`/parser work. Later slices added UTF-8 percent component encode/decode helpers. Added e2e coverage for runtime behavior, debug rendering, near-empty-prelude import enforcement, and rejecting `core:net:types`; updated docs/17, docs/18, docs/24, docs/29, ROADMAP, and goals. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture` and `cargo test -p cli --test suite -- --nocapture` green (316/316 e2e; expanded later to cover Debug dispatch at 326/326).

[DONE] /goal Fill `std:net:types.ParseError` foundational error/value semantics. — DONE: `ParseError` now implements `std:error.Error`, equality, clone, hash, stringification, and `std:fmt.Debug`, giving future URL/URI/IP parser APIs a stable ordinary stdlib error shape. Expanded `std_net_types_basic.otter`; updated docs/17, docs/18, docs/24, ROADMAP, and goals. Verification: `cargo test -p compiler` and `cargo test -p cli --test suite -- --nocapture` green (326/326 e2e).

[DONE] /goal Fill the first parser helpers for `std:net:types`. — DONE: added pure Otter Fusion `parse_ip_v4(text): IpAddr | ParseError` for dotted decimal IPv4, `parse_ipv4_socket_addr(text): SocketAddr | ParseError` for `IPv4:port`, `parse_uri(text): Uri | ParseError` for the existing structural URI value shape (scheme, optional authority, path, query, fragment), and `parse_url(text): Url | ParseError` for the existing structural URL value shape (`scheme://`, optional user, host, optional decimal port, path, `key=value` query pairs, fragment). The helpers validate decimal digits, arity/ranges, scheme delimiters, empty authority/host, and malformed query pairs with character-positioned `ParseError`s. Catalog exports, e2e coverage, docs/17, docs/18, docs/24, ROADMAP, and goals updated. Compressed IPv6 text parsing, DNS, sockets, IDNA, and stricter canonicalization remain planned provider/package work; later slices added UTF-8 percent component encode/decode helpers.

[DONE] /goal Implement `std:hash` as the deterministic hasher bridge over the existing core `Hash` contract. — DONE: added `crates/compiler/src/sema/stdlib_src/std/hash.otter` and cataloged `std:hash` as an Otter-authored module exporting `Hasher`, `DefaultHasher`, `hash_value`, and `combine_hash`. This keeps the compiler-recognized `Hash` interface in `core:prelude` while giving user code an ordinary stdlib hasher value with deterministic writes for `u8`, `u64`, `i64`, `bool`, `str`, `Bytes`, and any `T: Hash`; the mixer deliberately avoids wrapping arithmetic because Otter Fusion integer overflow is checked. Added e2e coverage for deterministic behavior, byte-stream writes, interface dispatch, near-empty-prelude import enforcement, and rejecting `core:hash`; updated docs/17, docs/18, docs/24, docs/29, ROADMAP, and goals. Verification: `cargo test -p cli --test suite -- --nocapture` green (319/319 e2e; expanded later to cover `write_u8`/`write_bytes` at 326/326).

[DONE] /goal Fill deterministic state-machine value semantics for `std:hash.DefaultHasher` and `std:rand.SeededRng`. — DONE: both pure Otter-authored state machines now implement state equality, clone, hash, diagnostic stringification, and `std:fmt.Debug`, so callers can snapshot and compare deterministic hasher/PRNG streams without implying target-backed entropy. Expanded `std_hash_basic.otter` and `std_rand_basic.otter`; updated docs/17, docs/18, docs/24, ROADMAP, and goals. Verification: `cargo test -p compiler`, `cargo test -p cli --test suite -- --nocapture` green (326/326 e2e), `git diff --check`, and `rustfmt --check crates/compiler/src/sema/stdlib.rs crates/compiler/src/sema/symbols.rs`.

[DONE] /goal Implement `std:http` as the portable HTTP request/response value slice without clients or servers. — DONE: added `crates/compiler/src/sema/stdlib_src/std/http.otter` and cataloged `std:http` as an Otter-authored module exporting HTTP method/version union variants, `Status`, multi-value `Headers`, `HeaderEntry`, `HttpRequest`, `HttpResponse`, and constructor helpers. The implemented slice covers method/version stringification, status constructors/class predicates/default reasons, header set/append/get/get_all/remove/clone/equality/hash, flattened header entries, request/response snapshot accessors and builders with value equality/clone/hash, stable hashing/stringification where appropriate, and diagnostic debug rendering for the value layer; it intentionally leaves HTTP clients, servers, sockets, wire parsing/rendering, and canonical rendering/parsing for future package/provider work. Later slices changed header keys from exact-name to ASCII case-insensitive normalized names. Added e2e coverage for behavior, value semantics, debug rendering, near-empty-prelude import enforcement, and rejecting `core:http`; updated docs/17, docs/18, docs/24, docs/29, ROADMAP, and goals. Verification: `cargo test -p cli --test suite -- --nocapture` green (325/325 e2e; expanded later to cover Debug rendering at 326/326 and HTTP value semantics).

[DONE] /goal Fill request/response builder helpers for `std:http` value types. — DONE: added pure Otter Fusion `HttpRequest.with_method`, `with_url`, `with_headers`, `with_body`, `with_version`, `with_header`, `with_set_header`, `without_header`, and `clear_headers`, plus `HttpResponse.with_status`, `with_headers`, `with_body`, `with_version`, `with_header`, `with_set_header`, `without_header`, and `clear_headers`. These helpers return updated values, clone unchanged sub-values, append headers with `with_header`, replace headers with `with_set_header`, remove a header with `without_header`, and clear all headers with `clear_headers`, without adding HTTP clients, servers, sockets, or parsing. Later slices made those header builder names ASCII case-insensitive through normalized keys. Expanded `stdlib/std_http_basic.otter`; updated docs/18, docs/24, ROADMAP, and goals.

[DONE] /goal Implement `std:rand` as the deterministic seeded RNG slice without target entropy. — DONE: added `crates/compiler/src/sema/stdlib_src/std/rand.otter` and cataloged `std:rand` as an Otter-authored module exporting `Rng`, `SeededRng`, `gen_range_i64`, `choose`, and `shuffle`. The initial slice was deterministic and reproducible, with `next_u32`, `next_u64`, `fill_bytes` appending generated bytes into `std:bytes.Bytes`, range selection, list choice, and in-place shuffle. Later slices added `ThreadRng`, `OsRng`, OS entropy hooks, exact byte-buffer helpers, and more range helpers; richer statistical distributions and cryptographic-strength guarantees remain planned. Added e2e coverage for deterministic behavior, interface dispatch, helper functions, near-empty-prelude import enforcement, and rejecting `core:rand`; updated docs/17, docs/18, docs/24, docs/29, ROADMAP, and goals. Verification: `cargo test -p cli --test suite -- --nocapture` green (322/322 e2e).

[DONE] /goal Fill portable deterministic distribution helpers for `std:rand`. — DONE: added pure Otter Fusion `gen_range_u64(rng, low, high)` and `gen_bool(rng, numerator, denominator)` beside the existing signed range helper. `gen_range_u64` is half-open and returns `low` for empty/reversed ranges; `gen_bool` samples a deterministic ratio and clamps invalid/zero numerator cases to false and numerator >= denominator to true. Catalog exports, `std_rand_basic.otter`, docs/18, docs/24, ROADMAP, and goals updated. Later slices added OS entropy-backed RNG handles and exact byte-buffer helpers; richer statistical distributions and cryptographic-strength guarantees remain planned work.

[DONE] /goal Implement `std:log` as a portable value/default line logging slice. — DONE: added `crates/compiler/src/sema/stdlib_src/std/log.otter` and cataloged `std:log` as an Otter-authored module exporting `Level`, prefixed concrete variants (`LogTrace`, `LogDebug`, `Info`, `Warn`, `LogError`), level constructor helpers, `Record`, `LoggerAlreadySet`, `record`, `empty_fields`, `log_record`, default line helpers (`trace`, `debug`, `info`, `warn`, `error`), and structured helpers (`trace_with`, `debug_with`, `info_with`, `warn_with`, `error_with`). The slice gives levels, records, and the marker type equality/clone/hash/stringification/debug semantics and writes compact lines through `std:io.println`; replaceable global logger backends remain planned provider/runtime work. Added e2e coverage for value behavior, default/structured output, near-empty-prelude import enforcement, and rejecting `core:log`; updated docs/17, docs/18, docs/24, ROADMAP, and goals. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture` and `cargo test -p cli --test suite -- --nocapture` green (331/331 e2e).

[DONE] /goal Fill pure `std:log.Record` accessors and builder helpers. — DONE: added `Record.level`, `message`, `module`, `fields`, `time`, plus immutable-style field/module/message/time builders including field add/remove/clear helpers. Field accessors/builders snapshot the structured field map so record composition does not alias the original fields. Expanded `stdlib/std_log_basic.otter`; updated docs/18, docs/24, ROADMAP, and goals. Replaceable global logger backends remain provider/runtime work.

[DONE] /goal Fill the portable `std:fs.DirEntry` / `FileKind` value shape. — DONE: added Otter-authored `FileKind` variants (`RegularFile`, `Directory`, `Symlink`, `OtherKind`), constructor helpers, and `DirEntry` with snapshotting `path()` / `kind()` accessors. `FileKind` and `DirEntry` now implement equality, clone, hash, stringification, and diagnostic debug rendering so future provider-backed `read_dir` can yield ordinary comparable values without changing the public API. Cataloged the new `std:fs` exports, expanded `stdlib/std_fs_path_basic.otter`, added an explicit-import error case, and updated docs/18, docs/24, docs/29, ROADMAP, and goals. `File`, metadata, native separator normalization, canonicalization, and host-backed directory reads remain provider/runtime work. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_fs_path_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (332/332 e2e), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, and `git diff --check`.

[DONE] /goal Fill the portable `std:process` value layer without pretending host process APIs exist yet. — DONE: added `crates/compiler/src/sema/stdlib_src/std/process.otter`, wired it into the builtins assembly and catalog, and exported `Command`, `ExitStatus`, captured `Output`, plus constructor helpers. `Command` provides snapshotting builders/accessors for args, env, and cwd, including immutable-style replacement/clearing helpers; `ExitStatus` models exited/signaled/unknown statuses; `Output` snapshots stdout/stderr byte buffers. The slice implements equality, clone, hash, diagnostic debug rendering, and stringification where appropriate, while initially leaving host env access/mutation, process spawning, `Child`, process exit/abort control, and target-specific status details as provider/runtime work. Later slices added host env helpers and moved process-control markers to `std:process`. Added e2e coverage for value behavior, snapshot isolation, explicit-import enforcement, and rejecting `core:process`; updated docs/18, docs/24, docs/29, ROADMAP, and goals. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_process_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (335/335 e2e), `rustfmt --check crates/compiler/src/sema/stdlib.rs crates/compiler/src/sema/symbols.rs`, and `git diff --check`.

[DONE] /goal Fill the portable atomic `Ordering` value contract without implementing atomic handles prematurely. — DONE: added `crates/compiler/src/sema/stdlib_src/core/sync_atomic.otter`, wired it into the builtins assembly and catalog under `core:sync:atomic`, and exported `Ordering`, its five memory-ordering variants (`Relaxed`, `Acquire`, `Release`, `AcqRel`, `SeqCst`), plus constructor helpers. `Ordering` now implements equality, clone, hash, stringification, and diagnostic debug rendering. Atomic handle types and load/store/swap/compare-exchange/fetch operations remain target/runtime work. This originally landed under `std:sync:atomic`; a later std/core split pass moved it to `core:sync:atomic` because atomic operations are compiler/runtime substrate under the revised core/std criteria. Added e2e coverage for ordering behavior, explicit-import enforcement, and rejecting the old `std:sync:atomic` path; updated docs/18, docs/24, docs/29, ROADMAP, and goals. Verification before the move: `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_sync_atomic_ordering_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (338/338 e2e), `rustfmt --check crates/compiler/src/sema/stdlib.rs crates/compiler/src/sema/symbols.rs`, and `git diff --check`.

[DONE] /goal Fill `std:io` stderr marker helpers. — DONE: added runtime-backed `eprint(str): null` and `eprintln(str): null` beside `print`/`println` in `std:io`. Wired the builtin path end-to-end: marker functions in `stdlib_src/std/io.otter`, catalog exports, builtin resolution/type checking, backend intrinsic lowering, JIT symbol registration, runtime `lang_eprint`/`lang_eprintln`, and LSP hover/signature metadata. Expanded e2e coverage for stderr writes and explicit-import enforcement; updated docs/18, docs/24, docs/29, ROADMAP, and goals. Concrete `stdin`/`stdout`/`stderr` handle values and buffered adapters remain provider/runtime work. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_io_stderr_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (340/340 e2e), `cargo check -p lsp`, `cargo check -p compiler -p backend -p runtime`, `rustfmt --check` on touched non-LSP Rust files, and `git diff --check`. Note: whole-file `rustfmt --check` over existing LSP files still wants broad unrelated formatting churn, so those files were compile-checked instead of bulk-formatted.

[DONE] /goal Make `std:bytes.Bytes` implement `std:fmt.FmtSink`. — DONE: added a pure Otter Fusion `FmtSink` impl for `Bytes` so it is the standard in-memory UTF-8 formatting sink. `write_str(s)` encodes `s` through `Bytes.from_str`, appends the bytes, and returns `null` because the current in-memory sink cannot fail. Expanded `stdlib/std_fmt_contracts.otter` to cover interface-object dispatch through `FmtSink`, direct `Bytes.write_str`, strict UTF-8 round-trip, and the expected byte rendering. Updated docs/18, docs/24, ROADMAP, and goals. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_fmt_contracts.otter`, and `cargo test -p cli --test suite -- --nocapture` green (340/340 e2e).

[DONE] /goal Fill the portable `std:fs.Metadata` / `Permissions` value shape. — DONE: added pure Otter Fusion `Permissions` and `Metadata` values to `std:fs`, plus constructor helpers `permissions(read_only, executable)` and `metadata(kind, byte_len, permissions)`. `Permissions` exposes read-only/executable predicates and immutable-style builders; `Metadata` exposes file kind, length, permissions, and file/dir/symlink predicates. Both values implement equality, clone, hash, stringification, and diagnostic debug rendering, with nested accessors cloning values so future provider-backed filesystem APIs can return ordinary comparable snapshots. Catalog exports, `stdlib/std_fs_path_basic.otter`, and an explicit-import error case were updated. Docs/18, docs/24, ROADMAP, and goals now distinguish implemented metadata value contracts from future host-backed metadata queries. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_fs_path_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (341/341 e2e), and `rustfmt --check crates/compiler/src/sema/stdlib.rs`.

[DONE] /goal Implement `std:collections.Deque` as an ordinary std collection. — DONE: added `crates/compiler/src/sema/stdlib_src/std/collections.otter`, wired it into toolchain source assembly and the stdlib catalog as `std:collections`, and exported `Deque<T>` plus `deque<T>()`. The deque is deliberately `std:`, not `core:`, because no syntax or compiler lowering depends on its layout. It uses two internal lists for front/back storage and implements `push_front`, `push_back`, `pop_front`, `pop_back`, `front`, `back`, `size`, `is_empty`, `to_list`, `iter`, and `clear`. Added value semantics: equality when `T: Eq`, clone when `T: Clone`, ordered deterministic hashing when `T: Hash`, stringification when `T: ToStr`, and diagnostic debug rendering when `T: std:fmt.Debug`. Added runtime and explicit-import e2e coverage; updated docs/18, docs/24, ROADMAP, and goals. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_collections_deque_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (343/343 e2e), `rustfmt --check crates/compiler/src/sema/stdlib.rs crates/compiler/src/sema/symbols.rs`, and `git diff --check`.

[DONE] /goal Fill portable `std:time.DateTime` / `TimeZone` value contracts. — DONE: added Otter-authored `TimeZone` (`Utc`, `FixedOffset`, `NamedTimeZone`), `DateTime`, and `TimeError` values plus constructor helpers `utc`, `fixed_offset`, `named_timezone`, `time_error`, and `date_time`. `DateTime.new` validates month/day ranges, leap years, time-of-day fields, and nanosecond precision; `TimeZone` models UTC, fixed offsets, and named zone identifiers as ordinary values without pretending named-zone resolution exists yet. The value layer implements equality, clone, hash, stringification/ISO-like formatting, accessors, and diagnostic debug rendering; `TimeError` implements `std:error.Error` plus value semantics. Added runtime and explicit-import e2e coverage; updated docs/18, docs/24, ROADMAP, and goals. System-time conversion, local timezone lookup, timezone databases, leap-second policy, and parsing remain planned provider/library work. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_time_datetime_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (345/345 e2e), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, and `git diff --check`.

[DONE] /goal Fill portable IPv6 text parsing for `std:net:types`. — DONE: added pure Otter Fusion `parse_ip_v6(text): IpAddr | ParseError`, exported it from the stdlib catalog, and expanded `std_net_types_basic.otter`. The parser accepts full eight-group IPv6 and `::`-compressed forms, validates hex groups and group counts, rejects multiple compression markers, and returns positioned `ParseError`s. Updated docs/18, docs/24, ROADMAP, and goals to mark IPv6 parsing implemented while leaving stricter URL canonicalization, DNS, sockets, and target-backed networking as future work. Later slices added scoped zone identifiers and lowercase hex IPv6 rendering. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_net_types_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (345/345 e2e), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, and `git diff --check`.

[DONE] /goal Fill portable ISO-like parsing for `std:time.DateTime`. — DONE: added pure Otter Fusion `parse_iso8601(s): DateTime | TimeError` and `DateTime.parse_iso8601`. The parser accepts the same portable shapes emitted by `format_iso8601`: UTC `Z`, fixed `+HH:MM` / `-HH:MM` offsets, optional fractional seconds up to nanosecond precision, and bracketed named timezone identifiers. It reuses `DateTime.new` validation and returns `TimeError` for malformed fields, invalid dates, bad fractions, missing timezone data, and out-of-range offsets. Exported the helper from the stdlib catalog, expanded `std_time_datetime_basic.otter`, and updated docs/18, docs/24, ROADMAP, and goals. A later slice added UTC/fixed-offset system-time conversion; provider-backed timezone database resolution, local-time lookup, named-zone conversion, and leap-second policy remain planned. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_time_datetime_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (345/345 e2e), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, and `git diff --check`.

[DONE] /goal Fill flattened `std:http.Headers` entry snapshots. — DONE: added `HeaderEntry` plus `header_entry(name, value)`, `Headers.entries()`, and `Headers.names()` to the Otter-authored `std:http` value layer. `entries()` returns flattened name/value snapshots for repeated headers and `names()` snapshots header names, without implying wire parsing/client/server support. Later slices made these snapshots use ASCII case-insensitive normalized header names. `HeaderEntry` implements equality, clone, hash, stringification, and diagnostic debug rendering. Catalog exports, `std_http_basic.otter`, docs/18, docs/24, ROADMAP, and goals updated. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_http_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (345/345 e2e), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, and `git diff --check`.

[DONE] /goal Fill bracketed IPv6 socket address parsing for `std:net:types`. — DONE: added pure Otter Fusion `parse_socket_addr(text): SocketAddr | ParseError`, exported it from the stdlib catalog, and kept `parse_ipv4_socket_addr` as the compatibility IPv4-specific helper. `parse_socket_addr` accepts both `IPv4:port` and bracketed `[IPv6]:port` endpoints, validates closing brackets, host/port delimiters, decimal port ranges, and nested IPv6 parse errors with positions shifted back into the original input. Expanded `std_net_types_basic.otter` for IPv4/general parsing, bracketed IPv6 success, and bracket/delimiter/empty-port/host-parse failures; updated the explicit-import error case to cover the new symbol. Docs/17, docs/18, docs/24, ROADMAP, and goals updated so bracketed IPv6 socket endpoints are no longer listed as future work. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_net_types_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (345/345 e2e), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, and `git diff --check`.

[DONE] /goal Fill IPv4-embedded IPv6 text parsing for `std:net:types`. — DONE: extended pure Otter Fusion `parse_ip_v6(text)` so a final dotted IPv4 tail contributes the last two IPv6 groups, covering forms such as `::ffff:192.0.2.128` and full six-group-plus-IPv4 forms. The parser rejects non-final IPv4 tails and shifts nested IPv4 decimal parse errors back to the original IPv6 input position. Expanded `std_net_types_basic.otter`; updated docs/18, docs/24, ROADMAP, and goals so IPv4-mapped/embedded IPv6 text is no longer listed as future work. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_net_types_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (345/345 e2e), and `git diff --check`.

[DONE] /goal Fill deterministic floating-point helpers for `std:rand`. — DONE: added pure Otter Fusion `gen_f64(rng): f64` and `gen_range_f64(rng, low, high): f64`, exported them from the stdlib catalog, and kept them deterministic/reproducible over the existing `Rng.next_u64` contract. `gen_f64` samples a `[0, 1)` value from 53 bits of the next random word; `gen_range_f64` scales that value over a half-open range and returns `low` for empty/reversed ranges. Expanded `std_rand_basic.otter` and updated the explicit-import error case to cover `gen_f64`. Docs/17, docs/18, docs/24, ROADMAP, and goals updated. Later slices added `ThreadRng`, `OsRng`, OS entropy hooks, and exact byte-buffer helpers; richer distributions and cryptographic-strength guarantees remain planned. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_rand_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (345/345 e2e), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, and `git diff --check`.

[DONE] /goal Fill immutable replacement/clearing builders for `std:process.Command`. — DONE: added pure Otter Fusion `Command.with_program(program)`, `with_args(args)`, `with_env(env)`, `clear_env()`, and `clear_cwd()` beside existing `arg`, `env_var`, `inherit_env`, and `with_cwd`. All replacement builders snapshot their list/map/path inputs so later mutation cannot alias the command value; `clear_env()` represents an explicitly empty environment map while `inherit_env()` keeps `env == null` for future provider inheritance. Expanded `std_process_basic.otter` for replacement, input snapshotting, env clear vs inherit, cwd clearing, and original-command immutability. Docs/18, docs/24, ROADMAP, and goals updated. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_process_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (345/345 e2e), and `git diff --check`.

[DONE] /goal Fill `std:log.Record` field removal and clearing helpers. — DONE: added pure Otter Fusion `Record.without_field(key)` and `Record.clear_fields()` beside the existing `with_field` and `with_fields` builders. Both helpers preserve immutable value semantics by snapshotting through the existing field-map builder, so removing or clearing fields never aliases the original record. Expanded `std_log_basic.otter` for field removal, clearing, and original-record immutability; updated docs/17, docs/18, docs/24, ROADMAP, and goals. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_log_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (345/345 e2e), and `git diff --check`.

[DONE] /goal Fix `std:http` request/response accessor snapshot semantics and complete header removal builders. — DONE: `HttpRequest` accessors for method/url/headers/body/version and `HttpResponse` accessors for status/headers/body/version now return cloned value snapshots, so mutating returned `Headers` or `Bytes` values cannot mutate the original request/response. Added `HttpRequest.without_header`, `HttpRequest.clear_headers`, `HttpResponse.without_header`, and `HttpResponse.clear_headers` to complete the immutable header builder surface. Later slices made those header removal names ASCII case-insensitive through normalized keys. Expanded `std_http_basic.otter` for snapshot accessors, header removal, header clearing, and original-value immutability. Docs/17, docs/18, docs/24, ROADMAP, and goals updated. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_http_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (345/345 e2e), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, and `git diff --check`.

[DONE] /goal Fill immutable replacement builders and constructor snapshots for `std:fs.DirEntry` and `std:fs.Metadata`. — DONE: `dir_entry(...)`, `DirEntry.new(...)`, `metadata(...)`, and `Metadata.new(...)` now snapshot nested `Path`, `FileKind`, and `Permissions` values. Added pure Otter Fusion `DirEntry.with_path(path)`, `DirEntry.with_kind(kind)`, `Metadata.with_file_kind(kind)`, `Metadata.with_len(byte_len)`, and `Metadata.with_permissions(permissions)` so provider-produced filesystem values can be transformed without aliasing or mutating the original values. Expanded `std_fs_path_basic.otter` for builder results and original-value immutability; updated docs/18, docs/24, ROADMAP, and goals. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_fs_path_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (345/345 e2e), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, and `git diff --check`.

[DONE] /goal Fix `std:fs.Path` hidden-file stem/extension semantics. — DONE: `Path.extension()` now treats a single leading-dot filename such as `.env` as a basename with no extension instead of returning `env`; `file_stem()` therefore returns `.env`, and `with_extension("txt")` produces `.env.txt`. Multi-dot hidden filenames such as `.profile.bak` still expose the final extension (`bak`) and can remove/replace it. Expanded `std_fs_path_basic.otter`; updated docs/18, docs/24, ROADMAP, and goals. Verification: `cargo run -p cli -- run tests/cases/stdlib/std_fs_path_basic.otter`, `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo test -p cli --test suite -- --nocapture` green (345/345 e2e), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, and `git diff --check`.

[DONE] /goal Fix `std:json` array/object aliasing so JSON values preserve deep value semantics around mutable containers. — DONE: `json_array(items)` and `json_object(entries)` now deep-snapshot their source `List<Json>` / `Map<str, Json>` inputs, and `Json.as_array()`, `as_object()`, `get(key)`, and `at(i)` return deep snapshots instead of aliases into the original JSON tree. This prevents later source-container mutations or mutations to extracted arrays/objects/elements from changing an existing `Json` value. Expanded `std_json_basic.otter`; updated docs/18, docs/24, ROADMAP, and goals. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_json_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (345/345 e2e), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, and `git diff --check`.

[DONE] /goal Fix `std:http` request/response constructor and replacement-builder input snapshot semantics. — DONE: `http_request(...)`, `http_response(...)`, `HttpRequest.with_method`, `with_url`, `with_headers`, `with_body`, `with_version`, and the matching `HttpResponse.with_status`, `with_headers`, `with_body`, and `with_version` now clone caller-provided value inputs before storing them. This completes the no-alias contract alongside existing snapshot accessors and header append/replace/remove/clear builders, so later mutations to source `Headers` or `Bytes` values cannot mutate an existing request/response. Expanded `std_http_basic.otter`; updated docs/18, docs/24, ROADMAP, and goals. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_http_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (345/345 e2e), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, and `git diff --check`.

[DONE] /goal Fix `std:net:types.Url` query-map constructor snapshot semantics. — DONE: `url(..., query, ...)` now clones the caller-provided `Map<str, str>` before storing it, and `parse_url(...)` inherits that no-alias behavior through the constructor. This keeps `Url` as an ordinary immutable value wrapper even though query pairs are supplied through a mutable `Map`; later source-map mutations no longer change an existing `Url`. Expanded `std_net_types_basic.otter`; updated docs/18, docs/24, ROADMAP, and goals. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_net_types_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (345/345 e2e), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, and `git diff --check`.

[DONE] /goal Fix `std:log.Record` constructor and nested accessor/builder snapshot semantics. — DONE: `record(...)` and all structured `*_with` helpers now snapshot caller-provided field maps through the record constructor. `Record.level()`, `Record.time()`, `with_level(...)`, and `with_time(...)` now clone nested value inputs/results, and `fields()`, `with_fields(...)`, `clone()`, field add/remove, and field clearing share the same field-map clone helper. This completes the no-alias contract for the public log-record helper API while keeping direct struct literals explicit. Expanded `std_log_basic.otter`; updated docs/18, docs/24, ROADMAP, and goals. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_log_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (345/345 e2e), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, and `git diff --check`.

[DONE] /goal Make `std:collections.Deque<T>` explicitly implement `std:fmt.Debug` when `T: Debug`. — DONE: added `extend<T: Debug> Deque<T>: Debug` so the documented diagnostic rendering contract is implemented directly rather than relying on stringification-shaped behavior. The debug implementation renders each element through `value.debug()`, so `Deque<T>` can be debug-rendered for types that implement `Debug` but not `ToStr`. Expanded `std_collections_deque_basic.otter` with a debug-only element type; updated docs/24, ROADMAP, and goals. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_collections_deque_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (345/345 e2e), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, and `git diff --check`.

[DONE] /goal Tighten `std:rand` choose/shuffle coverage and implemented-surface docs. — DONE: expanded `std_rand_basic.otter` to cover `choose(rng, empty_list) == null`, updated docs/24 to state empty-list `choose` and in-place `shuffle` behavior, and corrected ROADMAP's implemented export list to include `gen_f64` and `gen_range_f64` alongside integer ranges, `gen_bool`, `choose`, and `shuffle`. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_rand_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green on rerun after one summaryless interrupted attempt (345/345 e2e), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, and `git diff --check`.

[DONE] /goal Correct `std:io` documented contract drift for `Reader.read_to_end` and stderr helpers. — DONE: docs/24 no longer claims `Reader.read_to_end` has a default implementation; the implemented contract requires each `Reader` to provide it. The same catalog paragraph now lists implemented stderr helpers `eprint` and `eprintln` alongside `print` and `println`. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_io_contracts.otter`, `cargo run -p cli -- run tests/cases/stdlib/std_io_stderr_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (345/345 e2e), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, and `git diff --check`.

[DONE] /goal Tighten toolchain stdlib source-manifest plumbing for future providers/sysroots. — DONE: added an explicit `TOOLCHAIN_SOURCES` manifest in `crates/compiler/src/sema/stdlib.rs` that records each bundled Otter-authored `core:*`/`std:*` source file beside its public module path. `Program::collect_toolchain_source` now collects each bundled source through that manifest into hidden module-local owners under the private `__builtins__` root with separate synthetic `FileId`s, instead of parsing one concatenated blob, and the catalog/source-path sync test now derives its source set from the same manifest. Updated docs/29, docs/24, and ROADMAP to describe manifest-based collection rather than concatenation. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo test -p compiler sema::symbols::tests -- --nocapture`, `cargo test -p cli --test suite -- --nocapture` green (345/345 e2e), `rustfmt --check crates/compiler/src/sema/stdlib.rs crates/compiler/src/sema/symbols.rs`, and `git diff --check`.

[DONE] /goal Add e2e project-mode coverage for manifest `no-std` stdlib behavior. — DONE: extended the CLI suite framework with `//@ project: no-std`, which synthesizes a real temporary project containing `project.toml` and runs `otter_fusion run <project-dir>`. Added stdlib e2e cases proving a no-std project rejects `std:*` imports while still allowing `core:*` substrate imports. Updated tests/README and ROADMAP. Verification: `cargo test -p cli --test suite -- --nocapture` green (347/347 e2e), plus formatting and whitespace checks.

[DONE] /goal Make stdlib provider selection testable inside semantic collection. — DONE: added `Program::collect_multi_ctx_with_provider`, so semantic collection can build curated `core:*`/`std:*` import views from an explicit `StdProvider` instead of only the built-in active provider. Added unit coverage with a core-only provider proving unsupported `std:*` modules can be omitted while listed `core:*` substrate imports still resolve. Updated docs/29 and ROADMAP to reflect the explicit-provider semantic hook while CLI/sysroot selection remains future work. Verification: `cargo test -p compiler sema::symbols::tests -- --nocapture`, `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo test -p cli --test suite -- --nocapture` green (347/347 e2e), `rustfmt --check crates/compiler/src/sema/symbols.rs`, and `git diff --check`.

[DONE] /goal Make stdlib missing-module diagnostics provider-aware. — DONE: import resolution now distinguishes a truly unknown `core:*`/`std:*` path from a bundled toolchain module that the selected `StdProvider` intentionally omits. If a known module such as `std:io` is absent from the provider catalog, the diagnostic names the provider (`stdlib provider ... does not provide module ...`) instead of reporting a generic unknown built-in module. Expanded the explicit-provider unit test to assert this behavior; updated docs/29 and ROADMAP. Verification: `cargo test -p compiler sema::symbols::tests -- --nocapture`, `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo test -p cli --test suite -- --nocapture` green (347/347 e2e), `rustfmt --check crates/compiler/src/sema/symbols.rs`, and `git diff --check`.

[DONE] /goal Validate stdlib provider exports against collected toolchain source. — DONE: `build_builtin_views` now rejects a provider catalog that advertises an exported symbol not present in the collected `__builtins__` source, instead of silently creating a public module view missing the requested export. Added an explicit broken-provider regression test that asserts the provider/module/export diagnostic. Updated docs/29 and ROADMAP so provider export materialization is part of the stdlib extension contract. Verification: `cargo test -p compiler sema::symbols::tests -- --nocapture`, `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo test -p cli --test suite -- --nocapture` green (347/347 e2e), `rustfmt --check crates/compiler/src/sema/symbols.rs`, and `git diff --check`.

[DONE] /goal Validate custom stdlib provider module identity invariants. — DONE: `build_builtin_views` now rejects duplicate module paths in an explicit `StdProvider` and reports tier/path-root mismatches such as a `StdTier::Std` module under `core:*`. Added broken-provider unit tests for duplicate `core:collections` entries and wrong-tier `core:collections` entries. Updated docs/29 and ROADMAP so provider module uniqueness and tier-root agreement are part of the stdlib extension contract. Verification: `cargo test -p compiler sema::symbols::tests -- --nocapture`, `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo test -p cli --test suite -- --nocapture` green (347/347 e2e), `rustfmt --check crates/compiler/src/sema/symbols.rs`, and `git diff --check`.

[DONE] /goal Validate duplicate exports in custom stdlib providers. — DONE: `build_builtin_views` now rejects duplicate entries within a provider module's export list, matching the built-in catalog invariant instead of letting custom providers advertise ambiguous metadata. Added an explicit `DuplicateExportProvider` regression test for a repeated `List` export under `core:collections`. Updated docs/29 and ROADMAP so duplicate-free provider exports are part of the stdlib extension contract. Verification: `cargo test -p compiler sema::symbols::tests -- --nocapture`, `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo test -p cli --test suite -- --nocapture` green (347/347 e2e), `rustfmt --check crates/compiler/src/sema/symbols.rs`, and `git diff --check`.

[DONE] /goal Validate custom stdlib provider module path shape. — DONE: `build_builtin_views` now rejects provider module paths that only name a scheme root such as `std` without a module segment, preventing custom providers from creating ambiguous public builtin views. Added an explicit `RootOnlyProvider` regression test and updated docs/29 plus ROADMAP so provider paths are documented as scheme-plus-module paths. Verification: `cargo test -p compiler sema::symbols::tests -- --nocapture`, `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo test -p cli --test suite -- --nocapture` green (347/347 e2e), `rustfmt --check crates/compiler/src/sema/symbols.rs crates/compiler/src/sema/stdlib.rs`, and `git diff --check`.

[DONE] /goal Correct `std:fs` value-slice documentation drift. — DONE: fixed docs/24's malformed `std:fs` paragraph so the implemented `Path`, `DirEntry`, `FileKind`, `Permissions`, and `Metadata` value slice is described cleanly before the planned provider-backed filesystem APIs. Verification: `cargo run -p cli -- run tests/cases/stdlib/std_fs_path_basic.otter`, `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo test -p cli --test suite -- --nocapture` green (347/347 e2e), and `git diff --check`.

[DONE] /goal Tighten remaining docs around new `core:`/`std:` semantics. — DONE: scanned docs/examples/tests for stale old-core paths after the stdlib re-tiering; remaining `core:error`/`core:fmt`/`core:http`/similar references are intentional negative tests. Fixed docs/17's run-mode scheme table so `std:` availability is described as selected-provider dependent instead of OS-target-only, and removed a duplicate `format!` limitation bullet from docs/18. Verification: stale-path scan, `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo test -p cli --test suite -- --nocapture` green (347/347 e2e), and `git diff --check`.

[DONE] /goal Remove final portability-model wording from the core/std module diagram. — DONE: updated docs/17's built-in-module diagram so the layer between core and std is described as the provider boundary, with `std:` modules portable or target-backed, instead of "OS only for std targets." Verification: old-wording scan, `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo test -p cli --test suite -- --nocapture` green (347/347 e2e), and `git diff --check`.

[DONE] /goal Prove explicit stdlib providers can add custom `std:*` module views. — DONE: added a semantic regression test with a custom `StdProvider` that exposes a non-built-in `std:target_error` module path and successfully imports its exported `Error` type, covering the positive extension path in addition to omit/validation failures. Updated docs/29 and ROADMAP so provider-added modules are part of the documented stdlib provider contract. Verification: `cargo test -p compiler sema::symbols::tests -- --nocapture`, `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo test -p cli --test suite -- --nocapture` green (347/347 e2e), `rustfmt --check crates/compiler/src/sema/symbols.rs crates/compiler/src/sema/stdlib.rs`, and `git diff --check`.

[DONE] /goal Ensure `no-std` blocks provider-added `std:*` module views. — DONE: added a semantic regression test proving a custom `StdProvider` that exposes `std:target_error` still cannot make that module importable when the project context is `no-std`; resolution reports the standard `std:` unavailable diagnostic and does not bind the exported `Error` type. Verification: `cargo test -p compiler sema::symbols::tests -- --nocapture`, `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo test -p cli --test suite -- --nocapture` green (347/347 e2e), `rustfmt --check crates/compiler/src/sema/symbols.rs crates/compiler/src/sema/stdlib.rs`, and `git diff --check`.

[DONE] /goal Validate stdlib provider module path segments are addressable. — DONE: `build_builtin_views` now rejects provider module paths containing unaddressable segments such as empty segments, `.`/`..`, or embedded `/`, before creating a synthetic builtin view. Added an explicit broken-provider regression test for an empty `std:` segment and updated docs/29 to document addressable provider path segments. Verification: `cargo test -p compiler sema::symbols::tests -- --nocapture`, `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo test -p cli --test suite -- --nocapture` green (347/347 e2e), `rustfmt --check crates/compiler/src/sema/symbols.rs crates/compiler/src/sema/stdlib.rs`, and `git diff --check`.

[DONE] /goal Prevent malformed stdlib provider tier/path entries from materializing import views. — DONE: `build_builtin_views` now skips provider module entries whose `StdTier` does not match their `core`/`std` path root after reporting the provider diagnostic, so a wrong-tier catalog entry cannot also expose a public module through the invalid path. Expanded the wrong-tier provider regression test to import `core:collections` and assert that `List` is not bound. Updated docs/29 and ROADMAP to document that malformed provider entries are rejected before becoming importable views. Verification: `cargo test -p compiler sema::symbols::tests -- --nocapture`, `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo test -p cli --test suite -- --nocapture` green (347/347 e2e), `rustfmt --check crates/compiler/src/sema/symbols.rs crates/compiler/src/sema/stdlib.rs`, and `git diff --check`.

[DONE] /goal Prevent malformed stdlib provider export lists from materializing partial import views. — DONE: `build_builtin_views` now validates all exports in a provider module before creating its public view. Duplicate exports or exports missing from the collected toolchain source still produce provider diagnostics, but the entire module entry is skipped instead of exposing a partial view. Expanded broken-provider regressions to assert that missing-export and duplicate-export modules are absent from `builtin_modules`. Updated ROADMAP to document that all malformed provider entries are diagnosed and skipped before becoming importable views. Verification: `cargo test -p compiler sema::symbols::tests -- --nocapture`, `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo test -p cli --test suite -- --nocapture` green (347/347 e2e), `rustfmt --check crates/compiler/src/sema/symbols.rs crates/compiler/src/sema/stdlib.rs`, and `git diff --check`.

[DONE] /goal Enforce addressable path-shape invariants on built-in stdlib manifests. — DONE: expanded `sema::stdlib` catalog tests so built-in `TOOLCHAIN_MODULES` and `TOOLCHAIN_SOURCES` must use scheme-plus-module paths rooted at `core` or `std` with no empty, `.`, `..`, or embedded `/` segments. This keeps the built-in catalog/source manifests aligned with the stricter custom-provider path contract. Updated ROADMAP to record the invariant. Verification: `cargo test -p compiler sema::symbols::tests -- --nocapture`, `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo test -p cli --test suite -- --nocapture` green (347/347 e2e), `rustfmt --check crates/compiler/src/sema/stdlib.rs crates/compiler/src/sema/symbols.rs`, and `git diff --check`.

[DONE] /goal Fill `std:bytes.BytesCursor` value semantics. — DONE: added pure Otter Fusion equality and hashing for `BytesCursor` over both the snapshotted byte buffer and current cursor position, completing the cursor's ordinary value surface alongside clone, diagnostic stringification, and debug rendering. Expanded `std_bytes_basic.otter` to cover equal cloned cursors, hash equality, and inequality after seeking a clone. Updated docs/18, docs/24, ROADMAP, and goals. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_bytes_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (347/347 e2e), `rustfmt --check crates/compiler/src/sema/stdlib.rs crates/compiler/src/sema/symbols.rs`, and `git diff --check`.

[DONE] /goal Fill `std:io` union value `Eq` method surfaces. — DONE: added pure Otter Fusion `Eq` impls for `SeekFrom` and `IoErrorKind`, delegating to their existing variant-aware `equals` helpers. Expanded `std_io_contracts.otter` to cover both `.equals(...)` and `.eq(...)` along with hash/string/debug behavior. Updated docs/18, docs/24, ROADMAP, and goals. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_io_contracts.otter`, `cargo test -p cli --test suite -- --nocapture` green (347/347 e2e), `rustfmt --check crates/compiler/src/sema/stdlib.rs crates/compiler/src/sema/symbols.rs`, and `git diff --check`.

[DONE] /goal Fix overloaded equality for stdlib union alias values. — DONE: operator-overload resolution now tries the same resolved `extend` method path for non-nominal alias-lowered operands before falling back to primitive comparison, so union aliases such as `std:io.SeekFrom` and `std:io.IoErrorKind` honor their `Eq.eq` impls through `==`. Named types still emit the existing “operator not implemented” diagnostic when no overload exists, while builtin operands keep primitive handling. Added a compiler regression for a union alias `Eq` impl and expanded `std_io_contracts.otter` so `.equals(...)`, `.eq(...)`, and `==` agree. Updated docs/18, docs/24, ROADMAP, and goals. Verification: `cargo test -p compiler sema::check::tests -- --nocapture`, `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_io_contracts.otter`, `cargo test -p cli --test suite -- --nocapture` green (347/347 e2e), `rustfmt --check crates/compiler/src/sema/check/methods.rs crates/compiler/src/sema/stdlib.rs crates/compiler/src/sema/symbols.rs`, and `git diff --check`.

[DONE] /goal Add the first target-backed `std:fs` text file slice. — DONE: `std:fs` is now a mixed module for filesystem text APIs. Added private Rust-backed `lang_fs_read_text`, `lang_fs_write_text`, and `lang_fs_append_text` hooks over the runtime `str` ABI, with the Otter-authored public layer decoding results into `str | IoError` or `null | IoError`. Public exports now include `File`, `read_to_string(path)`, `write_string(path, contents)`, and `append_string(path, contents)`. `File.open` validates readability, `File.create` creates/truncates, `File.append` creates/opens append mode, `File.path` snapshots the path, `File.write_string` respects append mode, `File.append_string` appends explicitly, and `File.close` succeeds without holding an OS descriptor. Added runtime hook unit tests and an e2e `std_fs_text_basic.otter` case covering write, append, read, file open/create/append modes, close, and missing-file `IoError`. Updated docs/18, docs/24, docs/29, ROADMAP, and goals. Binary `Bytes` I/O, real descriptor-backed `Reader`/`Writer`/`Seeker`, metadata queries, native separator normalization, canonicalization, and `read_dir` remain planned provider work. Verification: `cargo test -p runtime fs::tests -- --nocapture`, `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_fs_text_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (348/348 e2e), `rustfmt --check crates/runtime/src/fs.rs crates/runtime/src/lib.rs crates/backend/src/lib.rs crates/compiler/src/sema/stdlib.rs`, and `git diff --check`.

[DONE] /goal Add target-backed `std:fs.Path` existence/type queries. — DONE: added private Rust-backed `lang_fs_exists`, `lang_fs_is_file`, and `lang_fs_is_dir` hooks and public `Path.exists()`, `Path.is_file()`, and `Path.is_dir()` methods returning `bool | IoError`. Missing paths return `false`; other host filesystem errors are decoded into ordinary `IoError` values. Expanded `std_fs_text_basic.otter` to cover regular files, `/tmp` as a directory, and missing paths through all three queries. Updated docs/18, docs/24, ROADMAP, and goals so these path queries are implemented while binary `Bytes` I/O, descriptor-backed IO contracts, richer metadata queries, canonicalization, and `read_dir` remain planned provider work. Verification: `cargo test -p runtime fs::tests -- --nocapture`, `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_fs_text_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (348/348 e2e), `rustfmt --check crates/runtime/src/fs.rs crates/runtime/src/lib.rs crates/backend/src/lib.rs crates/compiler/src/sema/stdlib.rs`, and `git diff --check`.

[DONE] /goal Add target-backed `std:fs.Path` metadata snapshots. — DONE: added private Rust-backed `lang_fs_kind`, `lang_fs_len`, `lang_fs_read_only`, and `lang_fs_executable` hooks plus public `Path.file_kind()`, `Path.byte_len()`, `Path.permissions()`, and `Path.metadata()` methods returning ordinary `FileKind`, `u64`, `Permissions`, and `Metadata` snapshots through `IoError` unions. File kind uses symlink-aware host metadata, length/read-only/executable use regular metadata, missing paths surface `IoError`, and executable detection is provider-specific (Unix execute bits, false on non-Unix). Expanded runtime hook tests and `std_fs_text_basic.otter` for file metadata and directory metadata. Updated docs/18, docs/24, ROADMAP, and goals so path metadata queries are implemented while binary `Bytes` I/O, descriptor-backed IO contracts, canonicalization, and `read_dir` remain planned provider work. Verification: `cargo test -p runtime fs::tests -- --nocapture`, `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_fs_text_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (348/348 e2e), `rustfmt --check crates/runtime/src/fs.rs crates/runtime/src/lib.rs crates/backend/src/lib.rs crates/compiler/src/sema/stdlib.rs`, and `git diff --check`.

[DONE] /goal Add target-backed `std:fs` path mutation operations. — DONE: added private Rust-backed `lang_fs_remove`, `lang_fs_rename`, `lang_fs_create_dir`, and `lang_fs_create_dir_all` hooks plus public `remove(path)`, `rename(from, to)`, `create_dir(path)`, and `create_dir_all(path)` functions in the mixed `std:fs` module. `remove(path)` is intentionally non-recursive: regular files and symlinks use file removal, directories must be empty and use directory removal. Exported the new APIs through the stdlib catalog, registered their JIT symbols, expanded runtime hook tests and `std_fs_text_basic.otter` to cover create, recursive create, rename, ordered empty-directory cleanup, and post-remove existence, and updated docs/18, docs/24, ROADMAP, and goals. Binary `Bytes` I/O, descriptor-backed IO contracts, canonicalization, and `read_dir` remain planned provider work. Verification: `cargo test -p runtime fs::tests -- --nocapture`, `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_fs_text_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (348/348 e2e), `rustfmt --check crates/runtime/src/fs.rs crates/runtime/src/lib.rs crates/backend/src/lib.rs crates/compiler/src/sema/stdlib.rs`, and `git diff --check`.

[DONE] /goal Add target-backed `std:fs.Path` canonicalization. — DONE: added private Rust-backed `lang_fs_canonicalize` plus public `Path.canonicalize()` and module-level `canonicalize(path)` APIs returning `Path | IoError` through the mixed `std:fs` layer. The provider hook uses host filesystem canonicalization, returns canonical path strings as ordinary `Path` values, resolves symlinks according to the selected target, and reports missing/non-canonicalizable paths as `IoError`. Exported the helper through the stdlib catalog, registered the JIT symbol, expanded runtime hook tests and `std_fs_text_basic.otter` to cover method/function canonicalization and missing-path errors, and updated docs/18, docs/24, ROADMAP, and goals so filesystem canonicalization is implemented while binary `Bytes` I/O, descriptor-backed IO contracts, native separator normalization, and `read_dir` remain planned provider work. Verification: `cargo test -p runtime fs::tests -- --nocapture`, `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_fs_text_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (348/348 e2e), `rustfmt --check crates/runtime/src/fs.rs crates/runtime/src/lib.rs crates/backend/src/lib.rs crates/compiler/src/sema/stdlib.rs`, and `git diff --check`.

[DONE] /goal Add native separator conversion for `std:fs.Path`. — DONE: added private Rust-backed `lang_fs_native_separator` plus public `native_separator()`, `Path.from_native(s)`, module-level `path_from_native(s)`, and `Path.to_native_str()` APIs in the mixed `std:fs` module. `Path` remains slash-separated in the portable value layer, while native conversion round-trips through the selected provider's path separator so target/custom std providers can expose platform path strings without changing import syntax. Exported the APIs through the stdlib catalog, registered the JIT symbol, expanded runtime hook tests and `std_fs_path_basic.otter` with a platform-independent native round-trip, and updated docs/18, docs/24, ROADMAP, and goals so native separator conversion is implemented while binary `Bytes` I/O, descriptor-backed IO contracts, and `read_dir` remain planned provider work. Verification: `cargo test -p runtime fs::tests -- --nocapture`, `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_fs_path_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (348/348 e2e), `rustfmt --check crates/runtime/src/fs.rs crates/runtime/src/lib.rs crates/backend/src/lib.rs crates/compiler/src/sema/stdlib.rs`, and `git diff --check`.

[DONE] /goal Add snapshot-backed `std:fs.read_dir`. — DONE: added private Rust-backed `lang_fs_read_dir`, public `read_dir(path): DirEntries | IoError`, and exported `DirEntries`, a snapshot-backed iterator implementing `Iterator<DirEntry | IoError>`. The runtime hook snapshots the selected provider directory into length-prefixed native path records with file-kind tags; the Otter layer decodes those records into portable `DirEntry` values using `path_from_native`, reports malformed payloads as `IoError`, and keeps iteration over the completed snapshot independent from OS directory handles. Exported the APIs through the stdlib catalog, registered the JIT symbol, expanded runtime hook tests and `std_fs_text_basic.otter` for successful iteration and missing-directory errors, and updated docs/18, docs/24, ROADMAP, and goals so host directory reads are implemented while binary `Bytes` I/O and descriptor-backed handles remain planned provider work. Verification: `cargo test -p runtime fs::tests -- --nocapture`, `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_fs_text_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (348/348 e2e), `rustfmt --check crates/runtime/src/fs.rs crates/runtime/src/lib.rs crates/backend/src/lib.rs crates/compiler/src/sema/stdlib.rs`, and `git diff --check`.

[DONE] /goal Add target-backed binary `std:fs` read/write over `Bytes`. — DONE: added private Rust-backed `lang_fs_read_bytes` and `lang_fs_write_bytes` hooks plus public `read(path): Bytes | IoError` and `write(path, data): null | IoError` APIs in the mixed `std:fs` module. The Rust hooks operate on raw host file bytes; the private runtime boundary uses ASCII hex payloads so arbitrary byte values, including invalid UTF-8 such as `0xff`, round-trip correctly without coupling Rust to the Otter-authored `Bytes { data: List<u8> }` layout. Exported the APIs through the stdlib catalog, registered JIT symbols, expanded runtime hook tests and `std_fs_text_basic.otter` for binary round-trips and missing-file errors, and updated docs/18, docs/24, ROADMAP, and goals so binary file read/write is implemented while descriptor-backed `Reader`/`Writer`/`Seeker` handles remain planned provider work. Verification: `cargo test -p runtime fs::tests -- --nocapture`, `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_fs_text_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (348/348 e2e), `rustfmt --check crates/runtime/src/fs.rs crates/runtime/src/lib.rs crates/backend/src/lib.rs crates/compiler/src/sema/stdlib.rs`, and `git diff --check`.

[DONE] /goal Add descriptor-backed `std:fs.File` IO contracts. — DONE: `File.open`, `File.create`, and `File.append` now allocate real runtime file handles through private `lang_fs_file_*` hooks, and `File.close` releases the registry handle. The Otter-authored public layer exposes `File.handle_id()` and implements `std:io.Reader`, `std:io.Writer`, and `std:io.Seeker` with byte reads, read-to-end, byte writes, flush, and `SeekFrom` handling while keeping errors as ordinary `IoError` values. Registered the JIT symbols, added runtime hook coverage for write/flush/seek/read/read_to_end/close/closed-handle errors, expanded `std_fs_text_basic.otter` to exercise handle-backed binary IO, and updated docs/18, docs/24, ROADMAP, and goals so descriptor-backed file handles are implemented while buffered adapters and richer open options remain future work. Verification: `cargo test -p runtime fs::tests -- --nocapture`, `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_fs_text_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (348/348 e2e), `rustfmt --check crates/runtime/src/fs.rs crates/runtime/src/lib.rs crates/backend/src/lib.rs crates/compiler/src/sema/stdlib.rs`, and `git diff --check`.

[DONE] /goal Audit std/core tier documentation after the descriptor-backed file slice. — DONE: checked the current stdlib catalog and bundled `stdlib_src` layout against the new rule that `core:*` is only compiler/runtime substrate while `std:*` is ordinary official library API, portable or target-backed. The live catalog remains limited to `core:prelude`, `core:collections`, `core:async`, `core:ffi`, and `core:compiler`, with the existing catalog test guarding against unreviewed core growth. Tightened docs/18 and docs/24 so `core:collections.Set` is justified by the compiler-recognized collection construction/tracing shape and future syntax reservation rather than generic portability. Removed stale docs/22 wording that described banned `std:*` macro imports as "OS-dependent"; the macro chapter now frames it as compiler-phase/capability isolation. Updated docs/24, docs/29, and ROADMAP to stop describing implemented `std:fs` metadata, directory iteration, and descriptor-backed handles as future work. Verification: targeted drift search, `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo test -p cli --test suite -- --nocapture` green (348/348 e2e), and `git diff --check`.

[DONE] /goal Add stdout/stderr `std:io.Writer` handles. — DONE: added `Stdout`, `Stderr`, `stdout()`, and `stderr()` to the `std:io` catalog as ordinary std target-backed stream handles. The Otter-authored public layer implements `Writer` for both handles over `Bytes`, encoding raw bytes through private hex payloads and decoding host write/flush failures into `IoError`. Added Rust runtime hooks `lang_io_stdout_write`, `lang_io_stderr_write`, `lang_io_stdout_flush`, and `lang_io_stderr_flush`, registered them for JIT, and covered raw hook behavior plus stdout/stderr handle e2e writes. Updated docs/18, docs/24, ROADMAP, and stale std:fs test descriptions so stdout/stderr are implemented while stdin and buffered adapters remain planned provider work. Verification: `cargo test -p runtime strings::tests -- --nocapture`, `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_io_stderr_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (348/348 e2e), `rustfmt --check crates/runtime/src/strings.rs crates/backend/src/lib.rs crates/compiler/src/sema/stdlib.rs`, and `git diff --check`.

[DONE] /goal Tighten stdlib docs and negative coverage after stdout/stderr handles. — DONE: updated docs/17 and docs/26 so `std:io` no longer says all concrete target-backed handles are planned and `std:fs` no longer says files, metadata, and directories are merely planned. Extended docs/24's `std:io` contract text to name `stdout()`/`stderr()` as implemented catalog exports. Expanded `std_io_stderr_requires_import.otter` so the near-empty prelude rule covers both the older stderr helper and the new `stdout()` handle. Verification: targeted stale-wording scan, `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_io_stderr_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (348/348 e2e), and `git diff --check`.

[DONE] /goal Add stdin `std:io.Reader` handle and stdin-fed e2e support. — DONE: added `Stdin` and `stdin()` to the `std:io` catalog as an ordinary target-backed stream handle implementing `Reader` over `Bytes`. Runtime hooks `lang_io_stdin_read` and `lang_io_stdin_read_to_end` read raw stdin bytes and return private hex payloads decoded by the Otter stdlib layer into appended `Bytes` buffers and `IoError` results. Registered the hooks for JIT, added a `//@ stdin: <text>` CLI suite directive so e2e cases can feed exact stdin bytes, documented the directive in `tests/README.md`, and added `std_io_stdin_basic.otter` covering chunked `read`, `read_to_end`, and EOF. Updated docs/17, docs/18, docs/24, docs/26, and ROADMAP so stdin/stdout/stderr are implemented while buffered adapters remain planned. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `printf abcdef | cargo run -p cli -- run tests/cases/stdlib/std_io_stdin_basic.otter`, `cargo test -p runtime strings::tests -- --nocapture`, `cargo test -p cli --test suite -- --nocapture` green (349/349 e2e), `rustfmt --check crates/cli/tests/framework/mod.rs crates/runtime/src/strings.rs crates/backend/src/lib.rs crates/compiler/src/sema/stdlib.rs`, and `git diff --check`.

[DONE] /goal Add `std:io` buffered Reader/Writer adapters. — DONE: added `BufReader`, `BufWriter`, `buf_reader(inner)`, and `buf_writer(inner)` to `std:io` as interface-object adapters over any `Reader` or `Writer`. `BufReader` supports configurable capacity, `buffered_len`, chunked `Reader` forwarding, `read_to_end`, `read_line` including the trailing newline when present, and `lines()` iteration over `str | IoError`. `BufWriter` supports configurable capacity, `buffered_len`, buffered `write`/`write_all`, and explicit `flush`. Generic-specialized wrappers remain future library/compiler work; the implemented stable surface uses `Reader`/`Writer` interface objects and works with existing in-memory and target-backed handles. Added e2e coverage over `BytesCursor` and stdout, expanded near-empty-prelude coverage for `BufReader`/`buf_reader`, and updated docs/17, docs/18, docs/24, ROADMAP, and goals. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_io_buffered_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (350/350 e2e), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, and `git diff --check`.

[DONE] /goal Add OS-backed `std:rand` RNG handles. — DONE: `std:rand` is now a mixed module: `SeededRng` remains the deterministic Otter-authored PRNG, while `OsRng`, `ThreadRng`, `RandomError`, `os_rng()`, `thread_rng()`, and `random_error()` are exported through the stdlib catalog. The runtime now exposes a target entropy hook (`lang_rand_os_u32`) backed by `getrandom`; `OsRng` provides fallible `try_next_u32`, `try_next_u64`, and `try_fill_bytes` methods returning `RandomError`, plus a non-fallible `Rng` impl that panics only if the provider entropy source fails. `ThreadRng` is a per-value generator seeded from `OsRng` and implements `Rng`; `RandomError` implements `std:error.Error`, equality, clone, hash, stringification, and debug rendering. Expanded `std_rand_basic.otter` to exercise deterministic RNGs, OS-backed handles, thread RNG construction, generic helper calls, and error value semantics; expanded near-empty-prelude coverage for `OsRng`/`thread_rng`. Updated docs/17, docs/18, docs/24, docs/29, ROADMAP, and goals so OS entropy/per-value RNG handles are implemented while cryptographic-strength API guarantees and richer distributions remain planned. Verification: `cargo test -p runtime rand::tests -- --nocapture`, `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_rand_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (350/350 e2e), `rustfmt --check crates/runtime/src/rand.rs crates/runtime/src/lib.rs crates/compiler/src/sema/stdlib.rs`, and `git diff --check`.

[DONE] /goal Add host environment helpers for `std:process`. — DONE: `std:process` is now a mixed module for process-state APIs. Added private runtime hooks `lang_process_args`, `lang_process_env`, `lang_process_env_all`, and `lang_process_set_env`, registered them for JIT, and exposed public `args(): List<str>`, `env(name): str | null`, `env_all(): Map<str, str>`, and `set_env(name, value): null`. The runtime snapshots argv/environment into length-prefixed string payloads decoded by the Otter-authored public layer, validates environment variable names and values before mutation, and keeps process spawning/child APIs planned. A later slice moved process-control markers to `std:process`. Expanded `std_process_basic.otter` to cover argv shape, environment read/write, `env_all` snapshots, and existing value semantics; expanded near-empty-prelude coverage for `args`/`set_env`. Updated docs/17, docs/18, docs/24, docs/29, ROADMAP, and goals so host environment access/mutation is implemented while process spawning and child handles remain planned. Verification: `cargo test -p runtime process::tests -- --nocapture`, `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_process_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (350/350 e2e), `rustfmt --check crates/runtime/src/process.rs crates/runtime/src/lib.rs crates/backend/src/lib.rs crates/compiler/src/sema/stdlib.rs`, and `git diff --check`.

[DONE] /goal Add explicit `std:fs.OpenOptions` for descriptor-backed files. — DONE: added an ordinary `OpenOptions` stdlib value with read/write/append/truncate/create/create_new flags, immutable-style `with_*` builders, accessors, equality, clone, hash, stringification, and debug rendering. Added `open_options()` and `File.open_with(path, options)` to the `std:fs` catalog; existing `File.open`, `File.create`, and `File.append` remain convenience constructors over the same runtime file-open hook. The Rust-backed `lang_fs_file_open` hook now accepts both legacy mode names and a validated six-flag options payload. Expanded runtime hook tests for create/truncate, append, and create_new failure; expanded `std_fs_text_basic.otter` for open options, append mode, create_new error, and value semantics; expanded near-empty-prelude coverage for `OpenOptions`/`open_options`. Updated docs/17, docs/18, docs/24, ROADMAP, and goals so richer open options are implemented while async filesystem adapters remain planned. Verification: `cargo test -p runtime fs::tests -- --nocapture`, `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_fs_text_basic.otter`, `cargo test -p cli --test suite -- --nocapture` green (350/350 e2e), `rustfmt --check crates/runtime/src/fs.rs crates/backend/src/lib.rs crates/compiler/src/sema/stdlib.rs`, and `git diff --check`.

[DONE] /goal Make bundled stdlib source module-local and add `std:time.sleep(Duration)`. — DONE: the compiler now collects each `TOOLCHAIN_SOURCES` entry into its own hidden module-local owner under the private `__builtins__` root, then builds catalog views from those owners instead of flattening all Otter-authored std/core source into one namespace. Internal stdlib source modules receive compiler-populated imports for unique shared toolchain names, while ambiguous duplicate names remain local, allowing ordinary exports like `std:time.sleep(Duration)` to coexist with `std:async.sleep(i64)`. Free-function intrinsic recognition now checks the exact module export def instead of only the spelling, and namespace access through toolchain views skips user-`pub` gating for catalog exports. Added Rust runtime hook `lang_time_sleep_nanos`, backend symbol registration, Otter Fusion wrapper `sleep(Duration)`, runtime/no-op tests, e2e coverage that imports `std:time.sleep` alongside namespace access to `std:async.sleep`, and missing-import coverage for `sleep`. Updated docs/18, docs/24, ROADMAP, and goals. Verification: `cargo test -p runtime time::tests -- --nocapture`, `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo test -p compiler sema::symbols::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_time_clocks_basic.otter`, `cargo run -p cli -- run tests/cases/stdlib/std_time_duration_requires_import.otter` (expected compile-error diagnostics), `rustfmt --check crates/compiler/src/sema/symbols.rs crates/compiler/src/sema/check/expr.rs crates/compiler/src/sema/stdlib.rs crates/runtime/src/time.rs crates/backend/src/lib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (350/350 e2e).

[DONE] /goal Enforce documented import collision rules for std/core module imports. — DONE: `Program::resolve_imports` now rejects duplicate named imports that bind the same local name instead of silently overwriting the earlier binding, and namespace imports now reject aliases that collide with local definitions, local modules, named imports, or another namespace alias. Local definitions still intentionally shadow named imports, preserving docs/17 §17.9. Added symbol-table tests for duplicate `std:async.sleep`/`std:time.sleep` imports, aliased duplicate names, local shadowing, namespace-vs-import collisions, and namespace-vs-local collisions. Added e2e compile-error coverage in `tests/cases/stdlib/std_duplicate_import_rejected.otter`. Verification: `cargo test -p compiler sema::symbols::tests -- --nocapture`, `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo run -p cli -- run tests/cases/stdlib/std_duplicate_import_rejected.otter` (expected compile-error diagnostic), `cargo run -p cli -- run tests/cases/stdlib/std_time_clocks_basic.otter`, `rustfmt --check crates/compiler/src/sema/symbols.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (351/351 e2e).

[DONE] /goal Expand old/wrong core-path rejection coverage for std runtime and collection APIs. — DONE: audited the live stdlib catalog against the new core/std rule. The core catalog remains limited to `core:prelude`, `core:collections`, `core:async`, `core:ffi`, and `core:compiler`, with `core_catalog_is_limited_to_language_substrate` guarding future drift. Added e2e compile-error cases proving `std:thread`, `std:sync`, `std:async` runtime helpers, and `std:collections.Deque` do not leak into wrong `core:*` paths: `std_thread_old_core_path_rejected.otter`, `std_sync_old_core_path_rejected.otter`, `std_async_runtime_old_core_path_rejected.otter`, and `std_collections_deque_old_core_path_rejected.otter`. Also updated stale goal text to describe current hidden module-local `TOOLCHAIN_SOURCES` collection instead of old concatenation. Verification: each new case was run directly and produced the expected diagnostic, `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (355/355 e2e).

[DONE] /goal Refresh stdlib extension/provider documentation for module-local toolchain sources. — DONE: updated `docs/29-extending-stdlib.html` so contributors add/extend stdlib modules against the current architecture: bundled `TOOLCHAIN_SOURCES` are collected into hidden module-local owners under the private `__builtins__` root, catalog views are curated API boundaries over those owners, and provider-added custom `std:*` views are currently tested over collected bundled source while external sysroot source loading remains future work. Updated the older ROADMAP "Real toolchain source" note to match the same module-local collection model. Verification: stale-architecture drift search, `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo test -p compiler sema::symbols::tests -- --nocapture`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (355/355 e2e).

[DONE] /goal Move process-control marker functions out of `core:prelude` and into `std:process`. — DONE: removed `exit` and `abort` from the `core:prelude` source/catalog exports, added them to the `std:process` source/catalog exports, and kept compiler builtin lowering tied to the resolved `DefId` so imported `std:process.exit`/`abort` still dispatch to `lang_exit`/`lang_abort`. Updated the no-std core availability case to rely on true core substrate, updated the exit-code panic case to import from `std:process`, removed stale example imports, and added `std_process_control_old_core_path_rejected.otter` so the old `core:prelude` path stays rejected. Added the catalog unit regression `process_control_markers_live_in_std_process_not_core_prelude` so future export drift is caught directly. Updated docs/18, docs/24, and ROADMAP to reflect that `panic` remains core language substrate while process control is a std process API. Verification: targeted stale-reference scan, `cargo test -p compiler sema::stdlib::tests -- --nocapture`, `cargo test -p compiler sema::symbols::tests -- --nocapture`, focused CLI runs for `std_process_basic`, `std_process_requires_import`, `std_process_control_old_core_path_rejected`, `no_std_core_import_available`, and `exit_code`, `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (356/356 e2e).

[DONE] /goal Align internal compiler/backend/CLI harness preludes with the `std:process` process-control split. — DONE: updated the broad auto-import strings used by sema checker tests, HIR lowering tests, backend tests, CLI inline run tests, and the REPL so `panic`/`panic_with` still come from `core:prelude` while `exit`/`abort` come from `std:process`. This prevents internal tests and REPL snippets from relying on the old rejected `core:prelude` process-control path. Tightened two exposed test assumptions: removed a duplicate `core:collections.List` import from a sema case now that duplicate imports are enforced, and made a HIR lowering test inspect only the user function body rather than stdlib module-local matches. Verification: `cargo test -p compiler sema::check::tests -- --nocapture` green (84/84), `cargo test -p compiler hir::lower_tests -- --nocapture` green (63/63), `cargo test -p backend -- --nocapture` green (223/223 plus doctests), `cargo test -p cli --test run exit_builtin_sets_process_code -- --nocapture`, `cargo test -p cli --test run repl_persists_state_and_recovers_from_errors -- --nocapture`, stale core-prelude process-control import scan clean except the intentional old-path negative case, and `git diff --check`.

[DONE] /goal Move atomic ordering to `core:sync:atomic` under the revised core/std criteria. — DONE: moved the Otter-authored atomic ordering source from `stdlib_src/std/sync_atomic.otter` to `stdlib_src/core/sync_atomic.otter`, changed the catalog/source manifest from `std:sync:atomic` to `core:sync:atomic`, and extended the core-substrate catalog guard to allow only this explicitly justified atomic core module. Updated e2e coverage so `core:sync:atomic` imports work, the near-empty-prelude import rule still applies, and the old `std:sync:atomic` path is rejected. Updated docs/18, docs/20, docs/24, docs/29, ROADMAP, and the older atomic-ordering goal note to describe atomics as compiler/runtime substrate under the new split. While running the full suite, fixed the surfaced `Deque<T>: Debug` rendering mismatch so debug output uses readable comma-space separators while `ToStr` remains compact. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (8/8), `cargo run -p cli -- run tests/cases/stdlib/core_sync_atomic_ordering_basic.otter`, direct compile-error diagnostics for `core_sync_atomic_requires_import.otter` and `core_sync_atomic_old_std_path_rejected.otter`, `cargo run -p cli -- run tests/cases/stdlib/std_collections_deque_basic.otter`, stale `std:sync:atomic` scan clean except the intentional old-path rejection and historical notes, and `cargo test -p cli --test suite -- --nocapture` green (356/356 e2e).

[DONE] /goal Refresh module/operator docs and no-std coverage after the atomic core move. — DONE: updated docs/17 to show `core:sync:atomic` in the core layer diagram, document it as an explicit-import core module, and distinguish boolean comparison `Ord` from atomic memory-ordering values. Updated docs/15 so the operator chapter matches the implemented `core:prelude.Ord` interface (`lt`/`le`/`gt`/`ge`) instead of the stale unimplemented `Ordering = Less | Equal | Greater` comparison enum. Expanded `no_std_core_import_available.otter` so a no-std project proves both `core:collections` and `core:sync:atomic` remain available while `std:*` is disabled. Verification: `cargo run -p cli -- run tests/cases/stdlib/no_std_core_import_available.otter`, `cargo run -p cli -- run tests/cases/stdlib/core_sync_atomic_ordering_basic.otter`, `cargo test -p compiler sema::symbols::tests::no_std_keeps_core_imports_available -- --nocapture`, and `cargo test -p cli --test suite -- --nocapture` green (356/356 e2e).

[DONE] /goal Align operator protocol docs/tests with the current core/prelude catalog surface. — DONE: clarified docs/15 and docs/17 so operator syntax is described as compiler-recognized core substrate over visible `extend` methods (`add`, `sub`, `eq`, `lt`, etc.) rather than importable arithmetic/bitwise `core:prelude` interfaces. The docs now keep true catalog exports (`Eq`, `Ord`, `Hash`, `Clone`, `Drop`, `ToStr`, iterator/future/try shapes, panic hooks) separate from non-exported protocol labels such as `Add`, and docs/18 now describes `List`/`Map` indexing as compiler-recognized collection syntax instead of `Index`/`IndexMut` interface impls. Renamed the generic-interface examples in docs/11 away from `Add` to avoid implying a stdlib operator trait. Updated the operator-overload e2e case to use a plain `extend V2 { add(...) }`, added a compile-error e2e proving `import { Add } from "core:prelude"` is rejected, and added a catalog unit guard that arithmetic/bitwise/index protocol labels are not `core:prelude` exports without a deliberate language-design change. Verification: `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), focused operator e2e run, `cargo test -p cli --test suite -- --nocapture` green (357/357 e2e), and `git diff --check`.

[DONE] /goal Refresh stdlib module indexes and implementation-kind examples against the current catalog. — DONE: updated the chapter 17 `std:*` module table so `std:time`, `std:fs`, `std:json`, `std:net:types`, `std:process`, `std:rand`, and `std:log` summaries match the implemented value layers, target-backed hooks, process-control split, parser helpers, and remaining planned work. Updated chapter 18 and chapter 29 implementation-kind examples so `RustBacked` and `Mixed` use concrete current stdlib surfaces (`std:io` markers, `core:prelude.panic`, `std:process.exit`/`abort`, `std:time.Instant/SystemTime`, `std:fs.File`, `std:rand.OsRng/ThreadRng`) rather than stale/vague examples. Verification: stale-text sweep for the old rows, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), and `git diff --check`.

[DONE] /goal Close positive stdlib-folder coverage gaps for importable core modules. — DONE: audited stdlib e2e coverage by module and added direct positive stdlib cases for `core:ffi` and `core:compiler`, complementing their existing import-enforcement cases and the deeper FFI/macro suites. `core_ffi_basic.otter` imports C ABI aliases plus `CString`/`Buffer` and exercises a small round-trip through the runtime-backed boundary helpers; `core_compiler_import_basic.otter` imports `MacroContext`, `ASTNode`, and `Span` as macro-authoring core substrate without invoking macro-host-only extern methods in ordinary runtime code. The stdlib folder now has positive import coverage for every importable `core:*` module while preserving separate behavior coverage in the domain suites. Verification: direct CLI runs for both new cases, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), `cargo test -p compiler sema::symbols::tests::stdlib_catalog_exports_materialize_in_builtin_views -- --nocapture`, `cargo test -p cli --test suite -- --nocapture` green (359/359 e2e), and `git diff --check`.

[DONE] /goal Tighten remaining stdlib roadmap wording after the process-control split. — DONE: updated ROADMAP's next-stdlib-slices wording so `std:process` correctly names provider-backed process spawning and child management as the remaining work after `exit`/`abort` moved to `std:process`. Updated docs/24's implementation-kind examples so mixed `std:fs.File` coverage is described as file helpers rather than only text helpers. Verification: stale process-control and file-helper wording search is clean; `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9); `git diff --check` clean.

[DONE] /goal Expand near-empty-prelude coverage for newer `std:fs` module-level helpers. — DONE: added `tests/cases/stdlib/std_fs_helpers_require_import.otter`, a compile-error e2e case that imports only `Path` and `Bytes`, then proves newer `std:fs` helper exports (`read_to_string`, `write_string`, `append_string`, binary `read`/`write`, `canonicalize`, native path conversion, `read_dir`, remove/rename/create_dir/create_dir_all) do not leak into scope without explicit named imports. This complements the existing `Path`, metadata/OpenOptions, and DirEntry import-enforcement cases. Verification: direct CLI run produced all expected unresolved-helper diagnostics; `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9); `cargo test -p cli --test suite -- --nocapture` green (360/360 e2e).

[DONE] /goal Add run-to-completion process execution for `std:process`. — DONE: added Rust-backed `lang_process_status` and `lang_process_output` hooks plus JIT symbol registration, using the existing tagged-string runtime boundary with length-prefixed command payloads and hex-encoded captured stdout/stderr. The Otter Fusion `std:process` layer now provides `Command.status(): ExitStatus | IoError` and `Command.output(): Output | IoError`, preserving snapshot args/env/cwd semantics, inheriting stdout/stderr for status, capturing byte output for output, and decoding launch/provider failures into ordinary `IoError` values. Live `Child` handles, streamed child stdio, and richer target-specific status details remain planned. Added runtime hook tests and `tests/cases/stdlib/std_process_exec_basic.otter` covering successful status, captured output, and missing-command errors. Updated docs/18, docs/24, docs/29, ROADMAP, and goals. Verification: `cargo test -p runtime process::tests -- --nocapture` green (4/4), `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), direct CLI run for `std_process_exec_basic.otter`, `rustfmt --check crates/runtime/src/process.rs crates/backend/src/lib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e).

[DONE] /goal Add explicit keyed hasher values to `std:hash`. — DONE: added pure Otter Fusion `KeyedHasher` and `keyed_hasher(seed)` to `std:hash`, sharing the deterministic stream mixer with `DefaultHasher` while recording the caller-provided seed as part of the value state. `KeyedHasher` implements the `Hasher` interface, convenience writes for signed integers/booleans/strings/`Hash` values, `seed()`, equality, clone, hash, stringification, and `std:fmt.Debug`. The stdlib catalog exports `KeyedHasher` and `keyed_hasher`; docs now distinguish implemented explicit keyed hasher values from still-planned keyed map/set construction and OS-seeded HashDoS-safe variants. Expanded `std_hash_basic.otter` and `std_hash_requires_import.otter`. Verification: direct CLI run for `std_hash_basic.otter`, expected compile-error diagnostics for `std_hash_requires_import.otter`, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e).

[DONE] /goal Add UTC/fixed-offset `SystemTime` conversion for `std:time`. — DONE: added pure Otter Fusion proleptic-Gregorian conversion helpers plus `SystemTime.to_datetime(tz): DateTime | TimeError` and `DateTime.to_system_time(): SystemTime | TimeError`. UTC and fixed-offset conversions round-trip through Unix-epoch nanoseconds, including pre-epoch timestamps; named timezone conversion returns `TimeError` until provider-backed timezone databases exist. Expanded `std_time_datetime_basic.otter` to cover Unix epoch, fixed-offset, pre-epoch, and named-zone error conversions, and expanded the near-empty-prelude datetime import case for `SystemTime`. Updated docs/18, docs/24, ROADMAP, and goals so UTC/fixed-offset conversion is implemented while local timezone lookup, named-zone conversion, timezone databases, and leap-second policy remain planned. Verification: direct CLI run for `std_time_datetime_basic.otter`, expected compile-error diagnostics for `std_time_datetime_requires_import.otter`, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e).

[DONE] /goal Add exact byte-buffer helpers for `std:rand`. — DONE: added pure Otter Fusion `fill_bytes_n(rng, buf, count): null` and `gen_bytes(rng, count): Bytes` over the existing `Rng` contract, plus `OsRng.try_fill_bytes_n(buf, count): null | RandomError` for fallible provider-backed entropy. The existing `Rng.fill_bytes` remains the one-chunk interface method; the new helpers generate exact-length byte buffers without changing the protocol. Exported the helper functions through the stdlib catalog, expanded `std_rand_basic.otter` for seeded/interface/OS-backed exact byte generation, expanded the near-empty-prelude rand import case for `gen_bytes`, and updated docs/18, docs/24, ROADMAP, and goals so random byte-buffer helpers are implemented while cryptographic-strength guarantees and richer statistical distributions remain planned. Verification: direct CLI run for `std_rand_basic.otter`, expected compile-error diagnostics for `std_rand_requires_import.otter`, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e).

[DONE] /goal Add scoped IPv6 zone identifiers and canonical hex rendering for `std:net:types`. — DONE: extended the pure Otter Fusion `IpAddr` value with an optional IPv6 zone id, added the explicit `ip_v6_scoped(...)` constructor, preserved scoped IDs through `parse_ip_v6` and bracketed IPv6 socket parsing, exposed `IpAddr.zone_id()`, and changed IPv6 rendering/debug/socket formatting to lowercase hex groups without leading zeroes. Invalid empty or nonportable zone ids now produce ordinary `ParseError` diagnostics. Updated the stdlib catalog, `std_net_types_basic.otter`, `std_net_types_requires_import.otter`, docs/17, docs/18, docs/24, ROADMAP, and goals so scoped zone IDs are implemented while broader URI/URL canonicalization, IDNA, DNS, sockets, and target-backed networking remain planned; later slices added UTF-8 percent component encode/decode helpers. Verification: direct CLI run for `std_net_types_basic.otter`, expected compile-error diagnostics for `std_net_types_requires_import.otter`, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), stale zone-wording scan clean, `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e).

[DONE] /goal Add bracketed IPv6 URL host parsing for `std:net:types`. — DONE: extended pure Otter Fusion `parse_url` so URL authorities may contain bracketed IPv6 literal hosts such as `https://[2001:db8::1]:8443/search` and scoped literal hosts such as `http://[fe80::1%en0]/`. Parsed IPv6 URL hosts are validated with the existing IPv6 parser and stored/rendered in bracketed lowercase-hex form, preserving zone IDs. Malformed bracketed URL hosts now surface ordinary positioned `ParseError` diagnostics for missing closing brackets, invalid host/port delimiters, and nested IPv6 parse failures. Expanded `std_net_types_basic.otter` and updated docs/17, docs/18, docs/24, ROADMAP, and goals while leaving broader URI/URL canonicalization, IDNA, DNS, sockets, and target-backed networking planned; later slices added UTF-8 percent component encode/decode helpers. Verification: direct CLI run for `std_net_types_basic.otter`, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e).

[DONE] /goal Add URI/URL accessors and immutable URL builders for `std:net:types`. — DONE: added `Uri.scheme/authority/path/query/fragment` accessors and `Url.scheme/user/host/port/path/query/fragment` accessors, with `Url.query()` returning a snapshot rather than the stored query map. Added immutable `Url.with_scheme`, `with_user`, `with_host`, `with_port`, `with_path`, `with_query`, and `with_fragment` builders that return new URL values and preserve query-map snapshot semantics for unchanged and replacement queries. Expanded `std_net_types_basic.otter` for URI accessors, URL accessors, accessor query snapshots, and builder query snapshots; updated docs/17, docs/18, docs/24, ROADMAP, and goals while leaving broader URI/URL canonicalization, IDNA, DNS, sockets, and target-backed networking planned; later slices added UTF-8 percent component encode/decode helpers. Verification: direct CLI run for `std_net_types_basic.otter`, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e).

[DONE] /goal Add immutable array/object builders for `std:json`. — DONE: added pure Otter Fusion `Json.append(value)`, `Json.set_at(index, value)`, `Json.with_key(key, value)`, and `Json.without_key(key)` so JSON arrays and objects can be transformed without exposing the opaque internal union or mutating extracted `List`/`Map` snapshots. Builders deep-clone inserted values, preserve existing snapshot semantics, return a cloned unchanged value for non-container receivers, and keep out-of-range `set_at` as a no-op clone. Expanded `std_json_basic.otter` for object replacement/removal, array append/set, non-container no-op behavior, and nested snapshot independence; updated docs/17, docs/18, docs/24, ROADMAP, and goals while leaving parsing and stricter canonicalization as package/follow-up work. Verification: direct CLI run for `std_json_basic.otter`, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e).

[DONE] /goal Expand `std:collections.Deque` construction and bulk operation surface. — DONE: added pure Otter Fusion `deque_from_list<T>(List<T>): Deque<T>`, `Deque.extend_front(List<T>)`, `Deque.extend_back(List<T>)`, and `Deque.contains(value)` when `T: Eq`, keeping `Deque` as an ordinary explicit-import `std:collections` API rather than compiler substrate. Bulk front extension preserves list iteration order by inserting the whole list before the existing front. Exported `deque_from_list` through the stdlib catalog, expanded positive Deque e2e coverage for construction, bulk operations, and membership testing, expanded near-empty-prelude import enforcement for the new helper, and updated docs/17, docs/18, docs/24, ROADMAP, and goals. Verification: direct CLI run for `std_collections_deque_basic.otter`, expected unresolved-name diagnostics for `std_collections_deque_requires_import.otter`, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e).

[DONE] /goal Normalize `std:http.Headers` names case-insensitively. — DONE: added a pure Otter Fusion ASCII lowercase normalization helper for `std:http` header keys and routed `Headers.get`, `get_all`, `set`, `append`, `remove`, `contains`, `HeaderEntry.new`, and `header_entry` through it. Header names are now normalized on insertion, lookup, removal, snapshots, equality, hashing, and debug rendering, so request/response header builders also operate case-insensitively while preserving deterministic lowercase snapshots. Expanded `std_http_basic.otter` for mixed-case set/append/get/get_all/remove/HeaderEntry/request-builder/response-builder behavior, expanded `std_http_requires_import.otter` for header helper import enforcement, and updated docs/17, docs/18, docs/24, ROADMAP, and goals so only canonical wire rendering/parsing and HTTP clients/servers remain planned. Verification: direct CLI run for `std_http_basic.otter`, expected unresolved-name diagnostics for `std_http_requires_import.otter`, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), stale exact-name/case-insensitive-planned drift search clean except historical notes, `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e).

[DONE] /goal Add UTF-8 percent component helpers for `std:net:types`. — DONE: added pure Otter Fusion `percent_encode_component(text): str` and `percent_decode_component(text): str | ParseError` to the portable `std:net:types` value layer. Encoding preserves unreserved ASCII characters and emits uppercase `%HH` UTF-8 byte escapes for all other Unicode scalars. Decoding accepts raw non-escaped characters, decodes consecutive `%HH` byte runs as strict UTF-8, and reports ordinary `ParseError` values for truncated escapes, non-hex digits, invalid leading/continuation bytes, truncated UTF-8 sequences, overlong encodings, surrogate scalars, and out-of-range code points. Exported both helpers through the stdlib catalog, expanded `std_net_types_basic.otter` for ASCII, reserved, and non-ASCII round trips plus malformed decode errors, expanded `std_net_types_requires_import.otter` for near-empty-prelude import enforcement, and updated docs/17, docs/18, docs/24, ROADMAP, and goals so IDNA, DNS, sockets, and stricter URL canonicalization remain planned while percent component helpers are implemented. Verification: direct CLI run for `std_net_types_basic.otter`, expected unresolved-name diagnostics for `std_net_types_requires_import.otter`, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), stale percent-planned drift search clean, `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e).

[DONE] /goal Expand standard status helpers for `std:http.Status`. — DONE: added pure Otter Fusion `Status` constructors and default reason phrases for common informational, success, redirection, client-error, and server-error status codes: 100, 101, 200, 201, 202, 204, 301, 302, 303, 304, 307, 308, 400, 401, 403, 404, 405, 409, 418, 422, 429, and 500 through 504. Added `Status.is_informational()` so the value layer now exposes all five HTTP status class predicates while preserving unknown-code rendering as the numeric code unless an explicit reason is supplied. Expanded `std_http_basic.otter` for constructors, reason defaults, status-class predicates, explicit custom reasons, and unknown-code fallback; updated docs/17, docs/18, docs/24, ROADMAP, and goals while leaving HTTP wire parsing/rendering and clients/servers planned. Verification: direct CLI run for `std_http_basic.otter`, expected unresolved-name diagnostics for `std_http_requires_import.otter`, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e).

[DONE] /goal Add direct equality semantics for `std:http.Method` and `HttpVersion`. — DONE: added pure Otter Fusion `Eq` impls for the `Method` and `HttpVersion` union values, comparing their rendered protocol strings. This matches the existing request/response equality behavior and makes standalone method/version values properly comparable through both `.eq(...)` and overloaded `==`, while preserving existing clone/hash/string/debug behavior. Expanded `std_http_basic.otter` for method/version `.eq(...)` and `==` comparisons, and updated docs/17, docs/18, docs/24, ROADMAP, and goals. Verification: direct CLI run for `std_http_basic.otter`, expected unresolved-name diagnostics for `std_http_requires_import.otter`, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e).

[DONE] /goal Fix field-order-independent equality and hashing for `std:log.Record`. — DONE: added pure Otter Fusion field-map comparison and order-independent field hashing helpers inside `std:log`, then changed `Record.eq` and `Record.hash` to compare structured fields by key/value membership rather than by `field_string()` rendering. This keeps compact log rendering unchanged while making record value semantics independent of field insertion/iteration order. Expanded `std_log_basic.otter` with two records containing the same fields inserted in different orders and verified equality/hash agreement, updated docs/17, docs/18, docs/24, ROADMAP, and goals, and kept replaceable global logger backends as planned provider/runtime work. Verification: direct CLI run for `std_log_basic.otter`, expected unresolved-name diagnostics for `std_log_requires_import.otter`, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e).

[DONE] /goal Expand near-empty-prelude coverage for newer `std:log` helpers. — DONE: widened `std_log_requires_import.otter` so the negative e2e case now proves `Level`, `Record`, `record`, `empty_fields`, `log_record`, `info`, and `warn_with` require explicit `std:log` imports, while importing only unrelated support types from `core:collections` and `std:time` to keep diagnostics focused. Verification: direct compile-error CLI run for `std_log_requires_import.otter` produced the expected unresolved `std:log` diagnostics, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e).

[DONE] /goal Expand near-empty-prelude coverage for newer `std:fs` value and handle types. — DONE: widened `std_fs_metadata_requires_import.otter` so the negative e2e case now proves `File`, `FileKind`, `DirEntry`, `DirEntries`, `Permissions`, `Metadata`, `OpenOptions`, `File.open`, `regular_file`, `dir_entry`, `permissions`, `metadata`, and `open_options` require explicit `std:fs` imports, while importing only `Path` to keep the missing-name diagnostics focused on omitted fs exports. Verification: direct compile-error CLI run for `std_fs_metadata_requires_import.otter` produced the expected unresolved `std:fs` diagnostics, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e).

[DONE] /goal Expand near-empty-prelude coverage for newer `std:process` value and helper exports. — DONE: widened `std_process_requires_import.otter` so the negative e2e case now proves `Command`, `ExitStatus`, `Output`, their static constructors, `command`, `exit_status`, `output`, `args`, `env`, `env_all`, `set_env`, `exit`, and `abort` require explicit `std:process` imports, while importing only `std:bytes.Bytes` to keep diagnostics focused on omitted process exports. Verification: direct compile-error CLI run for `std_process_requires_import.otter` produced the expected unresolved `std:process` diagnostics, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e).

[DONE] /goal Expand near-empty-prelude coverage for newer `std:rand` RNG types and helpers. — DONE: widened `std_rand_requires_import.otter` so the negative e2e case now proves `Rng`, `RandomError`, `OsRng`, `SeededRng`, `ThreadRng`, `SeededRng.from_seed`, `random_error`, `os_rng`, `thread_rng`, `gen_range_i64`, `gen_range_u64`, `gen_f64`, `gen_range_f64`, `gen_bool`, `fill_bytes_n`, `gen_bytes`, `choose`, and `shuffle` require explicit `std:rand` imports, while importing only collections and bytes support types to keep diagnostics focused on omitted rand exports. Verification: direct compile-error CLI run for `std_rand_requires_import.otter` produced the expected unresolved `std:rand` diagnostics, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e).

[DONE] /goal Expand near-empty-prelude coverage for `std:hash` interfaces and helper exports. — DONE: widened `std_hash_requires_import.otter` so the negative e2e case now proves `Hasher`, `DefaultHasher`, `KeyedHasher`, their static constructors, `hash_value`, `combine_hash`, and `keyed_hasher` require explicit `std:hash` imports. Verification: direct compile-error CLI run for `std_hash_requires_import.otter` produced the expected unresolved `std:hash` diagnostics, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e).

[DONE] /goal Expand near-empty-prelude coverage for broader `std:io` contracts, values, streams, and helpers. — DONE: widened `std_io_requires_import.otter` so the negative e2e case now proves `Reader`, `Writer`, `Seeker`, `SeekFrom`, `Start`, `Current`, `End`, `IoError`, `IoErrorKind`, `OtherIo`, `Stdin`, `Stdout`, `Stderr`, `BufReader`, `BufWriter`, `buf_reader`, `buf_writer`, `stdin`, `stdout`, `stderr`, `print`, `println`, `eprint`, and `eprintln` require explicit `std:io` imports. Verification: direct compile-error CLI run for `std_io_requires_import.otter` produced the expected unresolved `std:io` diagnostics, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e).

[DONE] /goal Expand positive edge coverage for `std:bytes` slices and cursor helpers. — DONE: widened `std_bytes_basic.otter` so the positive e2e case now locks down negative-start, oversized-end, reversed, and out-of-range `Bytes.slice` behavior, plus empty cursor state, `BytesCursor.len`, null reads on empty cursors, `rewind`, oversized `read_bytes`, and cursor position after overread without weakening the existing reader/seeker coverage. Verification: direct CLI run for `std_bytes_basic.otter` produced the expected output, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e).

[DONE] /goal Expand positive edge coverage for `std:json` typed accessors and pretty fallback. — DONE: widened `std_json_basic.otter` so the positive e2e case now locks down wrong-type object accessors, missing-key null checks, out-of-range and negative array typed accessors, scalar `get_is_null` behavior, and `pretty(0)`/negative-indent fallback to compact rendering. Verification: direct CLI run for `std_json_basic.otter` produced the expected output, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e).

[DONE] /goal Expand `std:http` import-gating and edge coverage. — DONE: widened `std_http_requires_import.otter` so the negative e2e case now proves the full exported HTTP value surface requires explicit `std:http` imports, including method/version union variants, status/header/request/response types, constructor helpers, and request/response builders. Expanded `std_http_basic.otter` with positive edge checks for less-common default status reasons, explicit custom status reasons, unknown-code reason fallback, empty/missing header snapshots, and `Headers.set` replacement after mixed-case appends. Verification: direct CLI run for `std_http_basic.otter` produced the expected output, direct compile-error run for `std_http_requires_import.otter` produced the expected unresolved `std:http` diagnostics, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e).

[DONE] /goal Expand `std:net:types` import-gating and URL builder edge coverage. — DONE: widened `std_net_types_requires_import.otter` so the negative e2e case now proves the full exported portable network identifier surface requires explicit `std:net:types` imports, including `IpAddr`, `SocketAddr`, `Uri`, `Url`, `ParseError`, constructors, parsers, socket helpers, and percent component helpers. Expanded `std_net_types_basic.otter` with immutable URL builder coverage for clearing nullable user, port, and fragment fields while preserving query snapshots. Verification: direct CLI run for `std_net_types_basic.otter` produced the expected output, direct compile-error run for `std_net_types_requires_import.otter` produced the expected unresolved `std:net:types` diagnostics, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e).

[DONE] /goal Expand near-empty-prelude coverage for the full `std:time` value surface. — DONE: widened `std_time_datetime_requires_import.otter` and `std_time_instant_requires_import.otter` so the negative e2e cases now prove `std:time` clock, timezone, datetime, and error exports require explicit imports: `Instant`, `SystemTime`, `TimeZone`, `Utc`, `FixedOffset`, `NamedTimeZone`, `DateTime`, `TimeError`, static constructors, `utc`, `fixed_offset`, `named_timezone`, `time_error`, `date_time`, and `parse_iso8601`. Verification: direct compile-error runs for the updated time import cases produced the expected unresolved `std:time` diagnostics, direct CLI run for `std_time_datetime_basic.otter` produced the expected output, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e).

[DONE] /goal Expand collection import-gating coverage across `core:collections` and `std:collections`. — DONE: widened `core_collections_set_requires_import.otter` so the negative e2e case now proves every `core:collections` catalog export (`List`, `Map`, `Set`, and `Entry`) requires an explicit import, including constructor/type-name lookups. Widened `std_collections_deque_requires_import.otter` so the negative case now covers `Deque`, `Deque.new`, `deque`, and `deque_from_list`, reinforcing that `Deque` remains ordinary `std:collections` API while compiler-recognized collection layouts stay in `core:collections`. Verification: direct compile-error runs for both updated collection import cases produced the expected unresolved diagnostics, direct CLI runs for `core_collections_set_basic.otter` and `std_collections_deque_basic.otter` produced the expected output, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e).

[DONE] /goal Expand import-gating coverage for foundational `std:error` and `std:fmt` contracts. — DONE: widened `std_error_requires_import.otter` so the negative e2e case now proves all `std:error` catalog exports require explicit imports: `Error`, `Annotated`, and `with_context`. Widened `std_fmt_requires_import.otter` so the negative case now proves all `std:fmt` contract/value exports require explicit imports: `Display`, `Debug`, `FmtSink`, and `FmtError`. Verification: direct compile-error runs for both updated import cases produced the expected unresolved diagnostics, direct CLI runs for `std_error_with_context.otter` and `std_fmt_contracts.otter` produced the expected output, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e).

[DONE] /goal Expand async tier import-gating coverage for `core:async` and `std:async`. — DONE: widened `core_async_requires_import.otter` so the negative e2e case now proves every `core:async` syntax-protocol export requires an explicit import: `Future`, `Ready`, `Pending`, `Context`, and `AsyncIterator`. Widened `std_async_runtime_requires_import.otter` so the negative case now covers `TimedOut`, `yield_now`, `sleep`, and `timeout`, reinforcing that runtime helpers live in `std:async` while compiler-recognized protocol shapes live in `core:async`. Verification: direct compile-error runs for both updated async import cases produced the expected unresolved diagnostics, direct CLI runs for `core_async_contracts.otter` and `std_async_runtime_basic.otter` produced the expected output, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e).

[DONE] /goal Expand import-gating coverage for remaining core substrate modules. — DONE: widened `core_ffi_requires_import.otter` so the negative e2e case now proves every `core:ffi` catalog export requires an explicit import, including all C ABI aliases plus `Foreign`, `CString`, `CStr`, and `Buffer`. Widened `core_compiler_requires_import.otter` so the macro-authoring substrate exports `MacroContext`, `ASTNode`, and `Span` all require explicit `core:compiler` imports. Widened `core_sync_atomic_requires_import.otter` so the `Ordering` union, all ordering variant structs, and all ordering constructor helpers require explicit `core:sync:atomic` imports. Verification: direct compile-error runs for all three updated cases produced the expected unresolved diagnostics, direct CLI runs for `core_ffi_basic.otter`, `core_compiler_import_basic.otter`, and `core_sync_atomic_ordering_basic.otter` produced the expected output, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e).

[DONE] /goal Expand import-gating coverage for `std:thread` and `std:sync` runtime-handle modules. — DONE: widened `std_thread_requires_import.otter` so the negative e2e case now proves every `std:thread` catalog export requires an explicit import: `Thread`, `JoinHandle`, `Joined`, and `Panicked`. Widened `std_sync_requires_import.otter` so the negative case now proves every `std:sync` runtime handle, marker value, and constructor helper requires an explicit import: `Sender`, `Receiver`, `ChannelClosed`, `Shared`, `LockBusy`, `MpmcSender`, `MpmcReceiver`, `channel`, `channel_bounded`, `channel_mpmc`, and `channel_mpmc_bounded`. Verification: direct compile-error runs for both updated cases produced the expected unresolved diagnostics, direct CLI runs for `std_thread_basic.otter` and `std_sync_basic.otter` produced the expected output, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e).

[DONE] /goal Expand near-empty-prelude coverage for every `core:prelude` catalog export. — DONE: widened `core_prelude_requires_import.otter` so the negative e2e case now proves all named `core:prelude` language protocols and panic hooks require explicit imports: `Iterator`, `Item`, `Done`, `FromResidual`, `Try`, `Clone`, `Drop`, `Eq`, `Ord`, `ToStr`, `Hash`, `Future`, `Ready`, `Pending`, `Context`, `panic`, and `panic_with`. This locks down the rule that the prelude is compiler-known substrate, not an implicit source-level namespace. Verification: direct compile-error run for the updated case produced the expected unresolved diagnostics, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e).

[DONE] /goal Expand `std:fs` metadata/value import-gating coverage for file-kind variants and helpers. — DONE: widened `std_fs_metadata_requires_import.otter` so the negative e2e case now proves the full file-kind value surface requires explicit `std:fs` imports, including variant structs `RegularFile`, `Directory`, `Symlink`, and `OtherKind`, plus constructor helpers `directory`, `symlink`, and `other_kind` alongside existing `File`, `FileKind`, `DirEntry`, `DirEntries`, `Permissions`, `Metadata`, `OpenOptions`, `regular_file`, `dir_entry`, `permissions`, `metadata`, and `open_options` checks. The case now imports `core:collections.List` explicitly so missing-name diagnostics stay focused on omitted fs exports. Verification: direct compile-error run for the updated case produced the expected unresolved diagnostics, direct CLI runs for `std_fs_path_basic.otter` and `std_fs_text_basic.otter` produced the expected output, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e).

[DONE] /goal Expand import-gating coverage for the full `std:log` exported surface. — DONE: widened `std_log_requires_import.otter` so the negative e2e case now proves every `std:log` catalog export requires an explicit import: `Level`, level variant structs `LogTrace`, `LogDebug`, `Info`, `Warn`, `LogError`, `Record`, `LoggerAlreadySet`, all level constructors, `record`, `empty_fields`, `log_record`, plain log helpers `trace`/`debug`/`info`/`warn`/`error`, and structured helpers `info_with`, `trace_with`, `debug_with`, `warn_with`, and `error_with`. Verification: direct compile-error run for the updated case produced the expected unresolved diagnostics, direct CLI run for `std_log_basic.otter` produced the expected output, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e).

[DONE] /goal Expand import-gating coverage for the full `std:bytes` exported surface. — DONE: widened `std_bytes_requires_import.otter` so the negative e2e case now proves every `std:bytes` catalog export requires an explicit import: `Bytes`, `BytesCursor`, and `Utf8Error`. Verification: direct compile-error run for the updated case produced the expected unresolved diagnostics, direct CLI run for `std_bytes_basic.otter` produced the expected output, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e).

[DONE] /goal Expand import-gating coverage for the full `std:json` exported surface. — DONE: widened `std_json_requires_import.otter` so the negative e2e case now proves every `std:json` catalog export requires an explicit import: `Json`, `json_null`, `json_bool`, `json_number`, `json_string`, `json_array`, and `json_object`. The case imports only support collections so diagnostics stay focused on omitted `std:json` exports. Verification: direct compile-error run for the updated case produced the expected unresolved diagnostics, direct CLI run for `std_json_basic.otter` produced the expected output, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e).

[DONE] /goal Expand `std:io` import-gating coverage for all exported `IoErrorKind` variants. — DONE: widened `std_io_requires_import.otter` so the negative e2e case now proves every exported IO error-kind variant requires an explicit `std:io` import: `NotFound`, `PermissionDenied`, `ConnectionRefused`, `IoTimedOut`, `UnexpectedEof`, `Interrupted`, and `OtherIo`, alongside the existing coverage for IO contracts, `SeekFrom` variants, `IoError`, stream handles, buffered adapters, and print/stderr helpers. Verification: direct compile-error run for the updated case produced the expected unresolved diagnostics, direct CLI runs for `std_io_contracts.otter`, `std_io_buffered_basic.otter`, and `std_io_stderr_basic.otter` produced the expected output, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e).

[DONE] /goal Expand static-constructor import-gating coverage for selected stdlib value types. — DONE: tightened `std_fs_path_requires_import.otter`, `std_fs_dir_entry_requires_import.otter`, and `std_time_duration_requires_import.otter` so the negative e2e cases now prove exported value types require explicit imports in both type positions and static-constructor/value positions: `Path`/`Path.new`, `DirEntry`/`DirEntry.new`, and `Duration`/`Duration.from_secs`, while preserving the existing `sleep` helper check. The `DirEntry` case now imports `Path` and `regular_file` explicitly so diagnostics stay focused on the omitted `DirEntry` export. Verification: direct compile-error runs for all three updated cases produced the expected unresolved diagnostics, direct CLI runs for `std_fs_path_basic.otter` and `std_time_duration_basic.otter` produced the expected output, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (9/9), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e).

[DONE] /goal Add a catalog invariant proving stdlib exports have near-empty-prelude import-gating coverage. — DONE: added `sema::stdlib::tests::catalog_exports_have_import_gating_diagnostics`, which scans the stdlib `*_requires_import.otter` / `*_require_import.otter` negative e2e cases via `include_str!`, extracts backtick-delimited unresolved-name expectations from `//@ stderr:` directives, and asserts every `TOOLCHAIN_MODULES` export is represented. This turns the repeated catalog/export coverage audit into a persistent compiler unit-test guard: future `core:*`/`std:*` exports must either get explicit import-gating diagnostics or the catalog test fails. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (10/10), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e; confirmed via `/tmp/otter_e2e.log` after a heartbeat wrapper kept the long suite observable).

[DONE] /goal Document the stdlib catalog-to-import-gating invariant. — DONE: updated `docs/29-extending-stdlib.html` so the required stdlib test matrix states every catalog export must appear in focused require-import negative e2e diagnostics, with catalog tests scanning those diagnostics. Updated ROADMAP's stdlib foundation/provider-invariant notes to record the same persistent guardrail. Verification: `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (10/10), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e, heartbeat wrapper summary).

[DONE] /goal Add uniform index distribution helpers for `std:rand`. — DONE: added pure Otter Fusion `gen_index(rng, len): i64 | null` and `choose_index<T>(rng, list): i64 | null` on top of the existing `Rng` contract. Both helpers return `null` for non-positive lengths or empty lists, and `choose` now delegates through `choose_index` so element and index sampling share the same behavior. Exported the helpers through the stdlib catalog, expanded `std_rand_basic.otter` and `std_rand_requires_import.otter`, and updated docs/18, docs/24, ROADMAP, and goals. Verification: direct CLI run for `std_rand_basic.otter` produced the expected output, direct compile-error run for `std_rand_requires_import.otter` produced the expected unresolved `std:rand` diagnostics, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (10/10), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e, heartbeat wrapper summary).

[DONE] /goal Add an interface-level `std:hash.write_hash` adapter. — DONE: added pure Otter Fusion `write_hash<T: Hash>(hasher: Hasher, value: T): null`, letting generic code feed any `Hash` value into any hasher interface object rather than relying only on concrete `DefaultHasher.write_hash` / `KeyedHasher.write_hash` methods. Exported the helper through the stdlib catalog, expanded `std_hash_basic.otter` and `std_hash_requires_import.otter`, and updated docs/18, docs/24, ROADMAP, and goals so hasher-writing `Hash` is implemented while keyed map/set construction and OS-seeded HashDoS-safe hashers remain planned. Verification: direct CLI run for `std_hash_basic.otter` produced the expected output, direct compile-error run for `std_hash_requires_import.otter` produced the expected unresolved `std:hash` diagnostics, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (10/10), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e, heartbeat wrapper summary).

[DONE] /goal Add direct indexed reads for `std:collections.Deque`. — DONE: added pure Otter Fusion `Deque.get(index): T | null`, reading the deque's logical zero-based order across the reversed front list and forward back list without forcing callers through `to_list()`. Negative and out-of-range indexes return `null`, matching the collection accessor style used elsewhere. Expanded `std_collections_deque_basic.otter` for front-side, middle, back-side, negative, and past-end reads, and updated docs/18, docs/24, ROADMAP, and goals. Verification: direct CLI run for `std_collections_deque_basic.otter` produced the expected output, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (10/10), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e, heartbeat wrapper summary).

[DONE] /goal Add direct append and clear helpers for `std:bytes.Bytes`. — DONE: added pure Otter Fusion `Bytes.append(other): null` and `Bytes.clear(): null` as ordinary managed byte-container operations. `append` snapshots the input before appending, so `b.append(b)` doubles the current contents once rather than iterating over newly appended bytes; `clear` empties the backing list. Expanded `std_bytes_basic.otter` for normal append, self-append snapshot behavior, and clearing; updated docs/18, docs/24, ROADMAP, and goals. Verification: direct CLI run for `std_bytes_basic.otter` produced the expected output, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (10/10), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e, heartbeat wrapper summary).

[DONE] /goal Add portable severity ordering helpers for `std:log.Level`. — DONE: added pure Otter Fusion `Level.rank(): i64` and `Level.is_at_least(min): bool`, ordering trace/debug/info/warn/error as `0..4` so filtering logic can be written before replaceable logger backends exist. Expanded `std_log_basic.otter` for all ranks and representative threshold checks, and updated docs/18, docs/24, ROADMAP, and goals. Verification: direct CLI run for `std_log_basic.otter` produced the expected output, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (10/10), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e, heartbeat wrapper summary).

[DONE] /goal Add direct mutation helpers for `std:bytes.Bytes`. — DONE: added pure Otter Fusion `Bytes.set(index, byte): bool`, `Bytes.pop(): u8 | null`, and `Bytes.truncate(len): null`. `set` rejects negative and out-of-range indexes without mutating, `pop` removes the final byte or returns `null` on an empty buffer, and `truncate` shrinks from the end, clears for non-positive lengths, and leaves larger target lengths unchanged. Expanded `std_bytes_basic.otter` for successful/out-of-range set, pop/empty pop, shorter/larger/zero truncation, and updated docs/18, docs/24, ROADMAP, and goals. Verification: direct CLI run for `std_bytes_basic.otter` produced the expected output, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (10/10), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e, heartbeat wrapper summary).

[DONE] /goal Add direct indexed mutation for `std:collections.Deque`. — DONE: added pure Otter Fusion `Deque.set(index, value): bool`, mutating the same logical zero-based order as `Deque.get(index)` across the reversed front list and forward back list. Negative and out-of-range indexes return `false` without mutation. Expanded `std_collections_deque_basic.otter` for front-side, middle, back-side, negative, and past-end writes, and updated docs/18, docs/24, ROADMAP, and goals. Verification: direct CLI run for `std_collections_deque_basic.otter` produced the expected output, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (10/10), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e, heartbeat wrapper summary).

[DONE] /goal Add snapshot and membership-predicate helpers for `core:collections.Set`. — DONE: added pure Otter Fusion `Set.to_list(): List<T>`, `Set.insert_all(other): null`, `Set.is_subset(other): bool`, and `Set.is_superset(other): bool` on the existing unique-list-backed `core:collections.Set<T>` surface. `to_list` snapshots the current iteration order, `insert_all` mutates by inserting each member from another set while preserving uniqueness, and the subset/superset helpers compare membership. Expanded `core_collections_set_basic.otter` for list snapshots, duplicate-safe bulk insertion, true/false subset checks, and true/false superset checks, and updated docs/18, docs/24, ROADMAP, and goals. Verification: direct CLI run for `core_collections_set_basic.otter` produced the expected output, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (10/10), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e, heartbeat wrapper summary).

[DONE] /goal Add compact stringification for `core:collections.Set`. — DONE: added pure Otter Fusion `extend<T: Eq + ToStr> Set<T>: ToStr`, rendering members in the set's current iteration order as `Set{...}`. Expanded `core_collections_set_basic.otter` to cover direct `to_str()` on a primitive set, and updated docs/18, docs/24, ROADMAP, and goals so Set value semantics include compact stringification alongside Eq/Clone/Hash and std:fmt Debug. Verification: direct CLI run for `core_collections_set_basic.otter` produced the expected output, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (10/10), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e, heartbeat wrapper summary).

[DONE] /goal Add object/shape inspection helpers for `std:json`. — DONE: added pure Otter Fusion `Json.is_array()`, `Json.is_object()`, `Json.len()`, `Json.contains_key(key)`, `Json.keys()`, and `Json.values()`. Arrays and objects report their size while scalars report `0`; object membership handles JSON null values correctly; `keys()` and `values()` return snapshots, and `values()` deep-clones nested JSON values so callers cannot alias an existing JSON tree. Expanded `std_json_basic.otter` for shape predicates, array/object/scalar lengths, key membership including JSON null and non-object receivers, deterministic key/value snapshots, and snapshot mutation isolation. Updated docs/18, docs/24, ROADMAP, and goals. Verification: direct CLI run for `std_json_basic.otter` produced the expected output, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (10/10), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e, heartbeat wrapper summary).

[DONE] /goal Add immutable URL query-entry helpers for `std:net:types`. — DONE: added pure Otter Fusion `Url.query_get(key): str | null`, `Url.with_query_param(key, value): Url`, `Url.without_query_param(key): Url`, and `Url.clear_query(): Url`. These helpers let callers inspect and update individual query entries without exposing the internal query map for mutation; all builders preserve the existing snapshot semantics and return new `Url` values, while missing removals are no-op value rebuilds. Expanded `std_net_types_basic.otter` for direct lookup, key replacement, new-key insertion, missing lookup/removal, clearing the query map, deterministic rendering, and original URL preservation. Updated docs/18, docs/24, ROADMAP, and goals. Verification: direct CLI run for `std_net_types_basic.otter` produced the expected output, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (10/10), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e, heartbeat wrapper summary).

[DONE] /goal Add header collection state helpers for `std:http.Headers`. — DONE: added pure Otter Fusion `Headers.is_empty(): bool`, `Headers.value_count(): i64`, and `Headers.clear(): null`. `size()` continues to count normalized header names, while `value_count()` counts flattened header values across repeated names; `clear()` replaces the internal normalized map and preserves existing snapshot lists returned by `get_all`, `entries`, or `names`. Expanded `std_http_basic.otter` for empty headers, value counts after set/replace, clear behavior, and snapshot preservation across clear. Updated docs/18, docs/24, ROADMAP, and goals. Verification: direct CLI run for `std_http_basic.otter` produced the expected output, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (10/10), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e, heartbeat wrapper summary).

[DONE] /goal Add direct structured-field inspection helpers for `std:log.Record`. — DONE: added pure Otter Fusion `Record.field(key): str | null`, `Record.has_field(key): bool`, and `Record.field_count(): i64`, so callers can inspect structured log fields without cloning the full field map through `fields()`. Existing builders and field-map accessors still snapshot maps to preserve value semantics. Expanded `std_log_basic.otter` for direct lookup, missing-field null/predicate behavior, field counts after add/remove/clear, and original-record preservation. Updated docs/18, docs/24, ROADMAP, and goals. Verification: direct CLI run for `std_log_basic.otter` produced the expected output, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (10/10), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e, heartbeat wrapper summary).

[DONE] /goal Add direct command inspection helpers for `std:process.Command`. — DONE: added pure Otter Fusion `Command.arg_count(): i64`, `Command.env_var_value(key): str | null`, `Command.has_env_var(key): bool`, and `Command.env_count(): i64`, so callers can inspect argument count and explicit command environment entries without cloning `args()` or `env()`. Inherited environments report no explicit command entries, preserving the existing `env == null` inheritance contract. Expanded `std_process_basic.otter` for direct arg/env inspection, missing env lookup, cleared env counts, inherited env behavior, and builder snapshot preservation. Updated docs/18, docs/24, ROADMAP, and goals. Verification: direct CLI run for `std_process_basic.otter` produced the expected output, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (10/10), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e, heartbeat wrapper summary).

[DONE] /goal Add subsecond component helpers for `std:time.Duration`. — DONE: added pure Otter Fusion `Duration.subsec_millis(): i64`, `Duration.subsec_micros(): i64`, and `Duration.subsec_nanos(): i64`, returning the absolute fractional remainder within the current second so signed durations expose positive component values for formatting. Expanded `std_time_duration_basic.otter` for positive, negative, and zero durations. Updated docs/18, docs/24, ROADMAP, and goals. Verification: direct CLI run for `std_time_duration_basic.otter` produced the expected output, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (10/10), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e, heartbeat wrapper summary).

[DONE] /goal Add lexical state/count helpers for `std:fs.Path`. — DONE: added pure Otter Fusion `Path.is_empty(): bool`, `Path.is_root(): bool`, and `Path.component_count(): i64`. `is_root` and `component_count` use the same lexical normalization/component semantics as the existing prefix/suffix helpers, so root and empty paths have zero components while relative `..` components are preserved. Expanded `std_fs_path_basic.otter` for empty paths, root normalization, root predicates, and normalized component counts. Updated docs/18, docs/24, ROADMAP, and goals. Verification: direct CLI run for `std_fs_path_basic.otter` produced the expected output, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (10/10), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e, heartbeat wrapper summary).

[DONE] /goal Add bytewise prefix/suffix helpers for `std:bytes.Bytes`. — DONE: added pure Otter Fusion `Bytes.starts_with(prefix): bool` and `Bytes.ends_with(suffix): bool`. The helpers compare raw bytes directly without UTF-8 decoding, treat empty affixes as matching, and return `false` when the affix is longer than the receiver. Expanded `std_bytes_basic.otter` for matching prefixes/suffixes, empty affixes, mismatches, and too-long affixes. Updated docs/18, docs/24, ROADMAP, and goals. Verification: direct CLI run for `std_bytes_basic.otter` produced the expected output, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (10/10), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e, heartbeat wrapper summary).

[DONE] /goal Add disjoint membership checks for `core:collections.Set`. — DONE: added pure Otter Fusion `Set.is_disjoint(other): bool` on the existing unique-list-backed `core:collections.Set<T>` surface, returning true only when the two sets share no members. Expanded `core_collections_set_basic.otter` for true and false disjoint checks and updated docs/18, docs/24, ROADMAP, and goals so the Set membership predicates include subset, superset, and disjoint checks. Verification: direct CLI run for `core_collections_set_basic.otter` produced the expected output including `disjoint=true/false`, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (10/10), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e, heartbeat wrapper summary).

[DONE] /goal Add scalar shape predicates for `std:json.Json`. — DONE: added pure Otter Fusion `Json.is_bool(): bool`, `Json.is_number(): bool`, and `Json.is_str(): bool`, completing the direct scalar/container predicate family alongside `is_null`, `is_array`, and `is_object`. Expanded `std_json_basic.otter` for true scalar checks and false cross-shape checks, and updated docs/18, docs/24, ROADMAP, and goals so JSON inspection describes scalar/container shape predicates. Verification: direct CLI run for `std_json_basic.otter` produced the expected output including `scalar-shape=true/true/true/false/false`, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (10/10), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e, heartbeat wrapper summary).

[DONE] /goal Add immutable reason builders for `std:http.Status`. — DONE: added pure Otter Fusion `Status.with_reason(reason): Status` and `Status.without_reason(): Status`, so callers can replace or clear an explicit reason phrase without going back through the module-level `status(code, reason)` constructor. Expanded `std_http_basic.otter` for explicit reason replacement, clearing back to default reason rendering, reason access, and original status preservation. Updated docs/18, docs/24, ROADMAP, and goals so the Status surface includes reason builders. Verification: direct CLI run for `std_http_basic.otter` produced the expected output including `status-builders=200 Fine|200 OK|Fine|true|200 OK`, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (10/10), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e, heartbeat wrapper summary).

[DONE] /goal Add indexed removal for `std:collections.Deque`. — DONE: added pure Otter Fusion `Deque.remove_at(index): T | null`, removing and returning the value at the same logical zero-based order used by `get` and `set`. Negative and out-of-range indexes return `null` without mutating. Expanded `std_collections_deque_basic.otter` for removals across the reversed-front/forward-back storage boundary plus negative and out-of-range checks. Updated docs/18, docs/24, ROADMAP, and goals so Deque's indexed surface includes reads, writes, and removals. Verification: direct CLI run for `std_collections_deque_basic.otter` produced the expected output including `remove_at=0/2/3/null/null left=Deque[1]`, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (10/10), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e, heartbeat wrapper summary).

[DONE] /goal Add indexed removal for `std:bytes.Bytes`. — DONE: added pure Otter Fusion `Bytes.remove_at(index): u8 | null`, removing and returning one byte at a zero-based index. Negative and out-of-range indexes return `null` without mutating. Expanded `std_bytes_basic.otter` for middle/front removals, invalid indexes, and remaining buffer contents. Updated docs/18, docs/24, ROADMAP, and goals so Bytes direct mutation helpers include indexed removal. Verification: direct CLI run for `std_bytes_basic.otter` produced the expected output including `remove_at=66/65/true/true left=bytes[67,90,91]`, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (10/10), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e, heartbeat wrapper summary).

[DONE] /goal Add immutable timezone replacement for `std:time.DateTime`. — DONE: added pure Otter Fusion `DateTime.with_timezone(tz): DateTime`, rebuilding a datetime with the same local date/time fields and a cloned replacement timezone. Expanded `std_time_datetime_basic.otter` for fixed-offset replacement and original datetime preservation. Updated docs/18, docs/24, ROADMAP, and goals so the DateTime value surface includes timezone replacement. Verification: direct CLI run for `std_time_datetime_basic.otter` produced the expected output including `builder=2024-02-29T23:59:58.123456789+01:00 original=2024-02-29T23:59:58.123456789Z tz=+01:00`, `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (10/10), `rustfmt --check crates/compiler/src/sema/stdlib.rs`, `git diff --check`, and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e, heartbeat wrapper summary).

[DONE] /goal Add portable validation for `std:process.Command` before provider execution. — DONE: added pure Otter Fusion `Command.validation_error(): IoError | null`, `Command.validate(): null | IoError`, and `Command.is_valid(): bool`. The validation layer rejects empty program names and explicit environment keys that are empty or contain `=`, and `Command.status()` / `Command.output()` now return those validation failures as `IoError` before crossing into Rust-backed execution hooks. Expanded `std_process_basic.otter` for valid/invalid value inspection and `std_process_exec_basic.otter` for execution-boundary validation failures. Updated docs/18, docs/24, ROADMAP, and goals so command validation is part of the current process API contract. Verification: direct CLI runs for `std_process_basic.otter` and `std_process_exec_basic.otter` produced the expected output including `validate=true true true ...` and `invalid=true true`; `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (10/10); `rustfmt --check crates/compiler/src/sema/stdlib.rs`; `git diff --check`; and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e, heartbeat wrapper summary).

[DONE] /goal Add portable validation for `std:fs.OpenOptions` before provider file opens. — DONE: added pure Otter Fusion `OpenOptions.validation_error(): IoError | null`, `OpenOptions.validate(): null | IoError`, and `OpenOptions.is_valid(): bool`. The validation layer rejects opens with no read/write/append access and rejects truncate/create/create_new flags unless write or append access is enabled. `File.open_with(path, options)` now returns those validation failures as `IoError` before crossing into the Rust-backed file-open provider hook. Expanded `std_fs_text_basic.otter` for valid options, invalid empty options, invalid create-without-write options, and execution-boundary validation failure. Updated docs/18, docs/24, ROADMAP, and goals so open-option validation is part of the current filesystem API contract. Verification: direct CLI run for `std_fs_text_basic.otter` produced the expected output including `open_options_validate=true true true ...`; `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (10/10); `rustfmt --check crates/compiler/src/sema/stdlib.rs`; `git diff --check`; and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e, heartbeat wrapper summary).

[DONE] /goal Fix `std:net:types.Url` equality/hash consistency for unordered query maps. — DONE: fixed the pure Otter Fusion `Url: Hash` implementation so it hashes scalar fields with nullable-field tags and folds query entries order-independently instead of hashing the rendered URL string. Also tightened nullable-field equality for `IpAddr.zone`, `Uri.authority/query/fragment`, and `Url.user/port/fragment`, avoiding direct nullable comparisons. Expanded `std_net_types_basic.otter` with two URLs built from identical query pairs inserted in opposite orders, proving `Url.eq(...)` and `Url.hash()` agree. Updated docs/18, docs/24, ROADMAP, and goals so URL query hashing is documented as order-insensitive. Verification: direct CLI run for `std_net_types_basic.otter` produced the expected output including `url-hash-order=true true`; `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (10/10); `rustfmt --check crates/compiler/src/sema/stdlib.rs`; `git diff --check`; and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e, heartbeat wrapper summary).

[DONE] /goal Fix `std:json.Json` structural hash consistency for unordered object maps. — DONE: replaced render-string-based `Json.hash()` with a pure Otter Fusion structural tree hash. The new hash keeps arrays order-sensitive, matches deep object equality by folding object key/value entries order-independently, and uses checked-arithmetic-friendly modular hash combiners instead of relying on wrapping overflow. Expanded `std_json_basic.otter` with equal objects built from opposite insertion orders proving equal hashes, plus an array-order regression proving arrays remain order-sensitive. Updated docs/18, docs/24, ROADMAP, and goals so JSON hashing is documented as structural with order-insensitive object entries. Verification: direct CLI run for `std_json_basic.otter` produced the expected output including `hash-order=true true array-order=false`; `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (10/10); `rustfmt --check crates/compiler/src/sema/stdlib.rs`; `git diff --check`; and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e, heartbeat wrapper summary).

[DONE] /goal Fix `std:http` equality/hash consistency for nullable status reasons and unordered header maps. — DONE: added pure Otter Fusion checked hash combiners and nullable string equality helpers to `std:http`. `Status.eq` now handles two absent reasons correctly and `Status.hash()` hashes the explicit reason state instead of only the rendered default phrase. `Headers.hash()` now mirrors header equality by folding normalized header names order-independently while preserving value order for repeated values under the same name. `HttpRequest` and `HttpResponse` equality/hash now use the value contracts of their nested method/version, URL/status, headers, and body fields rather than debug rendering. Expanded `std_http_basic.otter` with status null equality, reversed header insertion order, and request/response equality/hash regressions. Updated docs/18, docs/24, ROADMAP, and goals so HTTP hashing is documented as structural with order-insensitive header-name membership. Verification: direct CLI run for `std_http_basic.otter` produced the expected output including `hash-contract status=true headers=true/true req=true/true resp=true/true`; `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (10/10); `rustfmt --check crates/compiler/src/sema/stdlib.rs`; `git diff --check`; and `cargo test -p cli --test suite -- --nocapture` green (361/361 e2e, heartbeat wrapper summary).

[DONE] /goal Tighten `std:process` structural hash contracts for command/output values. — DONE: added pure Otter Fusion checked-friendly hash combiners to `std:process` and changed `Command.hash()` to tagged structural hashing: program, ordered args, explicit-vs-inherited env, env key/value membership, and cwd presence/value are folded consistently with command equality. `Output.hash()` now folds status, stdout, and stderr in field order instead of direct u64 addition. Expanded `std_process_basic.otter` with regressions proving reversed args are unequal with distinct hashes, equal explicit env maps inserted in opposite orders hash equally, and equal captured outputs hash equally. Updated docs/18, docs/24, ROADMAP, and goals so process hashing is documented as structural. Verification: direct CLI run for `std_process_basic.otter` produced the expected output including `hash-contract args=true env=true output=true`; broader verification follows in this continuation.

[DONE] /goal Add direct stdlib coverage and docs visibility for `std:task`. — DONE: added `tests/cases/stdlib/std_task_basic.otter`, a direct stdlib-module e2e case covering `Task.spawn` sync and async closures, executor `JoinHandle.join()`, cooperative `cancel()`, `detach()`, and `Cancelled` equality/clone/hash/stringification/debug behavior. Updated docs/18, docs/24, and docs/README so `std:task` appears in the standard-library diagrams/module tables and concurrency chapter card alongside `std:thread`, `std:sync`, and `std:async`. Verification: direct CLI run for `std_task_basic.otter` produced the expected output; `cargo test -p compiler sema::stdlib::tests -- --nocapture` green (10/10); `cargo fmt --check`; and `git diff --check`.

[DONE] /goal Implement an M:N work-stealing async executor + `Task.spawn` (`std:task`). — DONE: `spawn EXPR` and `Task.spawn` now run on the shared work-stealing executor with per-worker queues, stealing, global injection, timer/reactor wakeups, real cooperative cancellation, join/detach/cancel/abort handles, `Joined<R> | Panicked | Cancelled` results, task-held `Shared` cleanup, channel endpoint cleanup, GC safepoints for executor workers, async stdio integration, high-scale fanout/channel/timer/GC stress coverage, examples, docs, LSP hover/type coverage, and native/JIT parity across the non-stress and dedicated parity cases. Verification: `cargo test -p runtime -- --nocapture` green (116/116), `cargo test -p compiler --lib -- --nocapture` green (293/293), `cargo test -p backend -- --nocapture` green (222/222), `cargo fmt --check`, `git diff --check`, direct stress regressions for timeout loser cleanup and returned endpoint handles, and the full CLI e2e suite green after the final panic-matrix additions (`OTTER_SUITE_WORKERS=1 cargo test -p cli --test suite -- --nocapture`: 451/451 pass, 0 fail, 0 XFAIL).

[DONE] /goal Implement executor-multiplexed task-panic isolation for the M:N work-stealing executor + `Task.spawn`. — DONE: executor tasks are polled under the panic boundary at the poll call site, so a panicking `Task.spawn` or `spawn EXPR` future terminates only that task, publishes `Panicked`/re-propagates at the awaiter as appropriate, releases held `Shared` locks and roots through the existing unwind cleanup path, and leaves the shared worker thread alive to keep polling sibling work. Added e2e coverage for one-worker stress-GC `Task.spawn` panic storms with many siblings, four-worker stress-GC steal-contention panic storms, and a one-worker `spawn EXPR` panic-sibling case proving unrelated spawned futures still complete. Verification: direct runs produced `joined=112 panicked=16 cancelled=0 sum=7168`, `joined=481 panicked=31 cancelled=0 sum=122911`, and `count=64 sum=2016`; `cargo test -p runtime threads::tests -- --nocapture` green (31/31); `cargo fmt --check`; `git diff --check`; and the full CLI e2e suite green (`451 cases · 451 pass · 0 fail · 0 XFAIL`).

---

general:

/goal Analyze the current state of the project, go thought each doc, read all the necessary source code, run tests if needed. Then, update roadmap, ensure roadmap is up to date. Be detailed. We will use the roadmap to complete goals in goals.txt.

/goal Finish implementing the language end to end. Follow design, do many tests, follow CLAUDE.md rules. Maintain code organization, layers and modularity. Keep code clean and efficient. Apply logical optimizations when needed. Ensure we keep all the observability features. Keep up to day docs, lsp, extension. Finish the language end to end, keep ROADMAP up to date, use it. The objective is to finish every part one by one of a fully production ready language.

optimizations:

/goal apply optimizations to the backend. You can use possible-optimizations.txt as a reference but be smart and apply common logical optimization to the compiler and backend logic.

tooling:

/goal Implement the advanced package-manager features deferred in docs/17 and docs/23 (§7 dependency model, §8 manifest): real git-dependency FETCHING (sources are recorded today but not cloned), feature-gated optional-dependency resolution (`[features]` is parsed but optional deps are skipped), multi-major coexistence (incompatible majors currently error — allow them to coexist per the resolution model), and the publish metadata-sidecar (the server records empty `deps` for uploaded versions). Production quality at the level of rust/cargo, no shortcuts; follow the reserved/deferred decisions in docs/17 and ask first before any design change. Do many tests — unit + integration + e2e covering git fetch + checksum verification, feature resolution, multi-major graphs, and publish/resolve round-trips against the live registry server. Keep docs, ROADMAP and the lockfile format consistent and up to date.

/goal Finish the LSP follow-ups (docs/25): project-wide reverse references (find references/rename must also include files that IMPORT the open document, not just the open doc + its submodules) and body-position type-name go-to-definition (`var x: T`, `e as T`, and pattern type names — only item-level type positions resolve today). Production quality, no shortcuts; build a workspace index for reverse references. Do many tests — unit + integration covering cross-file reverse refs, rename across importers, and each new type-position goto case. Keep the VS Code extension working, and keep docs and ROADMAP consistent and up to date.

/goal Complete `otter_fusion fmt` (docs/23): add token-level intra-line spacing normalization and line wrapping (today fmt is whitespace/indentation-only). The blocker is comment preservation — the lexer currently discards ordinary comments (only `///`/`//!` doc comments become tokens), so implement a comment-retaining lex mode + faithful comment placement first, then the formatter on top. Production quality, no shortcuts; keep the existing safety invariant (re-lex the output and require an identical token stream, refusing to write otherwise) and idempotency. Ask first before any design change. Do many tests — unit + integration + e2e covering spacing rules, wrapping, comment preservation in all positions, idempotency, and `--check` as a CI gate; verify across all examples. Keep docs, the LSP `document_formatting` provider, and ROADMAP consistent and up to date.

new features:

/goal Design and build the full standard library as production infrastructure, not a bag of compiler magic. Stage it module-by-module according to docs/18 and docs/24: keep `core:*` compiler/runtime substrate and language-privileged utilities separate from `std:*` official library APIs and target-backed implementations; allow modules to be authored in Otter Fusion, Rust-backed runtime/compiler code, or mixed layers; route `core:*`/`std:*` availability through the stdlib catalog/provider architecture so future target/custom sysroots can replace, omit, or extend `std:*` packages without changing source-level import syntax; progressively move toolchain library definitions out of the monolithic embedded `PRELUDE_SRC` into maintainable std/core source modules or an equivalent well-structured toolchain package; keep the near-empty prelude rule; implement each module with unit, integration, and e2e tests, docs, examples, and LSP support. Priority slices after `std:error`/`std:bytes`/`std:io` contracts/`std:time::Duration`/`std:fs.Path`: `std:fs` (`File`, read/write/read_dir), `std:time` monotonic/system clocks, then `std:net`, `std:process`, `std:rand`, `std:log`, and the remaining collection/bytes/deque work. Do not change language design without asking.



/goal Implement the embedding engine `std:engine` (docs/26, ROADMAP Phase 7): run guest Otter Fusion inside a sandboxed isolate. Follow docs/26 exactly and the locked design decisions — bridged data crosses by COPY-BY-VALUE over a `@Bridge` repr-C layout (never a shared heap pointer), and resource isolation uses PER-ISOLATE runtime state (each isolate owns its heap/GC/registries, selected by a thread-local current-isolate pointer). Build on the proven macro-host substrate (crates/macros + crates/backend/macro_host). Production quality, no shortcuts; ask first before any language-design change. Stage it per ROADMAP Phase 7 (each step shippable): (1) single-isolate load/invoke returning primitives; (2) compile-time capability-whitelist enforcement of std:/core:/pkg:/file:; (3) the `@Bridge` ABI (frozen repr-C layout, bridge-compatibility checking, field-by-field copy marshalling, str-as-bytes); (4) host bindings into virtual `host:` modules + engine-managed bridge channels/Shared spanning the boundary + Unit entry introspection; (5) per-isolate runtime state threaded through every lang_* entry point with per-isolate stop-the-world GC; (6) hard limits (max_heap_bytes/max_alloc_bytes/timeout_ms/max_stack_depth via per-isolate accounting + deadline/cancel flag at existing safepoints) + Stats; (7) async entries driven on the isolate executor from the host's await (no user-visible block_on). Do many tests — unit + integration + e2e: capability denial as a compile error, @Bridge round-trips for every bridge type (host↔guest), host bindings + bridge channels, arbitrary-entry invocation by name, per-isolate heap independence, hard memory cap → OutOfMemory, timeout → Timeout (incl. tight-loop latency), guest-panic containment → GuestPanic, async entry awaited by a host; JIT + native parity + GC-stress. Keep docs/26, examples, LSP and ROADMAP consistent and up to date.
