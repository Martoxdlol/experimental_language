//@ kind: run
//@ description: str.split and char iteration
//@ stdout:
//~ 3
//~ 5
import { Future } from "core:prelude";
import { println } from "std:io";
function main(): Future<null> async {
  var parts = "a,b,c".split(",");
  await println("${parts.size()}");
  var n = 0;
  for ch in "otter" { n = n + 1; }
  await println("${n}");
  null
}
