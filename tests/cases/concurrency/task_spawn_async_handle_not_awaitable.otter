//@ kind: compile-error
//@ description: Task.spawn with an async closure still returns a JoinHandle; await join(), not the handle
//@ stderr: `await` requires a `Future`

import { Future } from "core:prelude";
import { Task } from "std:task";

function main(): Future<null> async {
  var _joined = await Task.spawn(() async => {
    7
  });
  null
}
