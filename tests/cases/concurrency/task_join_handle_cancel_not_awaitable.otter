//@ kind: compile-error
//@ description: Task JoinHandle.cancel is an immediate cancellation request, not an async join operation
//@ stderr: `await` requires a `Future`

import { Future } from "core:prelude";
import { Task } from "std:task";

function main(): Future<null> async {
  var handle = Task.spawn(() async => {
    null
  });
  var _cancelled = await handle.cancel();
  null
}
