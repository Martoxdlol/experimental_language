//@ kind: compile-error
//@ description: std:sync ChannelClosed.hash is ordinary marker value hashing, not an async channel operation
//@ stderr: `await` requires a `Future`

import { Future, Hash } from "core:prelude";
import { ChannelClosed } from "std:sync";

function main(): Future<null> async {
  var closed = ChannelClosed {};
  var _hash = await closed.hash();
  null
}
