//@ kind: compile-error
//@ description: Receiver.try_recv is an immediate non-blocking poll, not an explicit async receive surface
//@ stderr: `await` requires a `Future`

import { Future } from "core:prelude";
import { channel } from "std:sync";

function main(): Future<null> async {
  var ends = channel<i64>();
  var rx = ends.1;
  var _value = await rx.try_recv();
  null
}
