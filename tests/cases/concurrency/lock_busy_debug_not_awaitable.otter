//@ kind: compile-error
//@ description: std:sync LockBusy.debug is ordinary marker value rendering, not an async lock operation
//@ stderr: `await` requires a `Future`

import { Future } from "core:prelude";
import { Debug } from "std:fmt";
import { LockBusy } from "std:sync";

function main(): Future<null> async {
  var busy = LockBusy {};
  var _debug = await busy.debug();
  null
}
