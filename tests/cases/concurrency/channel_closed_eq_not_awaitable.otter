//@ kind: compile-error
//@ description: std:sync ChannelClosed equality is an ordinary marker value comparison, not an async channel operation
//@ stderr: `await` requires a `Future`

import { Eq, Future } from "core:prelude";
import { ChannelClosed } from "std:sync";

function main(): Future<null> async {
  var closed = ChannelClosed {};
  var _same = await (closed == ChannelClosed {});
  null
}
