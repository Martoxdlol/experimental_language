//@ kind: compile-error
//@ description: Thread.spawn returns a JoinHandle; await the handle's join() future instead of awaiting the handle itself
//@ stderr: `await` requires a `Future`

import { Future } from "core:prelude";
import { Thread } from "std:thread";

function main(): Future<null> async {
  var _joined = await Thread.spawn(() => 7);
  null
}
