//@ kind: compile-error
//@ description: Task.spawn returns a JoinHandle; await the handle's join() future instead of awaiting the handle itself
//@ stderr: `await` requires a `Future`

import { Future } from "core:prelude";
import { Task } from "std:task";

function main(): Future<null> async {
  var _joined = await Task.spawn(() => 7);
  null
}
