//@ kind: compile-error
//@ description: Thread JoinHandle.detach is an immediate fire-and-forget control, not an async join operation
//@ stderr: `await` requires a `Future`

import { Future } from "core:prelude";
import { Thread } from "std:thread";

function main(): Future<null> async {
  var handle = Thread.spawn(() => 7);
  var _detached = await handle.detach();
  null
}
