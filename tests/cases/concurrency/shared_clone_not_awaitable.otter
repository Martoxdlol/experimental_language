//@ kind: compile-error
//@ description: Shared.clone is ordinary handle cloning for the same cell, not an async lock operation
//@ stderr: `await` requires a `Future`

import { Future } from "core:prelude";
import { Shared } from "std:sync";

function main(): Future<null> async {
  var state: Shared<i64> = Shared.new(1);
  var _clone = await state.clone();
  null
}
