//@ kind: compile-error
//@ description: the executor driver is internal; user code awaits futures instead of calling block_on
//@ stderr: cannot find value `block_on` in scope

import { Future } from "core:prelude";
import { println } from "std:io";

function answer(): Future<i64> async {
  42
}

function main(): Future<null> async {
  var n: i64 = block_on(answer());
  await println(n as str);
}
