//@ kind: compile-error
//@ description: calling a function with too many arguments
//@ stderr: expected 2 argument(s), found 3
import { println } from "std:io";
function add(a: i64, b: i64): i64 { a + b }
function main() {
  println("${add(1, 2, 3)}");
}
