//@ kind: compile-error
//@ description: docs/19 §13 — a @Variadic call must still supply the whole fixed prefix.
//@ stderr: expected 3 argument(s), found 0
import { CString } from "core:ffi";
@Variadic
extern function snprintf(buf: *u8, size: u64, fmt: *u8): i32;
function main() { snprintf(); }
