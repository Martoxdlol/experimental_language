//@ kind: compile-error
//@ description: docs/19 §7 — @CallConv is only meaningful on an extern function; on an ordinary function it is rejected.
//@ stderr: `@CallConv` is only valid on an `extern function`
@CallConv("c")
function foo() {}
function main() {}
