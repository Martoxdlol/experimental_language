//@ kind: compile-error
//@ description: docs/19 §13 — a @Variadic extern needs at least one fixed parameter (the named prefix the promotions are defined against).
//@ stderr: needs at least one fixed parameter
@Variadic
extern function foo(): i32;
function main() {}
