//@ kind: compile-error
//@ description: @RefCounted may only decorate a struct, not a function
//@ stderr: `@RefCounted` may only decorate a `struct`
import { println } from "std:io";
@RefCounted
function main() { println("x"); }
