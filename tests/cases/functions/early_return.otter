//@ kind: run
//@ description: early return from a function
//@ stdout:
//~ found
//~ none
import { println } from "std:io";
function check(n: i64): str {
  if n > 0 { return "found"; }
  "none"
}
function main() {
  println(check(5));
  println(check(-1));
}
