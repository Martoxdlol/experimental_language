//@ kind: run
//@ description: nested loops with inner break
//@ stdout:
//~ 9
import { println } from "std:io";
function main() {
  var count = 0;
  var i = 0;
  while i < 3 {
    var j = 0;
    while j < 5 {
      if j == 3 { break; }
      count = count + 1;
      j = j + 1;
    }
    i = i + 1;
  }
  println("${count }");
}
