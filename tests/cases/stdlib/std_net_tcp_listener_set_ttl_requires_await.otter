//@ kind: compile-error
//@ description: std:net TcpListener.set_ttl returns a Future and must be awaited
//@ stderr: found `Future<

import { IoError } from "std:io";
import { TcpListener } from "std:net";

function requires_await(listener: TcpListener) {
  var _set: null | IoError = listener.set_ttl(64u32);
}
