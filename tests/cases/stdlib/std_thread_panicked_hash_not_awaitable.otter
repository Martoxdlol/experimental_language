//@ kind: compile-error
//@ description: std:thread Panicked.hash is ordinary marker value hashing, not an async join operation
//@ stderr: `await` requires a `Future`

import { Future, Hash } from "core:prelude";
import { Panicked } from "std:thread";

function main(): Future<null> async {
  var panic_value = Panicked { message: "boom" };
  var _hash = await panic_value.hash();
  null
}
