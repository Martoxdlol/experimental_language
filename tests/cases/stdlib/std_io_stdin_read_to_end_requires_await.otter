//@ kind: compile-error
//@ description: concrete std:io stdin().read_to_end returns Future<i64 | IoError> and must be awaited before use
//@ stderr: expected `i64 | IoError`, found `Future<i64 | IoError>`

import { Bytes } from "std:bytes";
import { IoError, stdin } from "std:io";

function main() {
  var _n: i64 | IoError = stdin().read_to_end(Bytes.new());
}
