//@ kind: compile-error
//@ description: std:net TcpStream.ttl returns a Future and must be awaited
//@ stderr: found `Future<

import { IoError } from "std:io";
import { TcpStream } from "std:net";

function requires_await(stream: TcpStream) {
  var _ttl: u32 | IoError = stream.ttl();
}
