//@ kind: compile-error
//@ description: std:net TcpStream.write_all returns a Future and must be awaited before using completion
//@ stderr: expected `null | IoError`, found `Future<null | IoError>`

import { Bytes } from "std:bytes";
import { IoError } from "std:io";
import { TcpStream } from "std:net";

function inspect(stream: TcpStream) {
  var _written: null | IoError = stream.write_all(Bytes.from_str("ping"));
}
