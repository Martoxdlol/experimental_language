//@ kind: compile-error
//@ description: std:process env returns a Future and cannot be used as an immediate environment value
//@ stderr: Future<

import { env } from "std:process";

function main() {
  var value: str | null = env("PATH");
}
