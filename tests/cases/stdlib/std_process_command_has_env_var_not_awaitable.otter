//@ kind: compile-error
//@ description: std:process Command.has_env_var is an immediate pure command environment predicate
//@ stderr: `await` requires a `Future`

import { Future } from "core:prelude";
import { Command } from "std:process";

function main(): Future<null> async {
  var _has = await Command.new("true").env_var("MODE", "test").has_env_var("MODE");
  null
}
