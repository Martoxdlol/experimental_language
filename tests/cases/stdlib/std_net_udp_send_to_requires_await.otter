//@ kind: compile-error
//@ description: std:net UdpSocket.send_to returns a Future and must be awaited
//@ stderr: expected `i64 | IoError`, found `Future<i64 | IoError>`

import { Bytes } from "std:bytes";
import { IoError } from "std:io";
import { UdpSocket } from "std:net";
import { SocketAddr } from "std:net/types";

function requires_await(socket: UdpSocket, addr: SocketAddr) {
  var _sent: i64 | IoError = socket.send_to(Bytes.from_str("ping"), addr);
}
