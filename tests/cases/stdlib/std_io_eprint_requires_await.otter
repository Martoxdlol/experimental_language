//@ kind: compile-error
//@ description: std:io eprint returns Future<null> and must be awaited before assigning to null
//@ stderr: expected `null`, found `Future<null>`

import { eprint } from "std:io";

function main() {
  var _err: null = eprint("stderr");
}
