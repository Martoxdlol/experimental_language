//@ kind: compile-error
//@ description: std:fs File.open_with returns Future<File | IoError> and must be awaited before use
//@ stderr: found `Future<

import { File, Path, open_options } from "std:fs";
import { IoError } from "std:io";

function main() {
  var _file: File | IoError = File.open_with(Path.new("/tmp/otter_fusion_requires_await.txt"), open_options().with_read(true));
}
