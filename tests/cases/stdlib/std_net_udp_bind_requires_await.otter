//@ kind: compile-error
//@ description: std:net UdpSocket.bind returns a Future and must be awaited
//@ stderr: found `Future<

import { IoError } from "std:io";
import { UdpSocket } from "std:net";
import { SocketAddr, ip_v4, ip_v6, socket_addr } from "std:net/types";

function main() {
  var addr = socket_addr(ip_v4(127u8, 0u8, 0u8, 1u8), 0u16);
  var _value: UdpSocket | IoError = UdpSocket.bind(addr);
}
