//@ kind: compile-error
//@ description: std:net UdpSocket.multicast_ttl_v4 returns a Future and must be awaited
//@ stderr: found `Future<

import { IoError } from "std:io";
import { UdpSocket } from "std:net";

function requires_await(socket: UdpSocket) {
  var _value: u32 | IoError = socket.multicast_ttl_v4();
}
