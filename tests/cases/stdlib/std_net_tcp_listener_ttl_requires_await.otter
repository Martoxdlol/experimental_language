//@ kind: compile-error
//@ description: std:net TcpListener.ttl returns a Future and must be awaited
//@ stderr: found `Future<

import { IoError } from "std:io";
import { TcpListener } from "std:net";

function requires_await(listener: TcpListener) {
  var _ttl: u32 | IoError = listener.ttl();
}
