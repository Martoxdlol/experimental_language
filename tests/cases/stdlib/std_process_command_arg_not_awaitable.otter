//@ kind: compile-error
//@ description: std:process Command.arg is an immediate pure command value builder
//@ stderr: `await` requires a `Future`

import { Future } from "core:prelude";
import { Command } from "std:process";

function main(): Future<null> async {
  var _command = await Command.new("printf").arg("hello");
  null
}
