//@ kind: compile-error
//@ description: std:async TimedOut.clone is ordinary marker value cloning, not an async runtime operation
//@ stderr: `await` requires a `Future`

import { Future } from "core:prelude";
import { TimedOut } from "std:async";

function main(): Future<null> async {
  var timed = TimedOut {};
  var _copy = await timed.clone();
  null
}
