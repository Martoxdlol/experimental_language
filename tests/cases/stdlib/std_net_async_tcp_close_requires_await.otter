//@ kind: compile-error
//@ description: std:net AsyncTcpStream.close returns a Future and must be awaited
//@ stderr: found `Future<

import { IoError } from "std:io";
import { AsyncTcpStream } from "std:net";

function requires_await(stream: AsyncTcpStream) {
  var _closed: null | IoError = stream.close();
}
