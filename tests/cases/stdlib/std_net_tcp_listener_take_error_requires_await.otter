//@ kind: compile-error
//@ description: std:net TcpListener.take_error returns a Future and must be awaited
//@ stderr: found `Future<

import { IoError } from "std:io";
import { TcpListener } from "std:net";

function requires_await(listener: TcpListener) {
  var _error: null | IoError = listener.take_error();
}
