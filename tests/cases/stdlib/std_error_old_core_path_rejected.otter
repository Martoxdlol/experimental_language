//@ kind: compile-error
//@ description: Error is stdlib API, not compiler substrate; the old core:error path is rejected
//@ stderr: no built-in module `core:error`
import { Error } from "core:error";

function main() {}
