//@ kind: compile-error
//@ description: IO contracts are stdlib API, not compiler substrate; the old core:io path is rejected
//@ stderr: no built-in module `core:io`

import { Reader } from "core:io";

function main() {}
