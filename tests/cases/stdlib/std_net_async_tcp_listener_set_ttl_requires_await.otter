//@ kind: compile-error
//@ description: std:net AsyncTcpListener.set_ttl returns a Future and must be awaited
//@ stderr: found `Future<

import { IoError } from "std:io";
import { AsyncTcpListener } from "std:net";

function requires_await(listener: AsyncTcpListener) {
  var _set: null | IoError = listener.set_ttl(64u32);
}
