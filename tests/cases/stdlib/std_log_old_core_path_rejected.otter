//@ kind: compile-error
//@ description: logging is ordinary stdlib API, not compiler substrate; the old core:log path is rejected
//@ stderr: no built-in module `core:log`

import { Level } from "core:log";

function main() {}
