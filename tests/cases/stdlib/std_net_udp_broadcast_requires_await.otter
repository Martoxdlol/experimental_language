//@ kind: compile-error
//@ description: std:net UdpSocket.broadcast returns a Future and must be awaited
//@ stderr: found `Future<

import { IoError } from "std:io";
import { UdpSocket } from "std:net";

function requires_await(socket: UdpSocket) {
  var _value: bool | IoError = socket.broadcast();
}
