//@ kind: compile-error
//@ description: std:process Child.stdout is a non-Future current-stdio placeholder accessor; streamed child stdout remains planned async-stream work
//@ stderr: `await` requires a `Future`

import { Future } from "core:prelude";
import { Child, Command } from "std:process";

function main(): Future<null> async {
  var spawned = await Command.new("true").spawn();
  if spawned is Child {
    var _stdout = await (spawned as Child).stdout();
  }
  null
}
