//@ kind: compile-error
//@ description: std:net UdpSocket.leave_multicast_v4 returns a Future and must be awaited
//@ stderr: found `Future<

import { IoError } from "std:io";
import { UdpSocket } from "std:net";
import { SocketAddr, ip_v4, ip_v6, socket_addr } from "std:net/types";

function requires_await(socket: UdpSocket) {
  var _value: null | IoError = socket.leave_multicast_v4(ip_v4(224u8, 0u8, 0u8, 251u8), ip_v4(0u8, 0u8, 0u8, 0u8));
}
