//@ kind: compile-error
//@ description: Random-number helpers are ordinary stdlib API, not compiler substrate; core:rand is rejected
//@ stderr: no built-in module `core:rand`

import { SeededRng } from "core:rand";

function main() {}
