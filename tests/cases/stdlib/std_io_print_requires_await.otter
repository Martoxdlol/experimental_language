//@ kind: compile-error
//@ description: std:io print returns Future<null> and must be awaited before assigning to null
//@ stderr: expected `null`, found `Future<null>`

import { print } from "std:io";

function main() {
  var _out: null = print("stdout");
}
