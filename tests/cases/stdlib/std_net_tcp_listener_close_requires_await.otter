//@ kind: compile-error
//@ description: std:net TcpListener.close returns a Future and must be awaited
//@ stderr: found `Future<

import { IoError } from "std:io";
import { TcpListener } from "std:net";

function requires_await(listener: TcpListener) {
  var _closed: null | IoError = listener.close();
}
