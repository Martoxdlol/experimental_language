//@ kind: compile-error
//@ description: std:net TcpStream.set_ttl returns a Future and must be awaited
//@ stderr: found `Future<

import { IoError } from "std:io";
import { TcpStream } from "std:net";

function requires_await(stream: TcpStream) {
  var _set: null | IoError = stream.set_ttl(64u32);
}
