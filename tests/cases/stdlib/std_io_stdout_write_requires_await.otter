//@ kind: compile-error
//@ description: concrete std:io stdout().write returns Future<i64 | IoError> and must be awaited before use
//@ stderr: expected `i64 | IoError`, found `Future<i64 | IoError>`

import { Bytes } from "std:bytes";
import { IoError, stdout } from "std:io";

function main() {
  var _n: i64 | IoError = stdout().write(Bytes.from_str("x"));
}
