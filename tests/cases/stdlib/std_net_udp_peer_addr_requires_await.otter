//@ kind: compile-error
//@ description: std:net UdpSocket.peer_addr returns a Future and must be awaited
//@ stderr: found `Future<

import { IoError } from "std:io";
import { UdpSocket } from "std:net";
import { SocketAddr, ip_v4, ip_v6, socket_addr } from "std:net/types";

function requires_await(socket: UdpSocket) {
  var _value: SocketAddr | IoError = socket.peer_addr();
}
