//@ kind: compile-error
//@ description: Hasher utilities are ordinary stdlib API, not compiler substrate; core:hash is rejected
//@ stderr: no built-in module `core:hash`

import { DefaultHasher } from "core:hash";

function main() {}
