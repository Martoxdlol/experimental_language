//@ kind: compile-error
//@ description: std:io stderr() constructs an ordinary Stderr handle; await the handle's write/write_all/flush methods instead
//@ stderr: `await` requires a `Future`

import { Future } from "core:prelude";
import { stderr } from "std:io";

function main(): Future<null> async {
  var _error = await stderr();
  null
}
