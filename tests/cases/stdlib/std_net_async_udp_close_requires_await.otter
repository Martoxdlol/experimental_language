//@ kind: compile-error
//@ description: std:net AsyncUdpSocket.close returns a Future and must be awaited
//@ stderr: found `Future<

import { IoError } from "std:io";
import { AsyncUdpSocket } from "std:net";

function requires_await(socket: AsyncUdpSocket) {
  var _value: null | IoError = socket.close();
}
