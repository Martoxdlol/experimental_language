//@ kind: compile-error
//@ description: std:net TcpStream.write returns a Future and must be awaited before using the byte count
//@ stderr: expected `i64 | IoError`, found `Future<i64 | IoError>`

import { Bytes } from "std:bytes";
import { IoError } from "std:io";
import { TcpStream } from "std:net";

function inspect(stream: TcpStream) {
  var _written: i64 | IoError = stream.write(Bytes.from_str("ping"));
}
