//@ kind: compile-error
//@ description: std:io BufReader.lines returns an ordinary iterator, not an explicit async stream surface
//@ stderr: `await` requires a `Future`

import { Future } from "core:prelude";
import { Bytes } from "std:bytes";
import { buf_reader } from "std:io";

function main(): Future<null> async {
  var reader = buf_reader(Bytes.from_str("line\n").cursor());
  var _lines = await reader.lines();
  null
}
