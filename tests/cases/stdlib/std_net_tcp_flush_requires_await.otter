//@ kind: compile-error
//@ description: std:net TcpStream.flush returns a Future and must be awaited before using completion
//@ stderr: expected `null | IoError`, found `Future<null | IoError>`

import { IoError } from "std:io";
import { TcpStream } from "std:net";

function inspect(stream: TcpStream) {
  var _flushed: null | IoError = stream.flush();
}
