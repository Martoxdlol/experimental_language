//@ kind: compile-error
//@ description: std:net AsyncTcpStream.peer_addr returns a Future and must be awaited
//@ stderr: found `Future<

import { IoError } from "std:io";
import { AsyncTcpStream } from "std:net";
import { SocketAddr } from "std:net/types";

function requires_await(stream: AsyncTcpStream) {
  var _addr: SocketAddr | IoError = stream.peer_addr();
}
