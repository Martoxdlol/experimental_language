//@ kind: compile-error
//@ description: Formatting contracts are stdlib API, not compiler substrate; the old core:fmt path is rejected
//@ stderr: no built-in module `core:fmt`

import { Display } from "core:fmt";

function main() {}
