//@ kind: compile-error
//@ description: sync runtime handles are std API, not core compiler substrate; core:sync is rejected
//@ stderr: no built-in module `core:sync`
import { Shared, channel } from "core:sync";

function main() {}
