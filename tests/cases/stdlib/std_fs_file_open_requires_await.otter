//@ kind: compile-error
//@ description: std:fs File.open returns Future<File | IoError> and must be awaited before use
//@ stderr: found `Future<

import { File, Path } from "std:fs";
import { IoError } from "std:io";

function main() {
  var _file: File | IoError = File.open(Path.new("/tmp/otter_fusion_requires_await.txt"));
}
