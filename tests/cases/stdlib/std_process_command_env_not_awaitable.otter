//@ kind: compile-error
//@ description: std:process Command.env is an immediate pure command environment snapshot
//@ stderr: `await` requires a `Future`

import { Future } from "core:prelude";
import { Command } from "std:process";

function main(): Future<null> async {
  var _env = await Command.new("true").env();
  null
}
