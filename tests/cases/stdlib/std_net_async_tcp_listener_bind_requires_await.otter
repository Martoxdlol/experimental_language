//@ kind: compile-error
//@ description: std:net AsyncTcpListener.bind returns a Future and must be awaited
//@ stderr: found `Future<

import { IoError } from "std:io";
import { AsyncTcpListener } from "std:net";
import { SocketAddr } from "std:net/types";

function requires_await(addr: SocketAddr) {
  var _listener: IoError | AsyncTcpListener = AsyncTcpListener.bind(addr);
}
