//@ kind: compile-error
//@ description: std:fs write_string returns Future<null | IoError> and must be awaited before use
//@ stderr: expected `null | IoError`, found `Future<null | IoError>`

import { Future } from "core:prelude";
import { IoError } from "std:io";
import { Path, write_string } from "std:fs";

function main(): Future<null> async {
  var _written: null | IoError = write_string(Path.new("/tmp/otter_fusion_requires_await.txt"), "x");
  null
}
