//@ kind: compile-error
//@ description: std:net TcpStream.connect returns a Future and must be awaited before using the stream
//@ stderr: expected `TcpStream | IoError`, found `Future<TcpStream | IoError>`

import { TcpStream } from "std:net";
import { IoError } from "std:io";
import { ip_v4, socket_addr } from "std:net/types";

function main() {
  var addr = socket_addr(ip_v4(127u8, 0u8, 0u8, 1u8), 9u16);
  var _connected: TcpStream | IoError = TcpStream.connect(addr);
}
