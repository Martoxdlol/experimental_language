//@ kind: compile-error
//@ description: thread handles are std runtime API, not core compiler substrate; core:thread is rejected
//@ stderr: no built-in module `core:thread`
import { Thread } from "core:thread";

function main() {}
