//@ kind: compile-error
//@ description: std:net AsyncUdpSocket.take_error returns a Future and must be awaited
//@ stderr: found `Future<

import { IoError } from "std:io";
import { AsyncUdpSocket } from "std:net";

function requires_await(socket: AsyncUdpSocket) {
  var _value: IoError | null = socket.take_error();
}
