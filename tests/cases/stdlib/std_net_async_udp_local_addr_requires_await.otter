//@ kind: compile-error
//@ description: std:net AsyncUdpSocket.local_addr returns a Future and must be awaited
//@ stderr: found `Future<

import { IoError } from "std:io";
import { AsyncUdpSocket } from "std:net";
import { SocketAddr, ip_v4, ip_v6, socket_addr } from "std:net/types";

function requires_await(socket: AsyncUdpSocket) {
  var _value: SocketAddr | IoError = socket.local_addr();
}
