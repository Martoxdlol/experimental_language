//@ kind: panic
//@ exit: 3
//@ description: exit(n) terminates with the given code after output
//@ stdout:
//~ bye
import { println } from "std:io";
import { exit } from "std:process";
function main() {
  println("bye");
  exit(3);
}
