//@ kind: panic
//@ description: shift amount >= bit width panics in both profiles
//@ stderr: shift amount >= bit width
import { Future } from "core:prelude";
import { println } from "std:io";
function main(): Future<null> async {
  var x = 1;
  await println("${x << 64}");
  null
}
